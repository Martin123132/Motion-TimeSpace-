# Post-Checkpoint Work

This folder is a protected sandbox for the motion-load simplification route.

It sits beside:

```text
formalization-workbench/
```

so the existing derivation path remains intact while the new route is tested.

## Rule

Do not alter the main workbench claims, ledgers, or numbered files from this
folder unless the post-checkpoint route passes its promotion gates.

## Starting Point

The pre-pivot checkpoint is:

```text
00-pre-pivot-checkpoint.md
```

The first new route contract is:

```text
01-motion-load-route-contract.md
```

The first local reduction gate is:

```text
02-motion-load-local-GR-reduction.md
```

The reciprocity-origin audit is:

```text
03-reciprocal-routing-parent-origin.md
```

The no-smuggling action contract is:

```text
04-vacuum-reciprocity-action-contract.md
```

The reciprocal theorem attempt is:

```text
05-reciprocity-theorem-attempt.md
```

The source-neutrality gate is:

```text
06-reciprocal-charge-source-neutrality.md
```

The nonpropagating constraint gate is:

```text
07-nonpropagating-reciprocity-constraint.md
```

The phase-volume origin audit is:

```text
08-phase-volume-reciprocity-origin.md
```

The Hamiltonian radial-cell audit is:

```text
09-hamiltonian-radial-cell-derivation.md
```

The observer-map symplectic contract is:

```text
10-observer-map-symplectic-contract.md
```

The cell-current origin attempt is:

```text
11-cell-current-origin-attempt.md
```

The gauge-Noether origin audit is:

```text
12-gauge-noether-origin-audit.md
```

The local closure PPN benchmark is:

```text
13-local-closure-PPN-benchmark.md
```

The closure-deviation PPN sensitivity budget is:

```text
14-closure-deviation-PPN-sensitivity.md
```

The local observables data map is:

```text
15-local-observables-data-map.md
```

The local bounds gate runner is:

```text
16-local-bounds-gate-runner.md
```

The post-checkpoint promotion gate summary is:

```text
17-post-checkpoint-promotion-gate-summary.md
```

The parent-action versus empirical-pillar decision is:

```text
18-parent-action-or-empirical-pillar-decision.md
```

The constrained parent-action skeleton is:

```text
19-constrained-parent-action-skeleton.md
```

The empirical pillar test queue is:

```text
20-empirical-pillar-test-queue.md
```

The cosmology parent-bridge audit is:

```text
21-cosmology-parent-bridge-audit.md
```

The cosmology bridge run manifest is:

```text
22-cosmology-bridge-run-manifest.md
```

The strict cosmology branch contract is:

```text
23-strict-cosmology-branch-contract.md
```

The cosmology preflight execution plan is:

```text
24-cosmology-preflight-execution-plan.md
```

The cosmology preflight safe-run readout is:

```text
25-cosmology-preflight-safe-run-or-wrapper.md
```

The isolated likelihood preflight wrapper is:

```text
26-isolated-likelihood-preflight-wrapper.md
```

The strict cosmology numeric lock is:

```text
27-strict-cosmology-numeric-lock.md
```

The isolated growth/CMB first score runner is:

```text
28-isolated-growth-CMB-first-score-runner.md
```

The growth/CMB score readout and robustness gate is:

```text
29-growth-CMB-score-readout-and-robustness-gate.md
```

The CMB distance-prior implementation audit is:

```text
30-CMB-distance-prior-implementation-audit.md
```

The repaired growth/CMB score is:

```text
31-repaired-growth-CMB-score.md
```

The repaired score readout and decision is:

```text
32-repaired-score-readout-and-decision.md
```

The CMB calibration freedom audit is:

```text
33-CMB-calibration-freedom-audit.md
```

The CMB-compatible MTS limit contract is:

```text
34-CMB-compatible-MTS-limit-contract.md
```

The early-time decoupling/calibration derivation is:

```text
35-early-time-decoupling-or-calibration-derivation.md
```

The parent CMB calibration-map attempt is:

```text
36-parent-CMB-calibration-map-attempt.md
```

The CMB-calibrated joint growth stress is:

```text
37-CMB-calibrated-joint-growth-stress.md
```

The calibrated closure holdout contract is:

```text
38-calibrated-closure-holdout-contract.md
```

The calibrated background backreaction guardrail is:

```text
39-calibrated-background-backreaction-guardrail.md
```

The fresh-holdout or official-likelihood roadmap is:

```text
40-fresh-holdout-or-official-likelihood-roadmap.md
```

The calibrated H(z) covariance guardrail is:

```text
41-calibrated-Hz-covariance-guardrail.md
```

## Current Status

The old path is not discarded.

The new question is:

```text
Can motion-load, clock residue, and spatial routing be made more primitive than
the motion field, with the previous fields emerging as bookkeeping variables?
```

If yes, this may simplify the theory.

If no, the original workbench path remains available.
