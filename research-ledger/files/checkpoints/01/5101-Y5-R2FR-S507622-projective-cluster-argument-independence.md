# 5101 — S507622 projective-cluster argument independence

The 5097 kinematic identity is independent of the external scattering argument.

For `p'=lambda p` and `C=sqrt((1-c)/(1+c))`, every global factor root is a ratio of momentum-linear forms of equal degree. The common scale cancels exactly in `plus_u`, `plus_v`, `minus_u`, and `minus_v`. Therefore the projective relation

`p_g2(q*)=-sqrt(1-x) p_decay(q*)`

forces all four `direct:g2` and `subtraction:decay` factor roots to coincide for every finite nonzero `C`, not just A00.

The generator checks both reciprocal projective roots across all fifteen locked E040 arguments, verifies that same-source poles stay separate, and applies the theorem to the sole unstable A14 residue row. The authorization remains exact-job scoped to A14 in the runner.

Outputs:

- `scripts/Y5_R2FR_5101_S507622_projective_cluster_argument_independence.py`
- `source-intake/functional_rg/5101/S507622_projective_cluster_argument_independence.json`
- `source-intake/mts_residuals/P8_Y5_BRR545_5101_VALIDATION.csv`
