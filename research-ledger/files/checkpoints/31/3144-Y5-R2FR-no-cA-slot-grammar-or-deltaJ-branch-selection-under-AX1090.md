# 3144 - No-cA Slot Grammar or delta_J Branch Selection under AX1090

Private checkpoint. This follows 3143 by trying to close the last missing coupling throat:

```text
same-current owner from action variation
+ no parent slot for c_A(Xhat) A_Q J_A
+ no parent slot for w_A S_A
=> delta_J = 0.
```

The result is useful but not a zero theorem.

## Result

The no-cA/no-wA grammar route has the right exact shape:

```text
Allowed[S_parent]
only contains ordinary matter/current terms built from
q(Phi), Obs_e(Q_obs), A_Q(q), fixed theta_A/n_A, matter fields,
and universal constants.
```

If the parent object language signs that grammar, then:

```text
c_A(Xhat) A_Q J_A
w_A S_A
```

are not legal source-only slots unless the coefficient is fixed representation/current data or a universal common action scale. In that signed case the 3143 theorem fires:

```text
Lie_v J_Q = 0,
delta_J = 0,
beta_source_alpha = 0,
Delta_T_EM^J = 0,
Delta(GM)_J = 0.
```

But the current corpus does not yet sign the required parent grammar.

## What Fails for Claim

The evidence stack from 3056, 2784, 1890, 3123, and 3143 shows the closure route, but leaves these gates unsigned:

| gate | status | consequence |
|---|---|---|
| typed parent object language | `fail_for_claim` | no global theorem forbids all hidden/current source slots |
| no `c_A(Xhat) A_Q J_A` slot | `fail_for_claim` | a current normalization spurion can still be written as a countermodel |
| no `w_A S_A` source-prefactor slot | `fail_for_claim` | relative matter-action weights are still not parent-killed |
| no hidden/readout/radiative spurion return | `fail_for_claim` | post-quotient re-entry is not excluded |
| `delta_J=0` from grammar | `not_claim_ready` | zero route stays conditional only |

So this is not a place where we should pretend a missing axiom is a derivation.

## Surviving Countermodel

If the parent admits a hidden/current spurion `sigma_J`, then both terms remain typeable enough to block a zero claim:

```text
c_A(sigma_J) A_Q J_A,
w_A(sigma_J) S_A.
```

That countermodel does not prove the finite branch is physically real. It proves only that the present corpus has not killed it.

## Branch Selection

Because the no-slot theorem is not signed, 3144 selects the conservative finite branch:

```text
before_variation_current_source_insertion.
```

This means a finite current/source normalization, if present, is carried before Maxwell solve and Hilbert variation. That is the most scrutiny-safe branch because it does not hide the problem in readout language. It projects into:

```text
F[J],
T_EM[J],
material Coulomb response,
source GM,
WEP/R10 source-test products.
```

The retained alternatives are:

| branch | retained status | observable risk |
|---|---|---|
| universal calibrated current unit | possible subcase | raw changes may absorb into measured charge/G unless differential |
| post-variation readout selector | retained alternative | cannot change Hilbert source GM directly |
| radiative/effective-action re-entry | retained alternative | needs separate action-level vs readout-only audit |

## Residuals Carried Forward

The selected finite branch carries these nonclaim rows:

| residual | status | needed next |
|---|---|---|
| `delta_J_before` | `MISSING_PARENT_COEFFICIENT_OR_ZERO_GRAMMAR` | coefficient origin, units, source/test legs, no-cancellation policy |
| `C_J_TA6V_minus_PtRh10` | smoke only | replace convention with source-backed material/readout tensor |
| `abs_deltaJ_one_channel_envelope` | smoke only | direct MTS coefficient or zero theorem |
| `Delta_GM_J` | `MISSING_DELTAJ_TO_SOURCE_GM_KERNEL` | EM binding fraction, source worldtube, GM denominator lock, PPN/orbital kernel |
| `beta_source_alpha` | `MISSING_SOURCE_TEST_CURRENT_LEGS` | beta source/test legs, `tau_WEP`, `tau_R10`, material/source maps |

## Decision

3144 makes a real fork decision:

```text
do not promote delta_J = 0 from grammar;
select the before-variation finite delta_J branch;
derive the first delta_J_before projection kernel or source-backed coefficient next.
```

This stops the loop where the work keeps returning to "maybe the coupling is zero" without a parent grammar theorem.

## Runner Artifacts

| artifact | path |
|---|---|
| inputs | `D:\Users\ollet\Desktop\Turn an intuitive research programme into a formal field-theoretic framework\Motion-TimeSpace--main\post-checkpoint-work\source-intake\mts_residuals\P8_Y5_R2FR_3144_INPUTS.csv` |
| no-cA grammar theorem | `D:\Users\ollet\Desktop\Turn an intuitive research programme into a formal field-theoretic framework\Motion-TimeSpace--main\post-checkpoint-work\source-intake\mts_residuals\P8_Y5_R2FR_3144_NO_CA_GRAMMAR_THEOREM.csv` |
| grammar gates | `D:\Users\ollet\Desktop\Turn an intuitive research programme into a formal field-theoretic framework\Motion-TimeSpace--main\post-checkpoint-work\source-intake\mts_residuals\P8_Y5_R2FR_3144_GRAMMAR_GATES.csv` |
| branch selection | `D:\Users\ollet\Desktop\Turn an intuitive research programme into a formal field-theoretic framework\Motion-TimeSpace--main\post-checkpoint-work\source-intake\mts_residuals\P8_Y5_R2FR_3144_DELTAJ_BRANCH_SELECTION.csv` |
| residual rows | `D:\Users\ollet\Desktop\Turn an intuitive research programme into a formal field-theoretic framework\Motion-TimeSpace--main\post-checkpoint-work\source-intake\mts_residuals\P8_Y5_R2FR_3144_SELECTED_DELTAJ_RESIDUAL_ROWS.csv` |
| decision | `D:\Users\ollet\Desktop\Turn an intuitive research programme into a formal field-theoretic framework\Motion-TimeSpace--main\post-checkpoint-work\source-intake\mts_residuals\P8_Y5_R2FR_3144_DECISION.csv` |
| validation | `D:\Users\ollet\Desktop\Turn an intuitive research programme into a formal field-theoretic framework\Motion-TimeSpace--main\post-checkpoint-work\source-intake\mts_residuals\P8_Y5_R2FR_3144_VALIDATION.csv` |
| runner | `D:\Users\ollet\Desktop\Turn an intuitive research programme into a formal field-theoretic framework\Motion-TimeSpace--main\post-checkpoint-work\scripts\Y5_R2FR_3144_no_cA_slot_grammar_or_deltaJ_branch_selection.py` |

## Next Target

3145 should take the selected finite branch seriously rather than circling the same missing grammar theorem:

```text
derive Delta_GM_J[delta_J_before]
or produce a source-backed delta_J_before coefficient row.
```

The cleanest route is the `Delta_GM_J` kernel first, because it directly tests whether this coupling route contaminates local GR/Newton source normalization or can be absorbed into measured source parameters without observable damage.
