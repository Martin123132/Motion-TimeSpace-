# 000 - Recovery Bookmark After Drive Upgrade

Date: `2026-07-03`  
Status: `active_worktree_recovered_and_verified`

## Active Working Copy

Use this as the authoritative local framework copy:

```text
D:\Users\ollet\Desktop\Turn an intuitive research programme into a formal field-theoretic framework\Motion-TimeSpace--main
```

## GitHub Recovery Anchor

Repository:

```text
https://github.com/Martin123132/laptop-back-up-
```

Verified branch:

```text
origin/codex/mts-framework-since-bookmark-20260702
```

Verified commit:

```text
7457fea2381d5d3f24473a07031c05ef612409cd
Back up MTS framework since bookmark
```

## Verified Match

The GitHub branch contains the post-bookmark framework snapshot and matches the local active copy for the backed-up framework subset:

```text
104/104 files same
```

## Latest Verified Checkpoints

```text
4163 validation: 12/12
4164 validation: 12/12
4165 validation: 13/13
```

Latest checkpoint:

```text
4165-Y5-R2FR-kappa-G-normalization-superselection-or-coupling-derivation.md
```

Latest formal bridge:

```text
formalization-workbench\181-PPC4161-kappa-G-normalization-gate.md
```

Latest claims row:

```text
L-006
```

## Do Not Restore From

Do not treat this older local backup folder as current:

```text
D:\Users\ollet\Desktop\laptop-back-up-\mts-fw-20260630
```

It is useful history, but it predates the `4163-4165` local-GR/coupling work.

## Next Work Target

Continue from:

```text
4166-Y5-R2FR-source-measure-ZH-owner-and-parent-kappa-lock.md
```

The open problem is now the coupling throat:

```text
kappa_eff = kappa_* Z_H
G_N = c^4 kappa_eff/(8*pi)
```

Either derive parent ownership of `kappa_*` and source-measure normalization `Z_H`, or keep `G_N` explicitly labelled as a calibrated empirical constant while bounding drift/species/frame/range residuals.
