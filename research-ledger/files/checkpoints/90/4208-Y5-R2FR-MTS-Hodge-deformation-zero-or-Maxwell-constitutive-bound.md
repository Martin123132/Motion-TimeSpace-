# 4208 - Y5 R2FR MTS Hodge Deformation Zero Or Maxwell Constitutive Bound

Decision: `HODGE_DEFORMATION_ZERO_ROUTE_IMPORTED_CONSTITUTIVE_COUNTERMODEL_RETAINED_DELTA_HODGE_BOUND_LAW_WRITTEN_ACTION_DOMAIN_AND_CHARGE_SCALE_UNSIGNED_NONCLAIM`

4208 sharpens the EM branch:

```text
unique observed Hodge star + parent-visible Maxwell action
=> Delta_Hodge_EM = 0.
```

But the current corpus still has the countermodel:

```text
chi_EM != chi(g_obs)
```

unless the parent action-domain exclusion is signed. Therefore the constitutive bound law stays active and nonclaim.
