# Source Intake

This directory contains compact source-register and residual artifacts used by the theorem-gate workflow.

Large raw datasets, covariance matrices, and generated run directories are intentionally excluded from the public repository.

