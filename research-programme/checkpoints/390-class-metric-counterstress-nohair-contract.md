# 390 - Class-Metric Counterstress No-Hair Contract

Private class-metric/local-GR branch checkpoint. This is not a public WEP, PPN, preferred-frame, fifth-force, Einstein-Hilbert, source-normalization, boundary, bulk-field, local-GR, cosmology, or unified-field claim.

## 1. Purpose

Checkpoint 389 left the strongest local-GR route as:

```text
identity coframe,
ehat=e,
```

but only as:

```text
conditional theorem target or labelled WEP closure.
```

So this checkpoint handles the honest alternative:

```text
what if the class-metric pullback is retained?
```

Answer:

```text
then it is a modified-gravity/counterstress branch unless it is theorem-zero,
pure gauge,
constant-universal GM normalization,
or source-budgeted below local rows.
```

The retained class metric cannot borrow the identity branch's GR badge.

It must pay its own bill.

## 2. Machine Artifact

Script:

```text
scripts/class_metric_counterstress_nohair_contract.py
```

Run:

```text
runs/20260602-022500-class-metric-counterstress-nohair-contract
```

Outputs:

```text
results/source_register.csv
results/counterstress_contract.csv
results/nohair_conditions.csv
results/source_budget_contract.csv
results/counterstress_fates.csv
results/no_go_results.csv
results/branch_gate_matrix.csv
results/gate_results.csv
results/decision.csv
results/next_queue.csv
status.json
DONE.txt
```

Status:

```text
class_metric_counterstress_nohair_contract_written_not_satisfied_pullback_branch_demoted_modified_gravity_no_local_GR_pass
```

Claim ceiling:

```text
class_metric_counterstress_contract_only_no_WEP_PPN_fifth_force_EH_source_boundary_bulk_or_local_GR_pass
```

Source paths missing:

```text
0
```

## 3. Counterstress Contract

The class-metric branch has to own the pullback:

```text
E_selector,I + Pi_I^matter = 0.
```

This means:

```text
Pi_I^matter
```

is not dropped.

It appears as a source in a varied selector equation.

The required contract is:

| Item | Mathematical form | Status |
|---|---|---|
| selector equation owns pullback | `E_selector,I + Pi_I^matter = 0` | contract written, not derived |
| total Ward identity | `nabla_mu(T_matter+T_metric+T_selector+T_boundary+T_bulk)^mu_nu=0` | mapped, not closed |
| species-universal geometry | `F_A(C_D)=F(C_D)`, no species class charge | conditional after 388 |
| common-mode no-hair or constant | `partial_C F=0`, pure gauge, or constant `GM` normalization | not derived |
| source budget if retained | all surviving coefficients mapped to local rows | required fallback |

This is the line:

```text
Ward ownership makes conservation honest.
It does not make the stress invisible.
```

## 4. No-Hair Conditions

For the class-metric counterstress to become locally harmless, one of the hard exits must hold:

| Condition | Required form | Kills |
|---|---|---|
| positive operator or constraint | `L_C sigma=0` with positive `L_C`, or algebraic removal | exterior class hair |
| source-free compact exterior | no matter/boundary/bulk/projector/domain source outside compact body | radial fifth-force and beta/gamma hair |
| regular decaying boundary data | surface term vanishes or is constant universal monopole | boundary-resourced class hair |
| Ward-owned flux | boundary/source charges balance `n_mu T_counter^mu_nu` | `alpha3`, `Gdot`, fake conservation |
| constant universal `GM` absorption | no radius/time/species/range dependence | `delta_G`, `Gdot`, fifth-force |
| no preferred-frame counterstress | no vector/domain/marker/TF anisotropic parts | `alpha1`, `alpha2`, `xi` |

None of these is parent-derived in the current branch.

So the class-metric branch stays:

```text
modified gravity / retained residual,
not local GR.
```

## 5. Source-Budget Contract

If counterstress survives, it must be mapped to local rows:

| Observable | Source lock | Class-metric failure |
|---|---:|---|
| `eta_WEP` | `2.8e-15` | species-dependent `F_A`, source normalization, composition charge |
| `alpha_clock_redshift` | `3.1e-5` | common `F(C_D)` changes clock metric/constants |
| `gamma_minus_1` | `2.3e-5` | scalar/boundary/bulk counterstress modifies curvature/light |
| `beta_minus_1` | `7.8e-5` | radial nonlinear source hair or boundary flux |
| `alpha1/alpha2/xi` | `1.0e-4`, `2.0e-9`, `4.0e-9` | vector/domain/projector anisotropic counterstress |
| `alpha3` | `4.0e-20` contingent | unowned momentum flux/nonconservation |
| `Gdot/G` | `9.6e-15 yr^-1` contingent | time-dependent common mode/source flux |
| fifth-force / `delta_G` | `alpha(lambda)` | finite-range radial mode or source charge |

This is not a punishment.

It is the fair scoreboard.

If the class metric branch can live inside these budgets, it remains empirically interesting.

But:

```text
budget-safe modified gravity
```

is not the same as:

```text
derived GR reduction.
```

## 6. Counterstress Fates

The legal fates are:

| Fate | Requirements | Current status |
|---|---|---|
| derived-zero | identity coframe, `F'=0`, or positive source-free no-hair | not available |
| pure gauge | pullback direction is exact representative gauge, not physical `C_D` | only for `Cperp`-like directions |
| constant universal monopole | measured-`GM`/unit calibration only | not parent-derived |
| Ward-owned no-hair counterstress | varied stress is conserved and locally harmless | contract only |
| source-budgeted residual | coefficients/range/coupling mapped to runner rows | required if no theorem |
| demoted open | no theorem, coefficient, range, or closure label | blocks local-GR branch |

So the retained class-metric branch is allowed.

But it must be explicit:

```text
source-budgeted modified gravity,
not hidden local GR.
```

## 7. No-Go Results

| No-go | Consequence |
|---|---|
| Ward ownership is not no-hair | PPN/WEP/fifth-force rows remain unless no-hair or budgets are supplied |
| common `F` is not local GR | `T partial_C F` still sources clock/gamma/fifth-force/`Gdot` |
| boundary monopole is not radial hair | only constant conserved monopole can be `GM` absorbed |
| counterstress cannot be hidden in Bianchi | total Ward ledger must retain every stress component |
| budget-safe is not GR-derived | bounded counterstress is modified-gravity control, not GR theorem |

This blocks the tempting shortcut:

```text
the selector equation cancels it,
therefore local GR.
```

No.

Correct statement:

```text
the selector equation owns it,
therefore now the owned stress faces no-hair and local-bound gates.
```

## 8. Branch Gate Matrix

| Gate | Required | Current result |
|---|---|---|
| counterstress equation varied | derive `E_selector,I + Pi_I^matter=0` | not derived |
| counterstress no-hair | stress vanishes or is boundary-only constant in compact exterior | not derived |
| source-budget coefficients | derive coefficients/ranges/couplings for survivors | missing |
| GR promotion | all class-metric stress rows theorem-zero or harmless constant monopole | failed |

The class-metric branch is therefore:

```text
demoted modified gravity.
```

Demoted does not mean worthless.

It means:

```text
testable, but not GR reduction.
```

## 9. Relation To Identity Branch

Checkpoint 389 kept:

```text
identity coframe
```

as the clean local closure/theorem target.

This checkpoint preserves that.

The identity branch says:

```text
kill Pi_I^matter by ehat=e.
```

The class-metric branch says:

```text
retain Pi_I^matter,
own it,
then prove it harmless or budget it.
```

Both are legitimate branches.

They just cannot share claims.

Identity closure can be used to test:

```text
EH/source/boundary/bulk debts
```

without WEP pullback ambiguity.

Class metric can be tested as:

```text
modified gravity / counterstress residual.
```

## 10. What Improved

Before this checkpoint, class metric could be vague:

```text
maybe the selector cancels it.
```

Now cancellation means:

```text
varied selector equation,
total Ward ledger,
species-universal geometry,
no-hair or constant mode,
or source-budgeted residual.
```

That is engineering-grade enough to test.

It is not a public claim.

It is a private discipline gate.

## 11. What Still Fails

Still missing:

```text
parent derivation of the selector counterstress equation,
total Ward closure,
species-blind geometry parent selector,
common-mode no-hair,
constant universal GM absorption,
preferred-frame no-hair,
boundary flux ownership,
bulk/source-normalized force law,
and all local PPN coefficients.
```

Therefore:

```text
WEP not promoted,
PPN not promoted,
fifth-force not promoted,
local GR not promoted.
```

## 12. Gate Results

| Gate | Status | Evidence |
|---|---|---|
| source paths exist | pass | all cited source paths exist |
| counterstress contract written | pass | five required counterstress contract items written |
| no-hair conditions written | pass | six no-hair/source-normalization conditions listed |
| source-budget contract written | pass | eight observable rows mapped to source budgets |
| counterstress parent-derived | fail | `E_selector + Pi_matter` and exits are contract-only |
| class-metric local-GR promotion | fail | branch remains modified-gravity/counterstress |
| identity branch preserved | pass | identity coframe remains clean local closure/theorem target |
| claim ceiling enforced | pass | no WEP/PPN/fifth-force/EH/source/boundary/bulk/local-GR pass |

## 13. Decision

Decision:

```text
class_metric_counterstress_nohair_contract_written_not_satisfied_pullback_branch_demoted_modified_gravity_no_local_GR_pass
```

Meaning:

```text
class metric is not dead,
but it is not local GR.
```

It is now:

```text
modified-gravity/counterstress branch
```

unless it earns:

```text
theorem-zero,
pure gauge,
constant universal monopole,
or source-budgeted local safety.
```

The identity branch remains:

```text
the clean local closure/theorem target.
```

## 14. Next Target

Next:

```text
391 - Local-GR Stack After Identity Coframe Closure
```

Aim:

```text
roll the local-GR stack forward assuming identity coframe as labelled closure,
then isolate EH/operator/source/boundary/bulk debts without WEP pullback ambiguity.
```

Pass condition:

```text
remaining GR-reduction blockers are listed cleanly,
with identity closure separated from parent derivation.
```

Why this next:

```text
390 has now disciplined the alternative class-metric branch.
```

So the GR-facing route can proceed under a labelled identity-coframe closure while still keeping the class-metric branch testable as modified gravity.
