# 5396 — D4 deformed-contour regular-away W3

Status: **IN PROGRESS / PRIVATE / NO W3 CLAIM**

## Derived route

For each contiguous away support, write

\[
f(E,x,\epsilon)=g(E,x,\epsilon)+\sum_b\frac{\rho_b(x,\epsilon)}{E-p_b(x,\epsilon)}.
\]

After subtracting the material poles `B01–B04`, the regular term is transported to the rectangular upper contour,

\[
\int_L^U g\,dE
=\int_{L}^{L+ih}g\,dE
+\int_{L+ih}^{U+ih}g\,dE
-\int_{U}^{U+ih}g\,dE,
\qquad h=0.03.
\]

The complete parent pole catalog supplies the analyticity contract: `B01–B04` are subtracted and `B05–B06` have zero residue. The Cauchy regulator tube is covered by two closed rectangular slabs; their union is the original full tube.

## Enclosure repairs

- The reciprocal right-copy hard–soft edge uses the exact identity `[1 3] = -(1+q)` rather than a dependency-broadened generic spinor chart.
- Adaptive paths persist accepted rows and the remaining subdivision stack, so runtime stops and app/laptop interruptions resume without discarding a path.
- The collision Jacobian now selects the strongest rigorous enclosure among the parent recoil mean-value chart and direct centered primary/alternate projective charts.
- On three boxes where the parent mean-value enclosure contained zero, the direct centered primary chart proves lower bounds `0.38493033886148736`, `0.7447098179240919`, and `0.8846122367293536`.
- Hard-pair denominator reconstruction now uses the exact reciprocal identity `1/A=B/s` rather than first reconstructing `A=s/B`. With `s12=4 R^2`, this remains valid when the rectangular enclosure of the opposite bracket crosses zero.
- The local KLT evaluators use that reciprocal identity in both ordinary and globally regularized Parke–Taylor factors. All eight parent point-residue crosschecks remain enclosed; finite-displacement relative errors range from `3.2533685195894e-10` to `2.554868834270642e-09`.
- The first hard spinor has diagonal and transverse rational null charts. These are identical gauges related by `p^+p^-=p_perp^+p_perp^-`, not new closure assumptions.
- The external `(4,2)` angle edge has an exact Möbius-factorized candidate using `z=(1-q)/(1+q)` and `sqrt(1-z^2)=2e/(1+q)`.
- Revision v10 no longer boxes `x`, `E`, and `R=sqrt(1-E)` independently for that candidate. It differentiates the exact joint path map `E=E_L(x)+t(E_U(x)-E_L(x))+ih` (and the two connector analogues), including the algebraic boundary-root branches, before taking the mean-value enclosure.
- The dual boundary roots agree with the parent boundary values to `2.220446049250313e-16` and with centered finite-difference derivatives to `1.5772777925456308e-09` relative across eight probes. At a point the correlated edge agrees with the previous exact factorized chart to `3.993616911584836e-16` relative.
- The old stress box now has external-edge absolute lower bound at least `0.04016677698003581`; the live pending stress box has lower bound at least `0.049836871548054794`. The latter also passes the complete four/eight-arc residue evaluator rather than only the isolated edge test.
- Revision v11 carries the same joint path map into the invariant `s42`. At a point it agrees with the previous invariant formula to `7.089838456485103e-16` relative.
- On the original four-arc failure box the old independent `s42` enclosure has zero lower bound on every arc, while the v11 correlated enclosure has lower bound at least `0.08857401692453637`. The live KLT evaluator selects that invariant and uses the reciprocal identity rather than demanding the broadened edge itself be nonzero.
- Revision v12 lifts `x`, `t`, `epsilon`, and the derived recoil into a two-direction mixed dual. One direction differentiates the projective collision coordinate and the other follows the exact deformed path, so the enclosure retains `d/dpath(d root/drelative)` rather than treating recoil as independent.
- The v12 primary and alternate derivatives agree exactly with the parent mixed-dual formula at six point/chart probes. On the original depth-14 Jacobian failure, the previous geometric denominator was zero while the path-correlated primary chart proves `|J| >= 0.5285392705893088` with chart denominator at least `0.1403618065495857`; the complete residue box now passes.
- A newer depth-14 box remains too broad as a whole, but its two depth-15 path children prove `|J| >= 1.1279392389292584` and `|J| >= 1.1299102106311454`. Thus v12 reduces required refinement without asserting that every coarse box is regular.
- Revision v13 covers a coarse `(x,t)` rectangle by an exact `N x N` family of closed subrectangles, evaluates the already-derived path-correlated `s42` invariant on each member, and takes an outward-rounded rectangular hull of their union. This is an enclosure refinement of the same invariant, not a new physical or numerical closure assumption.
- The unsubdivided correlated invariant still contains zero on the live depth-9 through depth-11 stress boxes. A `2 x 2` cover proves a lower bound of at least `0.08559580386262479` at depth 11, while a `4 x 4` cover proves lower bounds of at least `0.07283370573487831`, `0.09989734225106492`, `0.12986219282790085`, and `0.14736192625088798` on the depth-9 through depth-12 stress boxes respectively.
- Only a hull whose distance from zero is positive is admitted to the existing exact reciprocal identity. The direct edge fallback remains unchanged when neither the unsubdivided nor subdivided invariant certifies nonvanishing.
- Revision v14 applies the same derivation to the right-copy first-external pair. For external endpoint `0` and first hard state `h_1`, its exact invariant is evaluated on the correlated path as `s01 = 2[-E_1 + sqrt(1-T^2) p_1x + T p_1z]`, with `T=-9+i epsilon`, the deformed-path energy, recoil, and global displacement all retained in one dual map.
- At three point probes the new `s01` map agrees with the parent four-vector invariant to relative errors `9.93498412932028e-17`, `9.309503074677274e-17`, and `1.1210126175528405e-16`.
- On all three stored right-copy `(0,1)` failures, both the independent invariant box and unsubdivided correlated box contain zero on every contour arc. The exact `2 x 2` subcover proves `|s01| >= 0.061875557734542376` uniformly across those boxes and arcs; a `4 x 4` cover strengthens the minimum to `0.0825778849371811`.
- The three complete residue boxes consequently pass the targeted KLT edge and advance to the later collision-geometric gate. No cancellation or fitted tolerance was introduced.
- Revision v15 covers each failed `(x,t)` rectangle by closed subrectangles and admits, on each member, whichever already-derived primary or alternate projective chart has a rigorously nonzero local denominator. Because both charts represent the same collision root derivative, the outward-rounded hull of their local Jacobian enclosures is a chart-union enclosure of the parent Jacobian, not a new closure.
- On the current depth-12 frontier box the unsubdivided primary and alternate Jacobians both contain zero, while the exact `4 x 4` chart cover proves `|J| >= 1.225215903056107`. At depths 13 and 14, `2 x 2` covers prove lower bounds from `0.9977360709898858` to `1.252936716130731`.
- The broader historical no-chart boxes require no axiom either: an `8 x 8` cover proves `|J| >= 0.7999155660686073` at depth 7, and `4 x 4` covers prove `|J| >= 0.3266560571919297` and `0.3168428609167631` on the two depth-8 children.
- The first v15 rate test exposed a control-flow obstruction: the parent recoil mean-value chart could raise before the valid projective chart union was evaluated. Revision v16 catches only that exact parent-chart failure, retains the parent relative and selected-global roots, and requires a positive direct, correlated, or subdivided projective Jacobian before certification.
- All three historical no-chart boxes then pass the complete residue evaluator with collision-Jacobian lower bounds `0.7999155660686073`, `0.3266560571919297`, and `0.3168428609167631`. Thus v16 removes an enclosure dead end without declaring a zero Jacobian nonzero.
- Revision v17 treats the right-copy `(4,1)` stable edge and active quotient as one Laurent identity in the same rational-spinor gauge. Writing `delta=u-u_*`, `A14=[1 4]_0`, and `Q14=[1 4]_1/delta`, the exact relations are `[1 4]_1=delta Q14` and `s14=delta A14 Q14`.
- Point probes confirm the two finite-displacement identities to relative errors `1.8451448485331672e-08` and `2.98862842142233e-09` respectively. The construction deliberately uses the live rational-spinor gauge for both factors rather than mixing independently normalized edge charts.
- An exact subcover recomputes the deformed energy, recoil, spinors, Laurent exponents, `A14`, and `Q14` on every member. On the stored depth-11 stable failures a `2 x 2` cover proves `|A14| >= 0.22117167041540223` and `|Q14| >= 0.43550833320864857`, hence `|s14/delta| >= 0.0963221055355844`.
- On the broader depth-10 active failures a `4 x 4` cover proves `|A14| >= 0.3551471474665597`, `|Q14| >= 0.5249601187210632`, and therefore `|s14/delta| >= 0.186438088697492`. All six complete stored failure boxes pass their original `(4,1)` gate; the three broader boxes advance to the separate arc-multiplier gate.
- Revision v18 removes the first-hard-spinor pivot false zero by covering a failed path box with exact closed `(x,t)` subrectangles and selecting one projective family that survives on every member. Within the plus family, `p^+p^-=p_perp^+p_perp^-` gives `p_perp^-/p^+=p^-/p_perp^+`; within the minus family it gives `p_perp^+/p^-=p^+/p_perp^-`. The two local denominator choices therefore construct the same rational-spinor gauge, not a patchwork of little-group normalizations.
- On the three stored first-spinor failures, a `2 x 2` cover proves minimum local pivot divisors `0.07043532175804912`, `0.07082657267256708`, and `0.10997499469150875`. All four physical light-cone momentum factors reconstructed from the covered spinors contain the direct point values in all `12` box/arc probes.
- All three complete historical boxes clear the first-spinor gate and advance to independent `p2`-chart or later KLT-edge gates. Thus the v18 repair removes only the named enclosure obstruction; it does not label those boxes certified prematurely.
- Revision v19 separates left- and right-copy external invariant tables. The failed left `(0,2)` pair contains the incoming beam `p_0=(-1,0,0,-1)`, so its exact invariant is `s02(left)=-2(E_2-p_2z)=-2 p_2^-`. The right pair with the same indices contains the outgoing complex target and retains the separately derived correlated invariant `s02(right)=2[-E_2+sqrt(1-T^2)p_2x+T p_2z]`.
- The left identity agrees exactly with the direct four-vector invariant in all `24` point probes. On the six stored failures its unsubdivided path-correlated lower bound ranges from `1.31991805959105` to `1.4458930562401036`; the distinct right invariant has unsubdivided lower bound at least `1.029605007147139` and `2 x 2` lower bound at least `3.6037505787452933`.
- All six stored boxes clear both `(0,2)` owners and advance to the later arc multiplier. An accepted-row ownership audit found one row with simultaneous coarse `(0,2)` zeros; v19 requeued it and reproduced its previous integrated upper bound (`7.430921229517049e+29` versus `7.430921229517051e+29`) and divisor (`0.0430136923228972`). The `(2,4)` audit found zero left/right overlaps across all `17472` accepted-row arc tests, while `858` accepted rows spanning the historical `(0,1)` failure band required no right correlated-invariant insertion.
- Revision v20 retains the common deformed-path variables in the energy multiplier. With soft energy `E`, hard energies `E_1,E_2`, and `P=E_1E_2`, the exact hard-state formulas give `E_1+E_2=2-E`. Therefore the previously dependency-broadened denominator obeys

  \[
  D=P^2+E^2(E_1^2+E_2^2)
   =(P-E^2)^2+4E^2(1-E),
  \qquad N=3P^2.
  \]

  This identity does not assert positivity for complex contour energy. Instead, v20 encloses `N` and both exact forms of `D` by a joint `(x,t,epsilon)` mean-value map and accepts a closed `2 x 2`, `4 x 4`, or `8 x 8` path cover only when every local denominator excludes zero.
- The identity agrees with the original multiplier denominator in `36` point probes to maximum relative error `1.12332867835545e-16`, with `E_1+E_2=2-E` agreeing to absolute error `4.0721885685270747e-17`. All `81` sampled exact multiplier values lie inside their v20 interval certificates.
- On the three stored multiplier failures, the old direct denominator lower bound is zero. The exact `2 x 2` cover proves denominator lower bounds `0.8571927880459571`, `0.8539316624199458`, and `0.8952056957530129`, with multiplier absolute upper bounds `0.046625420737178125`, `0.04883653293000738`, and `0.03778727314727066`. Two complete historical boxes now pass; the third advances to the independent left-copy `s2:c1` `(0,1)` KLT edge.
- Revision v21 derives the second-hard-state spinors from its null momentum matrix. Writing `p+`, `p-`, `p_perp+`, and `p_perp-` for its four light-cone factors, `det(p)=0` gives `p+ p-=p_perp+ p_perp-`. The plus-family gauge is `(lambda,lambda_tilde)=((p+,p_perp+),(1,p_perp-/p+))`, with the identical transverse-pivot quotient `p-/p_perp+`; the minus-family gauge follows analogously. Plus and minus families are related on overlaps by the exact little-group factor `p_perp-/p+`, so a subdivided enclosure admits one common family rather than hulling differently normalized local gauges.
- The three stored `p2` failures have zero direct diagonal and unsubdivided correlated pivot bounds. A closed `2 x 2` path cover supplies one common plus-family on every box and contour arc, with minimum selected pivot `1.1953504809700677`. The null identity and within-family chart overlap agree to maximum relative error `7.805017705990336e-17`; all `48` sampled spinors and reconstructed momenta are enclosed, including both left and right momentum orientations.
- All three complete historical boxes clear the `p2` gate. Two advance to the older right-copy `(4,2)` edge and one to the collision-Jacobian gate; none is promoted to a completed path box by the isolated chart test.
- Revision v22 transports the parent recoil sheet instead of squaring an interval hull and taking a second square root. The parent path already defines `R=sqrt_+(1-E)`. Because every stored target box proves `Re(R)>0`, the connected positive-real sheet obeys the exact identity `sqrt_+(R^2)=R`; the former `sqrt_+(box(R)^2)` calculation discarded this correlation and manufactured values on the cut.
- Across the three stored failures, the exact radicand retains imaginary part `-0.03`; its real ranges are `[0.0014711568367331352,0.049580342059523844]`, `[0.0014711568367331352,0.03231774570510155]`, and `[-0.019357970694371056,0.1000543128832178]`. The corresponding recoil real-part lower bounds are `0.12499161317260572`, `0.1252135670613031`, and `0.08296019492502617`, so even the box whose radicand crosses the imaginary axis never reaches the root cut.
- Nine point probes reproduce the original parent geometry to maximum relative error `1.3248411792677028e-16` and the fixed-sheet identity to `5.343323192528572e-17`. All three complete historical boxes clear the square-root target and advance respectively to the right `(4,2)`, left `(2,3)`, and dominant left hard-pair gates; none is accepted merely by the isolated sheet test.
- Revision v23 derives the left incoming-beam/first-hard-state edges in one projective family. For `p_0=(-1,0,0,-1)`, the rational spinors are `lambda_0=(-2,0)` and `lambda_tilde_0=(1,0)`. In the hard-state plus family, `lambda_1=(p_1^+,p_1_perp^+)` and `lambda_tilde_1=(1,p_1_perp^-/p_1^+)`, hence `<01>=-2p_1_perp^+` and `[01]=p_1_perp^-/p_1^+`. In the minus family the same edges are `<01>=-2p_1^-` and `[01]=1`. The null identity `p_1^+p_1^-=p_1_perp^+p_1_perp^-` proves the two families overlap by one little-group factor.
- The repair requires one common family matching the parent box and never chooses a family separately by chirality. On two frontier boxes where both direct chiral enclosures reach zero, a closed `4 x 4` plus-family cover proves `|<01>| >= 0.2696773243227155` and `|[01]| >= 0.5347120775836437` across all eight contour-arc boxes. Thirty-six point comparisons agree with the parent rational-spinor edges to maximum relative error `2.830524433501838e-16`.
- The first controlled frontier box now advances from the left edge to the independent collision-Jacobian gate and the second advances to a right-copy edge. All three complete historical target boxes certify with minimum amplitude divisors `0.03269376751877817`, `0.0106101078274251`, and `0.030840315586249542`; the isolated repair does not promote either broader frontier box.
- Revision v24 resolves the re-exposed right-copy `(2,4)` angle edge in the live common projective family. The older Möbius-factorized expression is a valid mixed-family representative but differs from the live plus-family edge by a nonconstant little-group factor, so v24 does not insert it into a plus/plus box. For right-copy momentum `r_2=-h_2` and `p_4=(1,-sqrt(1-T^2),0,-T)`, the common plus-family spinors give

  \[
  \langle24\rangle_+
  =r_2^+p_{4\perp}^+-r_{2\perp}^+p_4^+
  =h_2^+\sqrt{1-T^2}+(1-T)h_{2\perp}^+,
  \qquad T=-9+i\epsilon.
  \]

  The hard factors, `T`, deformed-path energy, recoil sheet, and displaced global root are all evaluated in the same dual map.
- Twenty-four point comparisons across the two live boxes and four contour arcs agree with the parent rational-spinor edge to maximum relative error `1.0122464515436046e-15`. The unsubdivided, `2 x 2`, and `4 x 4` enclosures still contain zero; a closed `8 x 8` cover proves `|<24>| >= 0.02791419748604821` over all eight box/arc combinations.
- Both complete frontier boxes clear the named `(4,2)` failure and advance to the independent right-copy `(0,1)` edge. The repair is attempted only after both the direct and existing edge enclosures fail and only when both live spinors select the plus family; it neither changes accepted rows nor mixes little-group gauges.
- Revision v25 derives the right-copy `(0,1)` edge in its common minus family. With outgoing external momentum `p_0=(1,sqrt(1-T^2),0,T)`, right hard momentum `r_1=-h_1`, `lambda_0=(sqrt(1-T^2),1-T)`, and `lambda_1=(-h_{1\perp}^-,-h_1^-)`,

  \[
  \langle01\rangle_{-/-}
  =(1-T)h_{1\perp}^- - \sqrt{1-T^2}\,h_1^-.
  \]

  This identity retains the exact deformed-path energy, recoil sheet, displaced root, and external regulator in one dual map.
- Seventy-two point comparisons over six consecutive frontier boxes and four contour arcs agree with the direct parent-gauge edge to maximum relative error `3.3766115072321297e-16`. A closed `4 x 4` cover proves `|<01>| >= 0.22997269673944362`; all six complete boxes advance from the right edge to the collision-Jacobian gate.
- The v25 benchmark exposed one genuine family transition at the final x panel. Revision v26 derives that external-minus/hard-plus overlap without identifying it with the minus/minus gauge. With `lambda_1=(-h_1^+,-h_{1\perp}^+)`,

  \[
  \langle01\rangle_{-/+}
  =(1-T)h_1^+ - \sqrt{1-T^2}\,h_{1\perp}^+.
  \]

  Twelve direct point comparisons agree to maximum relative error `2.482534153247273e-16`, and a closed `4 x 4` cover proves `|<01>| >= 0.17728871591304632`. The complete transition box then advances to the same independent collision-Jacobian gate.
- Revision v27 differentiates the alternate `minus_v` collision root algebraically before interval evaluation. Writing its first light-cone factor and holomorphic denominator as `m=p+b(r+r^{-1})` and `h=q+a r+c/r`, the quotient derivative is exactly

  \[
  \partial_r\!\left(\frac{m}{h}\right)
  =\frac{(bq-pa)r^2+2b(c-a)r+(pc-bq)}{(ar^2+qr+c)^2}.
  \]

  On the live reciprocal branch `r=1/u`, this is evaluated directly in the native representative coordinate `u`; no boxed reciprocal is formed and the cancelling `r^{-2}` factors never enter the enclosure.
- Eighty-one point comparisons spanning the three final-panel coordinates and the real and imaginary Cauchy-tube endpoints agree with the parent mixed-dual derivative to maximum relative error `1.8429839678463635e-16`. The transformed projective denominator has pointwise lower bound at least `0.047311007504115536` on that probe set.
- Closed native-coordinate covers certify the two depth-8 children with `2 x 2` covers at live Jacobian lower bounds `0.015838314279299263` and `0.015394330083989492`. The broad depth-7 parent uses an `8 x 32` cover and proves `|J| >= 0.14236902916394445`; its complete amplitude divisor remains positive at `0.0021225545137120005`.
- All three persisted boxes pass the complete four-arc combined-tube evaluator. The exact union lower bound is carried separately from the display hull, so an outward rectangular hull that contains zero cannot erase a valid finite-cover proof.
- Revision v28 removes the next false hard-pair zero at its source. The parent internal invariant obeys

  \[
  s_{12}=4R^2=4(1-E_{\rm path}),
  \]

  so the path energy supplies an exact enclosure without squaring the rectangular recoil-root box. The older `4*box(R)^2` enclosure remains available and v28 selects only the stronger of two enclosures of this same parent invariant.
- Across `648` left/right momentum, four-arc, regulator-tube point comparisons, `4(1-E_path)` agrees with the direct parent momentum invariant to maximum relative error `1.5515838457795457e-17`.
- On the three pending parents generated by the hard-pair gate, the old boxed-square lower bound is `0`, `0`, and `0`, while the path-energy invariant proves `0.019339451593206582`, `0.026317521643386316`, and `0.028720260246815888`. All three consequently advance to later right-copy edges rather than splitting again at `(1,2)`.
- Revision v29 writes the reciprocal-branch external-minus/hard-plus edge in the native representative coordinate `u`. If `V` is the displaced selected root, `T=-9+i epsilon`, `X=sqrt(1-T^2)`, and the exact hard-state Laurent coefficients are denoted by `b,c,d,p,q`, then

  \[
  \langle01\rangle_{-/+}
  =\frac{-XV\,[c u^2+q u+(c+d)]
  +(1-T)[b u^2+p u+b]}{u}.
  \]

  This is the parent edge with its reciprocal-coordinate cancellations performed algebraically, not a fitted local approximation. `108` direct parent-gauge point comparisons agree to maximum relative error `1.4465058852174311e-16`.
- A closed negative-real-half-plane subcover proves the former deepest box nonzero with `|<01>| >= 1.2275754579916506e-06`, and that complete box certifies with integrated upper `0.5376186179188426`. The adjacent box cannot use that lemma: a point solve finds a genuine zero at `x=0.031080543955551168`, `t=0`, `epsilon=4.699561737271944e-17`, and zero global displacement, with edge modulus `5.034757746836185e-17`. The nonzero-edge route is therefore rejected exactly where it must be rejected.
- Revisions v30–v31 remove the resulting termwise KLT singularity before integration. Put `A=<01>`, `B=[01]`, so the exact massless identity is `s01=A B`. In the only two right-copy KLT sectors that retain the external edge (`special=2,3`, chirality `0`), write the ordered MHV factors as `L0=l0/A` and `R1=-r1/A`. The four-term five-point kernel then reduces algebraically to

  \[
  K_5=K_{\rm reg}+\frac{R_{01}}{A},
  \qquad
  R_{01}=-l_0\,B\,(s_{02}+s_{12})\,r_1,
  \]

  with the already-derived global-displacement powers absorbed in `l0`, `r1`, and `R01`. No division by `A` occurs while constructing either `K_reg` or `R01`.
- `96` near-zero, two-special, four-arc comparisons give exact midpoint agreement between the factorized direct KLT form and `K_reg+R01/A`. The largest parent four-vector versus spinor-factorized `s01` absolute defect is `1.0395448751264115e-16`, within the precision of the frozen source inputs.
- The singular edge is integrated in the two real connector variables rather than bounded pointwise. For fixed regulator and global-contour parameters, let `F(x,t)=(Re A,Im A)`. On the first three singular cells the interval Jacobian determinant excludes zero with absolute lower bounds `0.114743577778765`, `0.11481835404822846`, and `0.04846237475538552`. The full first cell also has `d(Re A)/dx>0` and `d(Im A)/dt<0`; the same interval bounds apply to every segment-averaged Jacobian, proving global injectivity on each accepted rectangle.
- If `U_A` bounds the image radius and `J_min` is the Jacobian lower bound, the exact change of variables gives the finite certificate

  \[
  \int_B\frac{|R_{01}|}{|A|}\,dx\,dt
  \le \|R_{01}\|_\infty\,\frac{2\pi U_A}{J_{\min}}.
  \]

  The three accepted singular cells have integrated path bounds `1.2197523501347783e-06`, `1.2192160583836487e-06`, and `0.00012031111810704101`; no principal-value prescription or pole deletion is used.
- Revision v32 derives the connector-independent form of the same change-of-variables certificate. For any two parameter points `p,q` in a convex path box,

  \[
  F(p)-F(q)=\left[\int_0^1 J_F\!\left(q+s(p-q)\right)\,ds\right](p-q).
  \]

  Every entry of the segment-averaged matrix lies in the corresponding interval derivative hull. Therefore, if the determinant interval of that complete matrix hull excludes zero, the averaged matrix is nonsingular and `F` is globally injective. This proof is independent of whether `Re A` increases or decreases with `x`; the old right-connector sign restriction was sufficient but not necessary.
- On the newly reached `S_X001_MC04_SP_DM` left-connector pole cell, `d(Re A)/dx<0`, `d(Im A)/dt<0`, and the complete interval-matrix determinant has absolute lower bound `0.6385154080432677`. The same exact KLT decomposition therefore gives a finite area-equivalent upper bound `57.6023425107396` and final integrated path contribution `1.34363028622333e-06` on that cell.
- The expanded pole probe passes all `96` algebraic KLT identities, all `6` right/left path-Jacobian boxes, and both complete integrated singular-cell evaluations. The minimum Jacobian lower bound across this regression set is `0.04846222666603979`; no principal value, deletion, or nonzero-edge axiom is introduced.
- Revisions v33-v35 resolve the next dominant right-copy `(4,2)` split at the parent-invariant level. For the ordered chiral factors of the massless pair, the exact factorization `s42=<42>[42]` permits the reciprocal angle to be evaluated as its opposite chirality divided by `s42`, with the fixed ordering sign retained by the existing KLT edge routine. Thus only `s42 != 0` must be certified; no term is removed and no angle-level plateau is assumed.
- Revision v35 fixes the regulator-halo sheet globally rather than reselecting a principal square root in every subbox. With `T=-9+i epsilon`, the physical `epsilon>=0` transverse root and its analytic continuation through the Cauchy halo are

  \[
  X=i\sqrt{T^2-1},
  \]

  where `T^2-1` remains in a zero-free positive half-plane. The v34 subcover could choose the opposite principal root in tiny negative-`epsilon` leaves; v35 removes that bookkeeping discontinuity and conservatively requeues every depth-eight-or-deeper row that could have used it.
- The parent kinematics also give `q=-(T-1)/(T+1)`. Substitution of this identity, the reciprocal selected-root chart, and the unit-circle relations into the exact hard-state momentum reduces the zero-displacement invariant to

  \[
  s_{42}(0)=-4r^2=-4(1-E_{\rm path}).
  \]

  If `delta` is the global-contour displacement, `zeta` the selected root, and `H_2`, `A_2` the raw holomorphic and antiholomorphic second-hard factors, the complete displaced identity implemented by v35 is

  \[
  s_{42}(\delta)=-4(1-E_{\rm path})
  -X\delta\left[H_2-\frac{A_2}{\zeta(\zeta+\delta)}\right].
  \]

  The correction is explicitly proportional to the `10^-7` contour radius; it is not estimated by subtracting two dependency-inflated momentum boxes.
- `108` direct right-cut momentum comparisons spanning the deep connector regression boxes, three regulator values, and four global arcs agree with the v35 factorized invariant to maximum relative error `2.233156366063473e-14`. All three closed half-plane covers pass with minimum rigorous invariant lower bound `0.02180479266107858`.
- On the former live depth-nine frontier, the same proof improves from `4095` interval evaluations and lower bound `0.005209444548898136` to one evaluation and `|s42|>=0.020117572185361792`. Point checks and the closed covers therefore reject a genuine `(4,2)` zero on this frontier; unlike the earlier genuine external `(0,1)` zero, no pole extraction is required here.
- Revisions v36-v37 derive the reciprocal right-cut external `(4,1)` active square quotient before interval evaluation. Put `T=-9+i epsilon`, `q=-(T-1)/(T+1)`, `r_0=-i sqrt(-q)` so that `r_0^2=q`, `zeta` for the selected root, and `V=zeta+delta`. The sheet lock gives `X=(T+1)r_0`, while external leg 4 is uniformly in the rational plus chart because `|1-T|>|1+T|` throughout the regulator tube. The parent square bracket therefore factors exactly as

  \[
  [14]=\delta Q_{41},\qquad
  Q_{41}=\begin{cases}
  -1/(r_0V),&\text{hard-first plus chart},\\
  1/\zeta,&\text{hard-first minus chart}.
  \end{cases}
  \]

  This is the quotient returned by the parent Laurent-affine factorization, with no fitted coefficient or nonzero-edge axiom.
- The dedicated v37 probe compares the derived quotient with the generic parent construction at `108` points spanning the live right-connector box, three path parameters, three regulator values, and four contour arcs. All identities pass; the maximum relative error is `8.936623008518753e-15`, the minimum quotient modulus is `1.1916575044544926`, and the minimum rigorous external-chart dominance margin is `1.9999987500011915`.
- v36 first computed the exact quotient only as a trigger for subdivision, so two later KLT calls could still enter the generic factorizer. v37 installs the exact value in the active-quotient map whenever its denominator is certified. Three bounded v37 continuations add `53` certified boxes with zero new active `(4,1)` quotient failures and zero new `(4,2)` failures. This explicitly separates a routing defect from the parent identity.
- Revision v38 derives the opposite-chirality right-cut external `(4,1)` stable angle in the same live rational-spinor gauge. External leg 4 remains in the plus chart with `lambda_4=(1-T,-X)`, where `T=-9+i epsilon` and `X=sqrt(1-T^2)` is the already locked sheet. The right hard momentum is `-h_1`, so the parent bracket is exactly

  \[
  \langle14\rangle=\begin{cases}
  Xh_1^+ +(1-T)h_{1\perp}^+,&\text{hard-first plus chart},\\
  Xh_{1\perp}^- +(1-T)h_1^-,&\text{hard-first minus chart}.
  \end{cases}
  \]

  The implementation evaluates the deformed-path energy, recoil sheet, hard state, target, locked transverse root, and global displacement in one dual map over `(x,t,epsilon,delta)`, then takes the centered mean-value enclosure. It may replace a direct dependency-broadened angle only with this enclosure of the identical parent bracket; no edge plateau or fitted nonzero constant is introduced.
- The dedicated v38 probe compares the formula with the direct parent spinor construction at `108` points spanning the live depth-ten box, three path parameters, three regulator values, and four contour arcs. All identities pass with maximum relative error `3.4632039529536487e-15`; the minimum sampled direct angle modulus is `0.05628641518595938` and the hard-first chart is uniformly plus on this regression box.
- The exact formerly blocked leaf `RRLDRDLDRU`, at depth `10`, now certifies without another split. Its stored amplitude-denominator lower bound is `0.011534106176756764` and its collision-Jacobian lower bound is `0.5560521574707733`. This is a direct production regression of the failed box, not only a pointwise identity check.
- Revision v39 removes the residual collision-root cancellation from the same plus-chart angle. With raw first-hard transverse factors `H_1,A_1`, selected collision root `zeta`, displaced root `V=zeta+delta`, and `[14]/delta=-1/(r_0V)`, the parent invariant gives

  \[
  \frac{s_{14}}{\delta}
  =-X\left(H_1-\frac{A_1}{\zeta V}\right),
  \qquad
  \langle14\rangle
  =Xr_0\left(VH_1-\frac{A_1}{\zeta}\right).
  \]

  This is algebraically identical to the v38 bracket but cancels at the symbolic invariant-quotient level before interval evaluation. A closed `16 x 16` `(x,t)` cover is used only on failed depth-eight plus-chart boxes; its union remains in one positive-imaginary half-plane.
- The v39 point probe compares the factorized expression with the direct parent spinors at `108` points. All pass with maximum relative error `1.6608144960976797e-13` and minimum sampled angle modulus `0.023334166253267556`. All four closed arc covers pass with `Im<14> >= 0.015643582875217856`, so the nonzero certificate is derived rather than inferred from sampling.
- A separate Jacobian audit rejects a tempting overclaim. On the current depth-eight pole box, all `27` point determinants are positive in `[0.14035811758472308,0.3350090545493317]`, but the complete broad interval determinant is `[-9156.038531963171,9718.925982440383]`. Midpoint preconditioning has Frobenius defect bound `58.37564773933901`, and even an `8 x 8 x 8` closed derivative-hull cover leaves the segment-averaged determinant in `[-81.49163243093872,88.18434969560943]`. Pointwise positivity therefore does not prove v32 global injectivity on that broad box; ordinary outer subdivision remains mandatory.
- `scripts/Y5_R2FR_5396_D4_frontier_angle_audit.py` audits a live adaptive leaf without changing the production revision. On depth-twelve leaf `RLRDLDRDRDRD`, covering `x in [0.2522048255362018,0.2527609108695259]` and `t in [0,0.03125]`, all `324` point probes remain in the positive-imaginary half-plane with minimum sampled modulus `0.006554931752434065`. More importantly, the complete production-matched `16 x 16` closed cover proves `Im<14> >= 0.0035075996446876775` on all four contour arcs; a `32 x 32` cover strengthens this to `0.005755997750042964`. The temporary depth-twelve stack is therefore not evidence for a zero or a missing parent identity.
- The same source-level audit is rerun when the frontier first reaches depth `14`, on leaf `RRLDLDRDLDRDLD` with `x in [0.2599900202027392,0.2602680628694013]` and `t in [0,0.015625]`. Its `324` point probes have minimum modulus `0.004717123564709671`; all four production-matched `16 x 16` closed covers prove `Im<14> >= 0.0034565376102558484`, and the `32 x 32` confirmation gives `0.004370113126105247`. The next two production resumes clear this entire transition and return the live stack to depth `8`.
- A stronger global-injectivity route is derived but not promoted. Gale and Nikaido's two-dimensional global-univalence criteria are sourced locally at `source-intake/functional_rg/5396/sources/Gale_Nikaido_1965_Jacobian_global_univalence.pdf`, DOI `10.1007/BF01360282`, SHA-256 `B66CE183D6B7BB0FDDC6AD336BA26EA4DCEA17DA0BC5AF1E9273267DD7446E66`. `scripts/Y5_R2FR_5396_D4_frontier_gale_nikaido_audit.py` constructs the exact native angle with a second-order interval jet, encloses its first derivatives by a Hessian mean-value form, splits the regulator and contour phase rather than filling the contour disk, and tests Theorem 8(ii) with one fixed output direction.
- `scripts/Y5_R2FR_5396_D4_centered_jacobian_crosscheck.py` compares the centred-jet derivatives with the production dual derivatives at `108` points. Every identity passes with maximum relative error `3.358272386625806e-15`. The current closed `1`, `2`, and `4` subdivision theorem covers nevertheless retain zero in at least one required interval, so `global_univalence_proved=false`; this route remains a sourced audit, not a production Jacobian replacement or local closure.
- The frontier-angle runner now retains every proof under a depth/path-qualified artifact prefix and accepts either a live stack index or a complete explicit historical frontier. Explicit historical reruns no longer replace the generic live-frontier aliases. This recovers reproducible artifacts for `RLRDLDRDRDRD` and `RRLDLDRDLDRDLD` and prevents later adaptive leaves from erasing their evidence.
- Three further live depth-fourteen leaves pass the same closed proof. `RRLDRDRDRDLDRD` has `Im<14> >= 0.0017274449733669912` on the `16 x 16` cover and `0.0030931025598807844` on the `32 x 32` confirmation; sibling `RRLDRDRDRDLDRU` gives `0.002743243670830217` and `0.003333355568685936`; later leaf `RRRDLDLDRDRDLU` gives `0.0022337778322355775` and `0.0028417265704297847`. All are path-addressed closed-cover results, not point-sample inferences.
- The centred-Jacobian and Gale-Nikaido runners now use the same path-addressed/live-alias contract, so neither exact-identity evidence nor a failed theorem attempt is overwritten by a later leaf. The historical depth-fourteen evidence is regenerated explicitly before auditing the live depth-sixteen frontier.
- On depth-sixteen leaf `RRRDRDLDLDRDLDRU`, the closed angle cover proves `Im<14> >= 0.0019842238716848426` at `16 x 16` and `0.002346347174205707` at `32 x 32`. Its centred derivatives agree with the production dual derivatives at `108` points with maximum relative error `4.588657601890406e-16`. The path-addressed Gale-Nikaido `1`, `2`, and `4` covers still do not prove the required principal-minor signs, so `global_univalence_proved=false` remains explicit.
- The next depth-sixteen leaf `RRRDRDLDRDRDRDLU` also passes: its `16 x 16` and `32 x 32` lower half-plane bounds are `0.0017588384795398512` and `0.0021336800047422723`. Production subsequently clears both depth-sixteen transitions without using the failed global-univalence prototype.
- The deferred selector scan is rerun after the fourth support cell closes and is upgraded from a bare Boolean to a transition audit. It still finds exactly `15/558` pointwise changes, confined to mapped cells `U033`, `U037`, `U053`, `U055`, `U058`, and `U063` in terms `MC04_SM_DM` and `MC04_SP_DP`. Every observed state is one of the algebraic selector theorem's declared representative/reciprocal chart variants; the maximum reciprocal-pair residual is `1.5987211576284532e-14`, and the smallest sampled selected-unit margin is `0.0015740300081879444`.
- This reclassifies the old `15` rows as known unit-circle chart transitions rather than unknown selector states, but it does not prove a closed selector margin on each mapped cell. A cell-midpoint selector therefore remains forbidden as global production ownership until either a closed unit-circle partition or a complete representative/reciprocal chart-union bound is derived.
- Checkpoints 5397-5399 now close that production contract. The finite-union lemma passes on all `15` transition probes and six closed two-chart boxes. A staged audit then closes all `5,841` previously accepted rows: `4,746` have a positive interval selector-role margin, `290` use an explicit two-chart calculation, and `805` belong to terms with one parent-declared chart. The minimum closed selector margin is `0.4802908273459625`.
- Revision v40 starts every future two-chart box from the complete declared chart set. It retains one chart only when interval arithmetic proves `1-sup|R_rep|>0` or `inf|R_rep|-1>0`; otherwise it evaluates the finite chart union. The original fifteen v39 path files are hash-preserved under checkpoint 5399, and the migrated live files match the staged certificates exactly.
- Revision v9 requeued and recomputed all `29` rows whose obsolete direct-edge diagnostic recorded zero before a successful reciprocal fallback. The clean state now contains no accepted zero-diagnostic row.

## Current numerical state

- The complete first support cell `S_X001_MC04_SM_DM` now has all three stored paths: left connector `288` boxes, top `4534` boxes, and right connector `126` boxes.
- The completed right connector contains `97` v31 pointwise-supremum rows, `3` explicit simple-pole change-of-variables rows, and `26` conservative legacy rows retained from earlier compatible revisions.
- The v32 external-pole validation contains `96/96` exact KLT decomposition passes, `6/6` nonzero path-Jacobian passes spanning both connector orientations, and `2/2` complete integrated-cell passes; the minimum tested Jacobian lower bound is `0.04846222666603979`.
- The full `S_X001_MC04_SP_DM` left connector is now complete at `52` boxes: `51` pointwise-supremum rows and one explicit simple-pole change-of-variables row. Its parameter-area cover is `0.203825401537005`, equal to the full connector area up to outward-rounding precision.
- Its current finite integrated upper bound is `8.785890860589e50`, dominated by intentionally coarse pointwise rows. This closes a convergence/coverage gate, not a competitive numerical `W3` estimate; tightening is deferred until the atlas cover exists.
- The full `S_X001_MC04_SP_DM` top path is now complete at `43` pointwise-supremum boxes. Its parameter-area cover is `0.203825401537005`; its finite but deliberately untightened integrated upper bound is `4.00431420420358e77`.
- The `S_X001_MC04_SP_DM` right connector is now complete at `267` certified boxes. Its parameter-area sum is `0.203825401537006`, matching the full connector area up to outward-rounding precision; its finite integrated upper bound is `6.81695416875858e39`, and its minimum stored amplitude denominator is `3.0975425670734e-05`.
- This completes all three paths of the second support cell. The targeted right-copy `(4,2)` split counter remains frozen at `129`, and the external `(4,1)` active-factor quotient counter remains frozen at `36`; the last four pending descendants close without a new analytic identity or a depth-gate exception.
- The next support `S_X002_MC04_SM_DM` left connector is also complete at `2` boxes. Its parameter-area sum is `0.0711789226654846`, its finite integrated upper bound is `3.58704758008442e57`, and its minimum stored amplitude denominator is `0.145913401577051`.
- The `S_X002_MC04_SM_DM` top path is complete at `56` boxes. Its parameter-area sum is `0.0711789226654846`, its finite integrated upper bound is `1.60302857595342e82`, and its minimum stored amplitude denominator is `0.000246308468092465`.
- The same support's right connector is complete at `16` boxes with the same full parameter-area sum, finite integrated upper bound `6.15847086592358e39`, and minimum stored amplitude denominator `0.00132223892641251`. This completes all three paths of the third support cell without promoting any transient edge split to a closure axiom.
- The `S_X002_MC04_SP_DM` left connector is complete at `57` certified boxes. Its parameter-area sum is `0.07117892266548465`, its finite integrated upper bound is `7.899594796637309e36`, its minimum amplitude-denominator lower bound is `6.669828797590807e-05`, and its minimum collision-Jacobian lower bound is `0.012769826061070471`.
- The same support's top path is complete at `3` certified boxes with the same full parameter-area sum. Its finite integrated upper bound is `1.3007211790489977e28`, its minimum amplitude-denominator lower bound is `0.006654956898720609`, and its maximum refinement depth is only `2`.
- The `S_X002_MC04_SP_DM` right connector is complete at `383` certified boxes. Its parameter-area sum is `0.0711789226654841`, matching the full connector area up to outward-rounding precision; its finite integrated upper bound is `7.3964484339378e33`, its minimum amplitude-denominator lower bound is `2.22977610647517e-05`, and its minimum collision-Jacobian lower bound is `0.00212349778959594`. This completes all three paths of the fourth support cell.
- The complete `S_X003_MC04_SM_DM` support cell then closes without a new analytic identity: left connector `1` box, top `12` boxes, and right connector `1` box. Every path has parameter area `0.0176739667451603`; their finite integrated upper bounds are respectively `8.20863436335813e36`, `4.84671185934571e34`, and `1.86532948769313e50`.
- The `S_X003_MC04_SP_DM` left connector is complete at `110` certified boxes and its top path is complete at `1` box. The active frontier is now the same support's right connector, with `164` accepted and `6` crash-safe pending boxes at maximum depth `8`. Its accepted parameter-area fraction is `0.1601562499999999`. Completed path jobs are `17/240`; the sixth support-cell contour certificate still requires this right connector.
- All computation remains single-process, thread-capped, below-normal priority, crash-safe, and resumable at revision `D4-deformed-contour-regular-away-W3-v40`.

## Benchmark decisions

- v4 proved the direct collision-Jacobian repair: no new Jacobian false zeros appeared.
- v7 proved the reciprocal-KLT repair: no new dominant `(1,2)` hard-pair failures appeared in the next benchmark.
- v8's fourfold path-parameter split bias did not improve certified-area throughput and was removed in v9.
- v10's complete live-box evaluation takes `8.162576699862257` seconds with eight global arcs, returns finite integrated upper bound `99953240.53993669`, and has minimum amplitude denominator `0.020956856701803767`.
- A five-minute v10 resume retained all conservative v9 rows, certified `45` additional boxes, and reduced the pending stack from `7` to `6`.
- A subsequent fifteen-minute v10 benchmark certified `180` boxes but added `159` external `(4,2)` failures, so the edge-only repair was not rate-stable.
- The ten-minute v11 benchmark certified `102` more boxes, reduced the pending stack from `9` to `6`, and added `47` external failures. The external-failure rate per accepted box therefore fell from about `0.883` to `0.461`, but did not freeze.
- The ten-minute v12 benchmark certified `99` more boxes and increased first-cell coverage by `0.0447387695312484`. It added `20` Jacobian failures, reducing the Jacobian-failure rate per accepted box from about `0.333` under v11 to `0.202` under v12.
- The next equal-duration v12 continuation certified `89` boxes but added `50` external `(4,2)` failures, confirming that this edge remained the dominant rate obstruction before v13.
- The ten-minute v13 continuation certified `103` boxes, increased first-cell coverage from `0.3674316406249855` to `0.4096679687499835`, and reduced the pending stack from `7` to `6`. It added exactly `0` external `(4,2)` failures, so the internally subdivided invariant removes the targeted split in this benchmark rather than hiding it with extra global depth.
- Historical split attempts do not invalidate accepted rows. The v13 telemetry redirects the next derivation to the right-copy `s2:c0:left0` `(0,1)` stable edge, which added `45` failures, followed by the collision charts (`27` geometric and `9` no-projective-chart additions).
- The ten-minute v14 continuation certified `85` boxes and increased first-cell coverage from `0.4096679687499835` to `0.421386718749982`. It added exactly `0` targeted `(0,1)` failures, while the dominant additions moved to collision geometry (`42`) and the newly exposed external `(4,1)` denominator (`24` stable-edge plus `4` active-quotient failures).
- The ten-minute v15 continuation certified `89` boxes and raised first-cell coverage from `0.421386718749982` to `0.4726562499999812`. It added `0` geometric-zero failures but `22` parent no-projective-chart failures, identifying the early-abort route fixed by v16.
- The ten-minute v16 continuation certified `104` boxes, raised first-cell coverage from `0.4726562499999812` to `0.4987792968749795`, and reduced the pending stack from `6` to `3`. It added exactly `0` geometric-zero and `0` no-projective-chart failures; additions moved to the external `(4,1)` denominator (`68` stable-edge and `2` active-quotient failures).
- The ten-minute v17 continuation certified `63` boxes but, because several were broad, raised first-cell coverage from `0.4987792968749795` to `0.6035156249999804`. It added exactly `0` stable-edge and `0` active-quotient `(4,1)` failures. New additions were first-spinor plus-diagonal `32`, square-root cut `13`, left-copy `(2,3)` edge `10`, and arc multiplier `10`.
- The v17 manifest migration reran all `104` chart-equivalence checks and all `8` parent-residue checks successfully. Maximum chart-to-parent and finite-displacement-to-parent relative errors are `3.3318459115074085e-09` and `2.554868834270642e-09` respectively; all `96` boundary parent-probe enclosures also pass.
- The ten-minute v18 continuation certified `56` boxes, reduced the pending stack from `5` to `4`, and raised first-cell coverage from `0.6035156249999804` to `0.6708984375`. It added exactly `0` first-spinor pivot failures. The new split telemetry is left-copy `(2,0)` edge `34` across the two main chiral copies, second-hard-spinor diagonal chart `8`, square-root cut `7`, another left-copy `(4,1)` edge `3`, and arc multiplier `3`.
- The v18 manifest migration again passes all `104` chart-equivalence checks, all `8` parent-residue enclosures, and all `96` boundary parent-probe enclosures. The selector scan remains unchanged at `15` mismatches among `558` probes.
- The ten-minute v19 continuation re-certified the explicitly requeued ownership-audit row and produced a net `54` additional stored boxes. It raised first-cell coverage from `0.6708984375` to `0.7333984375` and added exactly `0` failures to either targeted left `(0,2)` counter. The dominant new split is the arc multiplier (`24`), followed by the left `(0,1)` chiral edge (`11`), `p2` projective chart (`6`), square-root cut (`6`), and smaller edge categories.
- The v19 manifest migration again passes all `104` chart-equivalence checks, all `8` parent-residue enclosures, and all `96` boundary parent-probe enclosures. The selector scan remains unchanged at `15` mismatches among `558` probes.
- The ten-minute v20 continuation certified `29` additional boxes and raised first-cell coverage from `0.7333984375` to `0.800781249999981`. It added exactly `0` arc-multiplier failures, so the targeted denominator counter froze at `37`. New splits moved to the `p2` projective chart (`9`), square-root cut (`9`), left `(2,3)` edge (`5`), geometric collision denominator (`3`), and smaller KLT-edge categories.
- The v20 manifest migration passes all `104` chart-equivalence checks, all `8` parent-residue enclosures, and all `96` boundary parent-probe enclosures. Maximum chart-to-parent and finite-displacement-to-parent relative errors remain `3.3318459115074085e-09` and `2.554868834270642e-09`; the selector scan remains `15` mismatches among `558` probes.
- The ten-minute v21 continuation certified `16` additional boxes, reduced the pending stack from `4` to `3`, and raised first-cell coverage from `0.800781249999981` to `0.851562499999981`. It added exactly `0` `p2` chart failures, freezing that targeted counter at `23`. New splits were square-root cut `4`, left `(2,3)` edge `3`, right `(4,2)` edge `3`, left `s2:c0:left0` edge `2`, right `(0,1)` edge `2`, and collision geometry `1`.
- The v21 manifest migration again passes all `104` chart-equivalence checks, all `8` parent-residue enclosures, and all `96` boundary parent-probe enclosures. The selector scan remains `15` mismatches among `558` probes.
- The ten-minute v22 continuation certified `12` additional boxes, reduced the pending stack from `3` to `2`, and raised first-cell coverage from `0.851562499999981` to `0.8671874999999813`. It added exactly `0` square-root-cut failures, freezing the targeted counter at `82`. New independent splits were left `s2:c1:left0` `(0,1)` edge `4`, right `(4,2)` edge `3`, collision geometry `2`, dominant left hard-pair `1`, and left `(2,3)` edge `1`.
- The v22 manifest migration passes all `104` chart-equivalence checks, all `8` parent-residue enclosures, and all `96` boundary parent-probe enclosures. The selector scan remains `15` mismatches among `558` probes.
- The ten-minute v23 continuation produced a net `18` additional certified boxes and raised first-cell coverage from `0.8671874999999813` to `0.9160156249999818`. It added exactly `0` failures to either targeted left `(0,1)` chirality, freezing those counters at `5` and `16`. The pending frontier expanded from `2` to `4` boxes while advancing rightward; new independent splits were right `(4,2)` edge `8`, collision geometry `8`, and dominant left hard-pair `4`.
- The v23 manifest migration passes all `104` chart-equivalence checks, all `8` parent-residue enclosures, and all `96` boundary parent-probe enclosures. The selector scan remains `15` mismatches among `558` probes.
- The ten-minute v24 continuation certified `14` additional boxes, reduced the pending stack from `4` to `2`, and raised first-cell coverage from `0.9160156249999818` to `0.9335937499999822`. It added exactly `0` right-copy `(4,2)` failures, freezing the targeted counter at `974`. New independent splits were collision geometry `8`, right-copy `(0,1)` edge `3`, and dominant left hard-pair `1`.
- The v24 manifest migration passes all `104` chart-equivalence checks, all `8` parent-residue enclosures, all `96` boundary parent-probe enclosures, and all `12` registered source/hash checks. The selector scan remains `15` mismatches among `558` probes.
- The ten-minute v25 continuation certified `11` additional boxes and raised first-cell coverage from `0.9335937499999822` to `0.9746093749999823`. The common minus-family edge reduced the new right `(0,1)` split count to one; inspection located that one split at the external-minus/hard-plus transition. New independent splits were collision geometry `8` and dominant left hard-pair `3`.
- The ten-minute v26 continuation certified `9` additional boxes and raised first-cell coverage from `0.9746093749999823` to `0.9843749999999828`. It added exactly `0` right `(0,1)` and `0` right `(4,2)` failures, while all eight new targeted splits moved to collision geometry; the mixed-family repair is therefore rate-stable on the traversed frontier.
- The v26 manifest migration passes all `104` chart-equivalence checks, all `8` parent-residue enclosures, all `96` boundary parent-probe enclosures, and all `12` registered source/hash checks. The selector scan remains `15` mismatches among `558` probes.
- The v27 live resume accepted all `3` persisted collision boxes without further global splitting and completed the top path at `4534` boxes in `133.75617146492004` seconds. A controlled `623.737074136734`-second run then advanced onto the right connector, stored `6` certified boxes, and paused with `6` crash-safe pending boxes.
- The targeted collision counter therefore freezes on the closed top frontier. The newly exposed right-connector work is independent: its leading split is the already known left hard-pair edge rather than another failure of the v27 Jacobian identity.
- The controlled `602.653377532959`-second v28 resume stored `16` additional right-connector boxes, increasing the active certificate from `6` to `22`. The targeted left hard-pair counter added exactly `0` failures and remains frozen at `9`.
- Subdivision now exposes the independent right-copy `(0,1)` edge as the leading rate obstruction (`18` splits), followed by the right `(4,2)` edge (`6`). The single broad displaced first-spinor-pivot split remains unchanged.
- The initial v29 benchmark raised the right-connector certificate from `22` to `26` boxes, then reached the genuine external `(0,1)` zero. That result falsified the proposed global nonzero-edge continuation and selected KLT-level pole extraction instead.
- The first v30 singular-cell benchmark advanced from `26` to `70` accepted boxes. After aligning the KLT kernel to the exact spinor-factorized `s01` identity, v31 deliberately requeued all three singular rows rather than silently retaining revision-dependent numbers.
- Two `904`-second v31 resumes revalidated those rows and advanced the connector to `91` accepted boxes. A `1209`-second continuation advanced to `116`, and the final bounded resume completed the connector at `126` boxes after `430.0678298473358` seconds of active partition work.
- The completed first-cell right connector is finite without requiring `<01>` to be nonzero. The three singular rows use a derived simple-pole integral; every other newly accepted row continues to use the ordinary pointwise enclosure route.
- The v32 bounded continuation resumed `37` accepted and `12` pending boxes in the second-support left connector. It completed that connector at `52` boxes in `145.36995148658752` seconds, proving that the connector-independent pole certificate closes the exact v31 maximum-depth leaf rather than merely moving it.
- The same run advanced into the second-support top path and paused at the runtime budget with `13` accepted and `2` pending boxes. Overall production is now `4/240` completed path jobs with one active internal state and no lost work.
- A first subsequent `903.4867978096008`-second v32 continuation advanced the top path from `13` to `27` accepted boxes without reaching a depth gate. The remaining boxes were therefore correctly left to ordinary refinement rather than assigned an unnecessary analytic closure.
- The next bounded continuation completed the top partition at `43` boxes after `401.39479398727417` seconds of active partition work, then advanced into the right connector and paused at `7` accepted and `6` pending boxes. Overall production is now `5/240` completed path jobs.
- Four ordinary v32 continuations advanced the right connector from `7` to `131` accepted boxes. The targeted `(4,2)` counter reached `118` and depth `11`, selecting a derivation audit rather than continued blind subdivision.
- The v33 production test closed the three audited deep boxes and advanced to `135` accepted boxes, but exposed avoidable runtime from testing all half-plane phases on broad parents. Revision v34 gates the proof at depth `8` and returns on its first certified phase.
- The bounded v34 production test advances to `138` accepted boxes, reduces pending boxes from `8` to `6`, and leaves maximum pending depth `9`. The targeted `(4,2)` counter adds only one split during this pass; the path remains incomplete but no maximum-depth obstruction survives.
- Three further v34 continuations reached `149` accepted and `5` pending boxes but required up to `4095` subcover evaluations per difficult `(4,2)` box. The split counter rose from `121` to `129`, identifying a rate problem rather than a physical zero.
- The first v35 production run deliberately reduced the retained state to `26` shallow accepted rows and requeued `123` deep rows. It re-certified `101` of those rows in `901.2299869060516` seconds with zero new targeted failures; the next `621.6654984951019`-second run reached `146` accepted and `8` pending.
- Two subsequent bounded v35 passes completed the conservative migration, crossed the former depth-nine frontier, and reached `174` accepted boxes with `5` pending. A final `918.4216227531433`-second pass reached `198` accepted and `6` pending at maximum depth `10`; all new targeted `(4,2)` failures remain zero, while ordinary splitting exposes the independent `(4,1)` active quotient.
- The first `624.3048758506775`-second v36 continuation advanced from `198` to `210` accepted boxes. Its two new active-quotient splits exposed that the exact formula had been evaluated but not unconditionally routed into the KLT override map; v37 corrects that implementation defect.
- Three v37 continuations of `608.2163481712341`, `613.9888470172882`, and `616.467983007431` seconds advance the same path from `210` to `263` accepted boxes. Pending boxes evolve `7 -> 4 -> 3 -> 4` as the adaptive stack moves into the final right panel, with current maximum depth `11`. Both regression counters remain frozen throughout v37: external `(4,1)` active quotient `36`, external `(4,2)` stable edge `129`.
- The next `981.1723444461823`-second v37 continuation closes the remaining four right-connector descendants at `267` boxes after `111.59047818183899` seconds of active partition work, then completes `S_X002_MC04_SM_DM_LEFT_CONNECTOR` at `2` boxes after `40.48430824279785` seconds. Production advances from `5/240` to `7/240` completed path jobs and enters the next top path at `6` accepted boxes.
- A subsequent `1083.508327960968`-second bounded continuation advances `S_X002_MC04_SM_DM_TOP` from `6` to `10` accepted boxes and reduces its pending stack from `4` to `3` at maximum depth `5`. No repeated failure class reaches an analytic or depth gate.
- The next `1260.7725841999054`-second continuation advances the same top path from `10` to `14` accepted boxes. One broad parent splits, changing the pending stack from `3` to `4`, but maximum depth remains `5`; no failure class becomes rate-dominant.
- A `1809.197589635849`-second continuation advances the top path from `14` to `35` accepted boxes, reduces pending boxes from `4` to `2`, and lowers maximum pending depth from `5` to `4`.
- The following `1846.2688994407654`-second pass advances from `35` to `54` accepted boxes with two pending depth-six children. Although one left-copy edge leads the cumulative split ledger, the stack continues shrinking and does not approach the depth-18 gate.
- The final `1826.0885694026947`-second closure test completes the top path at `56` boxes after `94.96174812316895` seconds of active partition work, then completes the right connector at `16` boxes after `1078.9053239822388` seconds. It enters `S_X002_MC04_SP_DM_LEFT_CONNECTOR` at `8` accepted and `2` pending boxes, advancing production from `7/240` to `9/240` completed paths.
- A bounded v37 continuation advances that left connector from `8` to `35` accepted boxes while its stack changes from `2` to `6` pending boxes and reaches depth `10`. The external `(4,1)` stable-angle split becomes rate-dominant at count `16`, so blind refinement stops and selects the v38 parent-angle derivation.
- The `1203.6301438808441`-second v38 production test completes the left connector at `57` boxes after `933.5098993778229` seconds of active partition work, completes the top path at `3` boxes after `86.79103636741638` seconds, and enters the right connector. The exact depth-ten regression leaf is accepted at its unchanged refinement path; the completed-state cleanup does not retain a cumulative split ledger, so no unsupported post-completion counter is reported.
- A `1213.7620270252228`-second ordinary v38 baseline advances the new right connector from `0` to `28` accepted boxes and exposes `15` external `(4,1)` stable-angle splits, `6` native external-`(0,1)` Jacobian splits, and maximum pending depth `8`. This selects the v39 invariant-quotient factorization rather than further blind angle refinement.
- The first `1230.9802379608154`-second v39 pass advances from `28` to `43` accepted boxes. The targeted angle counter adds only one split (`15 -> 16`) while the independent Jacobian counter adds eight (`6 -> 14`), moving the rate obstruction away from the angle.
- The `905.5787668228149`-second confirmation advances from `43` to `53` accepted boxes. The angle counter again adds one broad-parent split (`16 -> 17`) rather than a deep cascade, while the Jacobian reaches `20`. v39 is therefore recorded as a depth-eight half-plane repair and substantial rate reduction, not as an exact global counter freeze.
- Nine further bounded v39 resumes advance the right connector from `53` to `159` accepted boxes. Their runtimes are `1244.9172971248627`, `1222.0756304264069`, `1204.012722492218`, `1212.0108075141907`, `1211.7331445217133`, `1243.4929633140564`, `1222.2154371738434`, `1249.1365580558777`, and `1247.5967800617218` seconds. The pending stack evolves `7 -> 5 -> 7 -> 7 -> 6 -> 5 -> 8 -> 4 -> 4 -> 9`; accepted certificates are never discarded.
- Across those resumes the external `(4,1)` angle counter evolves `17 -> 17 -> 17 -> 17 -> 23 -> 29 -> 35 -> 35 -> 35 -> 35`, while the native external-`(0,1)` Jacobian counter evolves `20 -> 31 -> 33 -> 33 -> 33 -> 35 -> 38 -> 47 -> 56 -> 65`. The v39 identity therefore remains rate-stable after the second transition, and the active obstruction is now unambiguously the already-audited global-injectivity enclosure rather than a new angle formula.
- The first depth-twelve transition clears on the next production pass: accepted boxes rise `123 -> 139`, pending boxes fall `8 -> 4`, and maximum pending depth falls `12 -> 7` with no new external `(4,1)` split. The final pass reaches another Jacobian-driven depth-twelve leaf but remains six levels below the depth-18 gate and checkpoint-resumable; no closure is inferred from its point samples.
- A `1355.151959657669`-second continuation advances `159 -> 164` accepted boxes and reaches depth `13`; a deliberately shorter `634.687103509903`-second probe advances to `167` boxes and triggers the declared depth-fourteen derivation gate. Computation stops there rather than consuming the remaining depth allowance blindly.
- The exact depth-fourteen angle cover passes, whereas the new sourced Gale-Nikaido/Hessian audit does not yet prove its complete regulator/subarc family. No theorem flag is promoted. A `605.5370845794678`-second production regression then advances `167 -> 175`, reduces pending boxes `11 -> 9`, and lowers maximum depth `14 -> 13`.
- The final `627.358256816864`-second regression advances `175 -> 184`, reduces pending boxes `9 -> 5`, and lowers maximum depth `13 -> 8`. This confirms the audited branch was a finite interval transition rather than a physical zero; the accepted certificate is obtained by the unchanged v39 production proof, not by the still-failing theorem prototype.
- A `1245.0615711212158`-second continuation advances `184 -> 199` accepted boxes while the adaptive stack expands `5 -> 9` and reaches depth `13`; angle and Jacobian split counters move `46 -> 54` and `74 -> 80`. A shorter `630.0725882053375`-second pass then advances to `208`, contracts the stack to `5`, and returns maximum depth to `8`.
- The next `1201.8250455856323`-second continuation reaches `222` accepted and two depth-fourteen siblings. Both siblings pass path-addressed closed angle covers before production resumes. A `1223.9821138381958`-second continuation reaches `239`, lowers depth to `12`, and freezes the Jacobian counter at `83` during that pass.
- A `1251.2607231140137`-second pass reaches `251` and one new depth-fourteen leaf; its path-addressed closed angle audit also passes. The following `1221.4071910381317`- and `1237.8922245502472`-second continuations advance `251 -> 267 -> 285`, reduce pending boxes `9 -> 6 -> 4`, and return maximum depth `14 -> 10 -> 8`. The angle counter freezes during the first of those two passes; the later additions remain finite conservative parent splits rather than a maximum-depth obstruction.
- A `1229.2502381801605`-second continuation advances `285 -> 301`, expands the adaptive stack `4 -> 10`, and first reaches depth `16`. The path-addressed angle, centred-derivative, and sourced global-univalence audits classify that leaf before further production: the angle and derivative identities pass, while the global theorem cover remains unproved and uninstalled.
- A `604.7768232822418`-second regression clears that transition, advances `301 -> 310`, and lowers maximum depth `16 -> 11`. A `1224.9042224884033`-second continuation reaches `327` and a second depth-sixteen transition; its independent path-addressed angle cover also passes.
- Three shorter/ordinary continuations of `612.4111590385437`, `641.1361923217773`, and `1207.7166380882263` seconds advance `327 -> 334 -> 343 -> 356` and repeatedly return the frontier from depth `16/15/14` to depth `10`. The final `1235.9767694473267`-second pass reaches `362` accepted boxes and pauses at depth `15`, with all accepted certificates retained and `91.6137695312494%` of the connector area closed.
- A `657.966374874115`-second continuation advances `362 -> 366`, reduces pending boxes `9 -> 7`, and lowers maximum depth `15 -> 14`; the Jacobian counter remains frozen. A `1227.9135372638702`-second pass then reaches `380`, reduces pending boxes to `3`, and returns maximum depth to `8`.
- The next bounded run completes the remaining right-connector partition at `383` boxes after `95.52268314361572` seconds of active partition work. It also completes `S_X003_MC04_SM_DM_LEFT_CONNECTOR` at `1` box after `5.5450005531311035` seconds and its top path at `12` boxes after `976.737634897232` seconds, advancing production `11/240 -> 14/240`.
- A final `131.445378780365`-second resume commits the already-empty right-connector state at `1` box and advances production to `15/240`. It enters `S_X003_MC04_SP_DM_LEFT_CONNECTOR` with four shallow pending boxes. The enriched `558`-point selector audit then reproduces exactly the historical `15` transitions, proves all observed states are declared variants, and keeps the closed-cell selector claim false.
- Checkpoint 5397 proves the finite selector-union algorithm at all observed transition classes. Checkpoint 5398 closes all `5,841` completed source rows while preserving every path area exactly, and checkpoint 5399 archives all original v39 paths before atomically installing `5,891` staged rows. All 5397-5399 validation gates pass and all downstream claims remain locked.
- The first bounded v40 production resume accepts the migrated `15` completed paths, upgrades the active internal state from v39 without restarting it, and advances `S_X003_MC04_SP_DM_LEFT_CONNECTOR` from `0` to `3` accepted boxes. It pauses after `330.7487931251526` seconds with `7` pending boxes at maximum depth `8` and `resume_safe=true`.
- Five further bounded v40 resumes run for `613.1780281066895`, `632.1603112220764`, `1200.4686906337738`, `1830.929340839386`, and `3672.068526983261` seconds. They advance the same connector `3 -> 11 -> 14 -> 23 -> 38 -> 68` accepted boxes without discarding a certificate; the pending stack evolves `7 -> 3 -> 6 -> 6 -> 5 -> 10` and remains resume-safe.
- The latest production-matched obstruction probe does not discover a new denominator class. Seven of the ten saved boxes reach result construction; the deliberately minimal probe then stops only because it omits the output-only `regulator_bin_index`. The three boxes that fail earlier contain one already declared alternate-chart stable edge and two already declared native external-`(0,1)` Jacobian enclosures.
- A non-claim `7 x 7 x 3` point audit over every saved box finds the native path determinant positive, with sampled minima between `0.22456405835198512` and `1.7156175564057765`. Five near-point interval boxes retain rigorous positive determinant lower bounds, the smallest being `0.39709971484827944`. This rejects an obvious sampled sign change but is not substituted for a closed box proof: broad-box determinant enclosures still contain zero, and even `32` regulator-real subslabs do not remove the dependency inflation. Ordinary outer subdivision therefore remains the valid production route at depth `12`; no plateau, fitted Jacobian, or point-sample claim is installed.
- The next `1254.0178396701813`-second v40 resume advances `68 -> 75` accepted boxes and first reaches the agreed audit trigger at depth `14`, path `RDRDRDLDRDLDRU`. Production is stopped there rather than raising the depth gate.
- The three path-addressed audit runners are generalized to accept the mapped cell, term, and connector orientation explicitly; this removes their former `S_X002` right-connector lock without changing their legacy defaults. On the exact `S_X003_MC04_SP_DM` left-connector leaf, `108` centred-Hessian derivative comparisons reproduce the production dual derivatives with maximum relative error `4.031539536365012e-16`.
- The same depth-fourteen leaf has `324` point angles with minimum sampled modulus `0.005271474725447416`. More importantly, every closed factorized-angle cover passes: the `16 x 16` scheme proves `Im<14> >= 0.0021797980304410582`, while the `32 x 32` confirmation strengthens the modulus lower bound to `0.005027441170423727`. This is a path-addressed interval proof, not a sampling claim.
- The stronger Gale-Nikaido route is tested on that same left-connector leaf and remains unproved. Its `1`, `2`, and `4` subdivision schemes retain zero in at least one transformed principal minor, so `global_univalence_proved=false` and no Hessian/Jacobian shortcut is installed. The exact production evaluator nevertheless certifies the leaf pointwise with amplitude-denominator margin `0.0010892239252466565`.
- A confirming `609.5889000892639`-second production resume commits the audited transition, advances `75 -> 84` accepted boxes, reduces the pending stack `9 -> 7`, and returns maximum depth `14 -> 10`. This establishes that the trigger was a finite conservative transition rather than a new parent obstruction.
- Checkpoint 5400 freezes this evidence with `8/8` validation gates passing. It authorizes continued v40 production while keeping both `valid_for_global_univalence_claim=false` and `valid_for_regular_away_W3_claim=false`.
- A further `1817.3067564964294`-second continuation advances the left connector `84 -> 98` boxes and reaches adjacent depth-fourteen path `RDRDRDRDRDLDRU`. Its production evaluator already closes with denominator margin `0.001027653220649849`; the path-addressed derivative identity passes at `9.099443547528498e-15`, and the closed angle cover proves `Im<14> >= 0.0014911446327966968`.
- The confirming `602.0255937576294`-second resume completes the left connector at `110` boxes in `506.68999886512756` seconds of active partition work, completes the top path at one box in `4.508143901824951` seconds, and advances production `15/240 -> 17/240`. The new right-connector state begins with four shallow boxes and no accepted row.
- Checkpoint 5401 validates this transition with `9/9` gates and authorizes the sixth-support right connector only; global univalence and regular-away `W3` remain false.
- The first `1818.19091796875`-second right-connector continuation advances `0 -> 26` accepted boxes and reaches depth-fourteen path `LDLDLDRDLDLDRU`. Its production evaluator closes with denominator margin `0.0010155353873341282`; the path-addressed derivative identity passes at `5.837451876955707e-16`, and the closed angle cover proves `Im<14> >= 0.001364553706249723`.
- A confirming `605.2740468978882`-second resume commits the audited leaf and advances the right connector `26 -> 34` accepted boxes. Checkpoint 5402 validates the resulting `12`-box, depth-`13` frontier while preserving all broad claim locks.
- Six further bounded continuations run for `1211.0177969932556`, `1202.570997953415`, `1826.8815319538116`, `1821.541865348816`, `1810.1908810138702`, and `1802.6198947429657` seconds. They advance the right connector `34 -> 52 -> 70 -> 95 -> 118 -> 139 -> 164` accepted boxes; the pending stack ends at `6` and maximum depth at `8`, so no new depth-trigger derivation is required.
- Checkpoint 5403 validates exact accepted-plus-pending parameter partitioning and the preserved audited row with `8/8` gates. It authorizes continued right-connector production only.
- Two subsequent one-hour v40 continuations advance the sixth-support right connector `164 -> 216 -> 264` accepted boxes and expose the second depth-fourteen right-connector path `RDLDRDRDLDLDRU`. Its unchanged production evaluator closes with amplitude-denominator margin `0.0010154847978798421` and collision-Jacobian lower bound `0.07480940521750942`.
- On that exact leaf, all `108` centred-derivative comparisons pass with maximum relative error `5.851378006985454e-16`. Every closed factorized-angle scheme lies in the positive-imaginary half-plane; the weakest certified lower bound is `Im<14> >= 0.0013575974869823363`, and the `32 x 32` cover gives `|<14>| >= 0.004676133928216189`.
- A confirming `618.8181116580963`-second production resume commits the audited leaf and advances `264 -> 272` accepted boxes. The pending frontier contracts to `7` boxes at maximum depth `10`, while exact accepted-plus-pending area is preserved and connector coverage reaches `0.5410156250000006`.
- Checkpoint 5404 freezes the second right-connector trigger with `9/9` validation gates passing. It authorizes only continued v40 production and leaves every broad theorem and physics claim locked.
- The next `3612.8885214328766`-second continuation advances the sixth-support right connector `272 -> 312` accepted boxes, raises exact area coverage to `0.6329956054687516`, and reaches depth-fourteen path `RDRDLDRDLDLDRU`.
- Its unchanged production evaluator closes with amplitude-denominator margin `0.0010154745605041326` and collision-Jacobian lower bound `0.08350093040679053`. All `108` centred-derivative comparisons pass with maximum relative error `5.854110513961395e-16`.
- Every path-addressed closed factorized-angle cover again lies in the positive-imaginary half-plane. The weakest lower bound is `Im<14> >= 0.0013562026730020496`, while the `32 x 32` scheme proves `|<14>| >= 0.004674574875521507`.
- A confirming `605.786417722702`-second production resume commits the leaf, advances `312 -> 320` accepted boxes, and returns the frontier to `10` pending boxes at maximum depth `13`. Connector coverage reaches `0.6347656250000017` with exact accepted-plus-pending area preserved.
- Checkpoint 5405 freezes this third right-connector trigger with `9/9` validation gates passing and authorizes continued v40 production only.
- The next `3612.306081533432`-second continuation advances the sixth-support right connector `320 -> 363` accepted boxes, raises exact area coverage to `0.664672851562501`, and exposes adjacent depth-fourteen siblings `RDRDRDRDLDRDRD` and `RDRDRDRDLDRDRU`.
- Both unchanged production evaluations pass. Their amplitude-denominator lower bounds are `0.0003970570095262492` and `0.0010154629949767665`; all `216` centred-derivative comparisons reproduce the direct derivatives with maximum relative error `5.958299164063814e-16`.
- Every path-addressed closed factorized-angle scheme passes on both siblings. The weaker branch still proves `Im<14> >= 0.0007456935308613039`, so neither leaf requires a point-sample claim or an added closure.
- A confirming `621.7110478878021`-second production resume commits both leaves, advances `363 -> 371` accepted boxes, and returns the frontier to `7` pending boxes at maximum depth `12`. Connector coverage reaches `0.6662597656250011` with exact accepted-plus-pending area preserved.
- Checkpoint 5406 validates this adjacent-sibling transition with `9/9` gates and authorizes continued v40 production only.
- The next `3693.9861183166504`-second production run completes the sixth-support right connector at `382` boxes after `545.5024292469025` seconds of active partition work. Its exact parameter-area sum is `0.01767396674516035`, its minimum amplitude-denominator lower bound is `0.0003970550036330211`, and all five audited depth-fourteen leaves remain committed.
- The same unchanged proof then completes all three `S_X004_MC04_SM_DM` paths at `1`, `4`, and `1` boxes; all three `S_X005_MC04_SM_DM` paths at `1`, `11`, and `2` boxes; and the `S_X006_MC04_SM_DM` left connector at `4` boxes. Production therefore advances `17/240 -> 25/240` completed path jobs without another analytic closure.
- The run enters `S_X006_MC04_SM_DM_TOP` and pauses with `25` accepted and `4` pending boxes at maximum depth `6`. Accepted-plus-pending area reproduces the complete top rectangle, with current coverage `0.65625` and `resume_safe=true`.
- Checkpoint 5407 validates the eight completed path jobs and the new active partition with `9/9` gates while preserving every broad claim lock.
- The next production pass completes `S_X006_MC04_SM_DM_TOP` at `42` boxes and its right connector at `4` boxes, advancing production `25/240 -> 27/240`. It then enters `S_X006_MC04_SP_DP_LEFT_CONNECTOR` and reaches depth-18 leaf `LLDLDLDLDLDLDLDLDL`, where independent spinor reconstruction loses the nonzero right-copy square edge `[14]` even though point evaluation gives `|[14]| approximately 0.03226`.
- The tempting `{1,4}` invariant override is explicitly rejected: the global displacement lifts that invariant only to order `10^-10`, and its phase winds around the four global contour arcs. Revision v41 instead derives the exact representative-chart ratio `[14] = -T/(1-z) - hbar_1/p1_plus`, eliminating the duplicated spinor dependencies without introducing a closure or fitted denominator.
- The derived ratio agrees with the direct rational-spinor bracket on all four point probes with maximum absolute error `1.1102230318952986e-16`. A finite `16 x 16` path subcover on each global arc proves a minimum absolute lower bound `0.02008154891211636` over the complete failed box.
- The unchanged full production evaluator then certifies the formerly failing leaf with minimum amplitude-denominator lower bound `0.0003981115169413247`. A bounded v41 resume commits that exact row and advances the active path to `7` accepted plus `15` pending boxes at maximum depth `16`, with `resume_safe=true`.
- Checkpoint 5408 freezes the direct identity, the `1,024`-leaf closed subcover, the v40-to-v41 manifest migration, and the committed production row with `9/9` validation gates passing. It authorizes continued v41 production only and preserves every broad claim lock.
- Three further bounded v41 resumes advance the same left connector `7 -> 13 -> 22 -> 37` accepted boxes. Every depth-17 descendant encountered during those runs certifies, the pending stack changes `15 -> 16 -> 16 -> 13`, and maximum pending depth recedes `16 -> 17 -> 17 -> 14` without another depth-18 failure.
- Accepted plus pending boxes still reproduce the complete parameter rectangle at `0.03932753620548857`. Current accepted-area coverage is `0.00036621093750002065`; the small fraction reflects the deliberately fine endpoint transition rather than discarded area.
- The split ledger contains only the five pre-existing outer-enclosure categories. No new representative external-4/first square class appears, and all `30` post-5408 rows retain positive denominator bounds and false broad-claim flags.
- Checkpoint 5409 validates this production regression with `10/10` gates. It classifies the current frontier as ordinary outer subdivision, not a missing analytic identity, and authorizes continued v41 production only.
- A further v41 continuation advances the active left connector `37 -> 59` accepted boxes but repeatedly refines one depth-17 geometric-denominator transition. Inspection shows that only the boxed collision Jacobian reaches zero; the relative and selected-global roots remain separated, so the split is an interval-dependency problem rather than evidence of a physical collision.
- Revision v42 removes an artificial square-cover restriction from the already-derived projective collision chart. It partitions the same parent `(x,t)` rectangle into independent `N_x x N_t` closed subrectangles: on the historical depth-15 parent, the `8 x 8` enclosure retains zero whereas the exact `8 x 16` cover proves `|J| >= 27.466941964759005` with projective-denominator lower bound `0.007260540309702259`.
- A bounded v42 regression migrates the v41 state without discarding accepted rows and advances the active connector to `67` accepted plus `12` pending boxes at maximum depth `14`. Two committed production rows use the new `8 x 16` proof; accepted plus pending area remains exactly `0.03932753620548857`.
- Checkpoint 5410 validates the anisotropic cover and production migration with `10/10` gates. It authorizes continued v42 production only; complete-path, regular-away `W3`, event-local, UV, local-GR, and full-MTS claims remain false.
- Two further bounded v42 resumes advance the active left connector `67 -> 92 -> 105` accepted boxes. The pending stack ends at `13` boxes and maximum depth `16`; exact accepted-plus-pending area remains `0.03932753620548857`.
- A production-matched evaluation of the intervening depth-17 frontier path `LLDLDLDRDLDRDRDLU` proves amplitude-denominator margin `0.0004176254172608071` and collision-Jacobian lower bound `20.715268296834868` under the existing exact `8 x 8` projective cover. The confirming resume commits that row and recedes from depth `17` without changing the proof.
- Exact `8 x 16` production certificates increase from `2` at checkpoint 5410 to `14`. The geometric-denominator failure counter remains frozen at `27`; the complete split ledger still contains exactly the same five declared outer-enclosure categories, so no new analytic obstruction is exposed.
- Checkpoint 5411 validates the same-revision continuation with `13/13` gates. It authorizes continued v42 production only and preserves all broad claim locks.
- A one-hour v42 continuation advances the active connector `105 -> 156` accepted boxes while contracting its pending stack `13 -> 10` and maximum depth `16 -> 12`. Certified parameter-area coverage increases from `0.4959106445312516%` to `1.977539062500006%` without restarting the partition.
- Exact `8 x 16` projective certificates increase `14 -> 49`; the geometric-denominator counter remains frozen at `27`, and the failure ledger retains exactly the same five declared outer-enclosure categories. Thus the v42 repair continues to remove the named dependency while later refinements remain ordinary stable-edge subdivisions.
- Accepted plus pending area still reproduces `0.03932753620548857`. All `156` accepted rows have finite positive amplitude-denominator and collision-Jacobian margins, while every broad claim flag remains false.
- Checkpoint 5412 validates this frontier contraction with `14/14` gates and authorizes continued v42 production only.
- A two-hour v42 continuation advances the active connector `156 -> 254` accepted boxes, reduces pending boxes `10 -> 9`, and raises certified area coverage `1.977539062500006% -> 8.22753906249998%` while maximum depth remains `12`. The geometric-denominator counter stays frozen at `27` and no new class appears during that run.
- A final half-hour continuation reaches `284` accepted boxes and approximately one-quarter area coverage, then exposes the first left-copy hard-2/soft stable-angle failure at coarse path `LRD`: `away_arc_left_K5:s1:c0:left0:edge_2_2_3:stable_edge`.
- Revision v43 derives that edge in its uniform hard-minus/soft-plus chart. At chirality zero the global unit-circle factors cancel exactly, leaving a correlated function only of the parent `(x,t,epsilon)` path variables. The unsubdivided and `2 x 2` enclosures contain zero, while exact `4 x 4` and `8 x 8` covers prove `|<23>| >= 0.9305401862269655` and `|<23>| >= 2.2936470483431965` respectively.
- Nine point crosschecks reproduce the parent hard-soft edge with maximum absolute error `2.0419256932316684e-18`. Re-evaluating the complete historical parent advances past `(2,3)` and stops only at the previously declared left-copy `(4,2)` stable-edge class, so the repair removes the named dependency without claiming that the coarse parent is wholly certified.
- The v42 adaptive state migrates to v43 and remains resume-safe at `285` accepted plus `11` pending boxes, maximum depth `12`, with exact accepted-plus-pending area preserved. Checkpoint 5413 validates the identity, finite covers, failure-order advance, and migration with `13/13` gates.
- A two-hour and a one-hour v43 continuation advance the active connector `285 -> 366 -> 418` accepted boxes. The pending stack contracts `11 -> 6 -> 9`, maximum depth ends at `10`, and certified area coverage rises `25.0244140625% -> 56.25%` without restarting the partition.
- The repaired left `(2,3)` failure counter remains frozen at its single historical pre-v43 occurrence throughout all `133` new accepted rows. The ledger retains exactly six declared categories, the geometric-denominator counter remains frozen at `27`, and no seventh analytic obstruction appears.
- All `418` accepted rows retain finite positive amplitude-denominator and collision-Jacobian margins. Accepted plus pending area still reproduces `0.03932753620548857`, while every broad claim flag remains false.
- Checkpoint 5414 validates the post-repair production stability and frontier contraction with `14/14` gates and authorizes continued v43 production only.
- A two-hour v43 completion run advances the active left connector `418 -> 511` accepted boxes and empties its pending stack after `5024.318186283112` seconds of active partition work. The completed source and partition CSVs agree row-for-row, and their parameter-area sum is exactly `0.03932753620548857`.
- The same run certifies `S_X006_MC04_SP_DP_TOP` as one box in `12.83511233329773` seconds, advancing production `27/240 -> 29/240` completed path jobs. It then enters the right connector and pauses resume-safe at `21` accepted plus `12` pending boxes, maximum depth `13`.
- The new right-connector ledger contains only four previously declared classes: left hard-soft `(1,3)`, left external-hard `(4,2)`, right external-first `(0,1)`, and the first-spinor pivot. No new analytic obstruction is exposed.
- Checkpoint 5415 validates both completed paths, their exact source/partition equality, the active-right area partition, and all `533` completed-plus-active proof rows with `12/12` gates.
- Two bounded v43 continuations advance the preserved right-connector state `21 -> 109 -> 131` accepted boxes. The frontier ends with `14` pending boxes at maximum depth `17`, exact accepted-plus-pending area `0.03932753620548857`, and certified area coverage `1.6860961914062614%`.
- A production-matched audit of depth-17 path `LLDLDRDLDLDRDLDRU` proves amplitude-denominator lower bound `0.000613556963558325` and collision-Jacobian lower bound `4.924765244435844` under the exact `SUBDIVIDED_PATH_CORRELATED_PROJECTIVE_UNION_8X8` enclosure. The confirming continuation commits that exact row without altering the proof.
- The right-connector split ledger still contains only the four classes already declared at checkpoint 5415. All `131` accepted rows retain finite positive proof margins, while every broad claim flag remains false.
- Checkpoint 5416 validates the committed finite transition, exact area partition, unchanged failure-class set, and claim boundary with `14/14` gates. It authorizes continued v43 production only.
- A two-hour continuation and one-hour confirmation sequence advance the preserved right connector `131 -> 203 -> 233` accepted boxes. Certified area coverage rises `1.6860961914062614% -> 6.378173828124999%`; accepted plus pending area remains exactly `0.03932753620548857`.
- The `102` newly accepted rows have minimum amplitude-denominator lower bound `0.00010237716235513576` and minimum collision-Jacobian lower bound `0.7281267861815186`. The failure ledger remains exactly the same four declared classes, so no new identity, closure, or fitted repair is introduced.
- The frontier ends resume-safe with `14` pending boxes at maximum depth `17`. Checkpoint 5417 validates the same-revision expansion, preserved checkpoint-5416 audited row, monotone failure ledger, exact area partition, and false broad claims with `15/15` gates.

## Claim boundary

Checkpoint 5396 now certifies `29/240` contour path jobs, including genuine external-collinear singular cells in both connector orientations, every completed path through `S_X006_MC04_SM_DM`, and the left/top paths of `S_X006_MC04_SP_DP`. Checkpoints 5397-5399 close the selector-transition ownership defect for every completed row and install a closed-margin-or-chart-union rule for future boxes without midpoint ownership. Checkpoint 5408 removes one representative-spinor interval dependency by an exact identity, checkpoint 5410 removes one projective collision-Jacobian dependency by an exact anisotropic finite cover, checkpoints 5411-5412 demonstrate stable frontier contraction, checkpoint 5413 removes the newly reached left hard-2/soft mixed-angle dependency by another exact finite cover, checkpoint 5414 confirms that repair across `133` further accepted boxes, checkpoint 5415 closes two complete path jobs, checkpoint 5416 commits the first depth-17 right-connector transition, and checkpoint 5417 expands that same frontier by `102` further certified boxes without exposing a new class. The other `211` path jobs, the complete regular-away integral, and the event-local remainder remain open. Therefore the regular-away `W3`, combined `W3`, regulator-limit, UV, local-GR, and full-MTS claims remain false until those independent gates close.

## Next action

Before the next long resume, evaluate the two current depth-17 siblings `LLDRDLDLDRDRDRDLD` and `LLDRDLDLDRDRDRDLU` with the unchanged production-matched evaluator. If they close, commit them through a short confirming run and continue `S_X006_MC04_SP_DP_RIGHT_CONNECTOR` from its preserved `233` accepted and `14` pending boxes; if either fails, classify its exact surviving factor before adding any repair. Allow the exact anisotropic projective and left hard-2/soft covers only where their parent enclosures are dependency-broadened, and require any newly exposed edge to be classified as a physical zero, an exact cancellation, or an interval dependency before changing the production proof. Use the positive closed selector margin whenever it exists and the complete finite chart union otherwise; do not restore midpoint ownership. Keep the sourced Gale-Nikaido/Hessian route explicitly non-production unless a complete closed cover passes, preserve the distinction between pointwise and integrated simple-pole rows, and keep the regular-away, event-local, combined `W3`, regulator-limit, UV, local-GR, and full-MTS claims false.
