# 5406 — Sixth-right adjacent depth-trigger gate

## Purpose

Checkpoint 5406 advances the sixth-support right connector and audits both adjacent depth-fourteen siblings exposed by the same parent split. Neither sibling is allowed back into production from point samples alone.

## Result

- The one-hour continuation advances the connector `320 -> 363` accepted boxes and raises area coverage to `0.664672851562501` before exposing paths `RDRDRDRDLDRDRD` and `RDRDRDRDLDRDRU`.
- Both ordinary production evaluations pass. Their amplitude-denominator lower bounds are `0.0003970570095262492` and `0.0010154629949767665`; their collision-Jacobian lower bounds are `1.2974283268065656` and `0.09318473085244246`.
- All `216` centred-derivative comparisons pass. The two maximum relative errors are `5.958299164063814e-16` and `4.736186993205518e-16`.
- Every closed factorized-angle scheme passes on both siblings. The common weakest positive-imaginary lower bound is `0.0007456935308613039`.
- The confirming `621.7110478878021`-second resume commits both leaves and advances `363 -> 371` accepted boxes. The remaining frontier has `7` pending boxes at maximum depth `12`.
- Accepted parameter area is `0.011775452941294597`, or `0.6662597656250011` of the complete connector rectangle. Accepted plus pending area remains complete.

## Decision

Both adjacent transitions are finite under the unchanged production proof. Revision v40 may continue from the preserved frontier without adding a closure or raising the depth allowance. Global univalence, regular-away `W3`, regulator removal, local GR, and full MTS remain unproved.

Machine-readable validation is stored in `source-intake/functional_rg/5406/P8_Y5_BRR5396_5406_VALIDATION.csv` and `source-intake/functional_rg/5406/sixth_right_adjacent_depth_trigger_gate_result.json`.
