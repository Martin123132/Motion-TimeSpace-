# 344 - Amplitude Closure Claim Ledger Update

Private discipline checkpoint. This is not a public cosmology, CMB, local-GR, PPN, perturbation-theory, or unified-field claim.

## Purpose

Checkpoint 343 made the hard call:

```text
B_mem = 2/27
```

is no longer to be treated as nearly parent-derived.

This checkpoint updates the local claim ledger so the rest of the programme does not drift back into overclaiming.

Short answer:

```text
B_mem = 2/27 is now an explicit locked closure value,
an empirical test pillar,
and a theorem target.
```

It is not:

```text
a parent-derived amplitude.
```

## Machine Artifact

Script:

```text
scripts/amplitude_closure_claim_ledger_update.py
```

Run:

```text
runs/20260601-213000-amplitude-closure-claim-ledger-update
```

Status:

```text
claim_ledger_updated_Bmem_2over27_explicit_closure_not_parent_derived
```

Claim ceiling:

```text
ledger_update_no_parent_derivation_claims
```

## Claim Ledger

| Claim | Current status | Allowed claim | Forbidden claim |
|---|---|---|---|
| `B_mem=2/27` | explicit closure value | locked empirical closure branch/theorem target | parent-derived amplitude |
| `epsilon_H=1` | closure/fitted coupling; conditional theorem under exact readout | unit value in locked branch | unconditional Hamiltonian normalization |
| `q_trace=2/27` | conditional algebraic identity | exact if rank/dim assumed | rank/dim derived from parent action |
| `H_star=H0` | closure/theorem target | fixed calibration in locked branch | parent-derived calibration |
| locked no-clock branch | empirical closure branch retained | competitive enough to keep testing | stable cosmological evidence |
| finite-fibre route | conditional mechanism | future theorem route | current proof of dim/rank/no-marker |
| exact-readout route | conditional mechanism | counterterm forbidden if premises hold | parent-derived readout status |
| local GR/PPN | independent unresolved gate | separate theory gate remains necessary | solved by amplitude closure |
| CMB/growth bridge | independent unresolved gate | needs separate bridge/robustness work | solved by SN/BAO closure score |

## Transition

Before checkpoint 343:

```text
2/27 was a locked empirical lead and active theorem target.
```

After checkpoint 343:

```text
2/27 is a locked empirical closure value and theorem target,
not a parent-derived result.
```

That means every future document should obey:

```text
use 2/27,
test 2/27,
stress 2/27,
but do not claim MTS derives 2/27.
```

unless a genuinely new parent theorem is supplied.

## Gate Results

| Gate | Result |
|---|---|
| source paths exist | pass |
| `B_mem` marked explicit closure | pass |
| `B_mem` parent derivation forbidden | pass |
| `epsilon_H` not unconditionally derived | pass |
| `q_trace` marked conditional | pass |
| empirical branch retained | pass |
| local GR not solved by amplitude | pass |
| CMB/growth not solved by late-time closure | pass |

## Next Gate Options

| Priority | Gate | Why |
|---:|---|---|
| 1 | empirical locked-branch robustness | amplitude is closure, so testing is now the productive path |
| 2 | `H_star=H0` calibration gate | factorization still contains an independent calibration assumption |
| 3 | local GR/PPN independent gate | a unified field theory must reduce to GR locally |
| 4 | CMB/growth bridge gate | late-time closure does not prove early-universe or perturbation safety |

## Standing

The current honest status is:

```text
MTS has a disciplined late-time closure branch with B_mem=2/27.
```

Not:

```text
MTS has derived the 2/27 amplitude from the parent action.
```

This is a better scientific posture. It makes the branch easier to test and harder to accidentally oversell.

## Next Target

Next:

```text
post_343_empirical_locked_branch_or_independent_theory_gate.
```

Recommended default:

```text
build a post-343 empirical locked-branch robustness manifest,
then run/source-lock the next fair comparison when data and runtime are ready.
```

If we instead stay theoretical, the cleanest independent gate is:

```text
H_star = H0
```

because it is still in the amplitude factorization:

```text
B_mem = q_trace * epsilon_H * (H_star/H0)^2.
```
