# 242 - Strict Local Coframe Branch or Domain Projector Action

Private local-derivation checkpoint. This is not a public WEP, clock, PPN,
local-GR, BAO, or field-theory completion claim.

## 1. Trigger

Checkpoint 241 left the honest fork:

```text
derive why the local matter coframe is strict metric/coframe only,
or derive a Bianchi-safe domain projector that suppresses local C residuals.
```

This checkpoint separates the two jobs instead of mixing them.

## 2. Machine Artifact

Script:

```text
scripts/strict_local_coframe_branch_or_domain_projector_action.py
```

Run:

```text
runs/20260601-000059-strict-local-coframe-branch-or-domain-projector-action
```

Command:

```text
python scripts/strict_local_coframe_branch_or_domain_projector_action.py --timestamp 20260601-000059
```

Status:

```text
strict_local_coframe_branch_selected_as_local_C_silence_contract_domain_projector_retained_for_cosmology_no_promotion
```

Claim ceiling:

```text
strict_coframe_conditional_theorem_no_parent_selection_or_local_GR_promotion
```

## 3. Branch Decision

The local branch should use:

```text
strict local matter coframe.
```

Meaning:

```text
S_matter = sum_A S_A[Psi_A, ehat, omega[ehat], constants_A],
```

with local representative:

```text
ehat^a_mu = Omega_D e^a_mu,
partial_mu Omega_D = 0.
```

So:

```text
beta_C^loc := partial ln ghat_mu_nu / partial C |loc = 0.
```

The domain projector route is retained for:

```text
cosmology,
endpoint memory,
BAO common-mode bookkeeping.
```

But it is not accepted as:

```text
a local-GR rescue.
```

unless its representative, response bound, and stress ledger are parent-derived.

## 4. Conditional Theorem

If the parent action selects the strict local representative:

```text
R_loc[D_bound]: ehat^a_mu = Omega_D e^a_mu,
partial_mu Omega_D = 0,
```

then:

```text
delta_C S_matter
= 1/2 int sqrt(-ghat) T_hat beta_C^loc delta C
= 0.
```

Therefore:

```text
c_matter^direct = 0,
c_clock^direct = 0,
c_C_trace^loc = 0.
```

This is the clean local result:

```text
local matter cannot source C because C is not a local matter-metric variable.
```

## 5. Why The Projector Is Not Enough Locally

Checkpoint 207 made the domain projector formal:

```text
C = C_D + C_perp,
integral_D sqrt(h) W_D C_perp = 0.
```

That helps.

But for local GR it still lacks:

```text
physical representative selection,
parent-derived domain scale,
response operator below the local gate,
Bianchi-safe projector/domain stress.
```

The local gate from checkpoint 241 needs a response fraction:

```text
<= 0.000621
```

relative to `B_mem`.

No parent response operator currently proves that.

So:

```text
Pi_D is not allowed as a silent local rescue knob.
```

## 6. Bianchi Discipline

Strict local coframe route must keep:

```text
T_matter + T_metric + T_memory + T_boundary.
```

Domain projector route must keep:

```text
T_matter + T_C + T_Pi + T_D + T_chi + T_rel + T_boundary.
```

Forbidden shortcut:

```text
use Pi_D in the equations,
then drop T_Pi, T_D, or boundary stress.
```

That would fake conservation.

## 7. What Improved

Before this checkpoint:

```text
C-silence had two live routes mixed together.
```

After this checkpoint:

```text
strict local coframe is the lead local-GR branch,
domain projection is a cosmology/BAO branch unless fully parent-derived.
```

The win is conceptual but important:

```text
we no longer let cosmology's useful zero-mode become a local-test escape hatch.
```

## 8. What Still Fails

Still not derived:

```text
R_loc[D_bound] from the parent action,
the local representative-selection law,
N1 conserved M_eff,
N2 no trace-free/tangential stress,
N4 exact relative memory,
N5 projector-stress cancellation,
N6 auxiliary no-hair,
metric-only Einstein-Hilbert exterior.
```

So this checkpoint does not promote:

```text
local GR,
PPN safety,
WEP pass,
clock pass,
BAO support,
or a completed parent action.
```

## 9. Gate Results

| gate | result |
|---|---|
| all cited local sources exist | pass |
| strict local coframe theorem written | conditional pass |
| domain projector judged for local branch | pass |
| parent selects strict local coframe | fail |
| domain projector response bound derived | fail |
| local GR or PPN promoted | fail |

## 10. Decision

Decision:

```text
strict_local_coframe_branch_selected_as_local_C_silence_contract_domain_projector_retained_for_cosmology_no_promotion
```

Meaning:

```text
local C-silence is now reduced to a precise representative-selection theorem:
the parent must force the observed local matter coframe to be independent of C
up to constant unit rescaling.
```

Main gain:

```text
the local branch now has a cleaner non-cheating target.
```

Main failure:

```text
the parent action still has not selected the local representative or derived
the metric-only exterior.
```

## 11. Next Target

Create:

```text
243-local-representative-selection-action-or-no-shear-gate.md
```

Purpose:

```text
either derive the parent action that selects R_loc[D_bound],
or attack N2 no trace-free/tangential stress as the next local-GR gate.
```

Pass condition:

```text
stationary bound domains select the strict local matter coframe before local
tests are applied,
```

or:

```text
the no-shear theorem removes the gamma/slip channel independently.
```

Fail condition:

```text
strict local coframe is assumed because PPN needs it.
```
