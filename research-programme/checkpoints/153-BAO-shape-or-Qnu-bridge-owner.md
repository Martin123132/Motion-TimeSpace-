# 153 - BAO Shape or Qnu Bridge Owner

Private bridge-owner checkpoint. This is not a public claim.

## 1. Trigger

Checkpoint 152 narrowed the CMB bridge problem:

```text
alpha/r_d is not the repair,
the conservative Omega map is identity,
and B_mem*(2/9) is a strong clue but not a theorem.
```

This checkpoint asks the next live question:

```text
Should the bridge chase a BAO-shape/ruler owner,
or jump to a matter-memory exchange current Q^nu?
```

Short answer:

```text
The BAO-shape route is sharper, but a pure H(z) correction is not enough.
The live lower-risk route is an anisotropic/projective BAO owner.
Q^nu stays a fallback because it threatens local GR and growth.
```

## 2. Machine Artifact

Script:

```text
research-programme\scripts\BAO_shape_or_Qnu_bridge_owner.py
```

Run:

```text
research-programme\runs\20260531-235958-BAO-shape-or-Qnu-bridge-owner
```

Generated:

```text
source_register.csv
failed_gate_shape_target_rows.csv
observable_shape_summary.csv
redshift_shape_summary.csv
pure_metric_consistency.csv
owner_route_matrix.csv
theorem_contract.csv
gate_results.csv
decision.csv
status.json
```

Status:

```text
BAO_shape_owner_route_sharpened_pure_Hz_repair_rejected_Qnu_deferred
```

Claim ceiling:

```text
BAO_shape_or_Qnu_owner_contract_no_bridge_promotion
```

## 3. What the Failed Gate Wants

The comparison is:

```text
failed joint branch:
joint_tied_alpha__LCDM__strict_full4__MTS_locked_2over27

target late branch:
late_T7_locked_2over27_free_alpha__MTS_locked_2over27
```

The failed joint branch needs a real radial correction:

```text
5/6 DH rows require lower H than the failed joint branch.
5 DH rows at z >= 0.7 require lower H.
max inferred |delta H/H| from DH rows = 0.015182831800200653.
```

So the radial pressure is not imaginary. The CMB-driven joint branch is too stiff in radial expansion compared with the late-only locked branch.

## 4. The Catch

A pure single-metric H(z) repair is not enough.

The audit finds:

```text
5/6 DM rows require lower transverse distance than the failed joint branch.
```

But lowering H to raise `DH/rs` normally raises the integral distance `DM/rs`.

So the signs fight:

```text
DH wants lower H.
Most DM rows want lower DM.
```

That means the owner cannot simply be:

```text
adjust H(z) a bit.
```

The owner must be one of:

```text
anisotropic/projective BAO ruler response,
full Boltzmann inference response,
or a screened Q^nu exchange map.
```

## 5. Route Matrix

| Route | Status | Readout |
|---|---|---|
| scalar `alpha` or `r_d` | rejected | checkpoint 152 already showed this is not the lever |
| pure single-metric `H(z)` shape | insufficient | fixes radial pressure but conflicts with most transverse targets |
| anisotropic BAO projection owner | best shape route | can separate radial and transverse BAO response without immediate matter exchange |
| screened `Q^nu` exchange | high-risk fallback | can move Omega, but threatens local GR/growth |
| full Boltzmann inference map | needed eventually | compressed priors may be misleading |

This is useful because it stops the theory from making the wrong repair.

The target is no longer just:

```text
derive a radial H correction.
```

The target is:

```text
derive a parent projection/ruler tensor that changes radial and transverse BAO observables consistently.
```

## 6. Owner Contract

Any BAO-shape owner must satisfy:

```text
B_mem -> 0 implies delta_BAO_shape -> 0.
```

It must derive together:

```text
delta(DH/rs),
delta(DM/rs),
delta(DV/rs),
```

without fitting row-by-row residuals.

It must also:

```text
preserve source-locked H(z) draw,
preserve growth suppression,
vanish in local/PPN regimes,
and survive the future CMB kill-screen.
```

If the only viable owner is `Q^nu`, then the contract becomes much harsher:

```text
nabla_mu T_m^{mu nu} = Q^nu
nabla_mu T_mem^{mu nu} = -Q^nu
Q^nu -> 0 locally
no fitted growth fudge
no violation of Bianchi identity
```

## 7. Decision

Current fair status:

```text
BAO-shape route sharpened.
Pure H(z) repair rejected as complete owner.
Q^nu deferred.
```

Next checkpoint:

```text
154-anisotropic-BAO-projection-owner-attempt.md
```

That is the cleanest next jab: try to derive a projection/ruler tensor before throwing the heavy `Q^nu` punch.
