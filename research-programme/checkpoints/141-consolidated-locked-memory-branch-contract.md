# 141 - Consolidated Locked Memory Branch Contract

Private branch contract. This is not a public claim.

## 1. Purpose

The previous checkpoints split the locked memory branch into parts:

```text
pressure kernel: conditional theory route
activation shape: conditional theory route
amplitude: empirical closure
growth owner: effective subhorizon EFT
local GR: open
```

This checkpoint consolidates the branch so we do not accidentally promote one part because another part improved.

Short verdict:

```text
locked memory is a serious empirical EFT closure with conditional theory mechanics.
It is not a derived parent field theory.
```

## 2. Machine Artifact

Script:

```text
research-programme\scripts\consolidated_locked_memory_branch_contract.py
```

Run:

```text
research-programme\runs\20260531-210000-consolidated-locked-memory-branch-contract
```

Generated:

```text
source_register.csv
branch_equation_contract.csv
status_stack.csv
promotion_requirements.csv
allowed_test_matrix.csv
claim_control_ledger.csv
input_factor_ownership_ledger.csv
gate_results.csv
decision.csv
status.json
```

Status:

```text
locked_memory_branch_consolidated_as_empirical_EFT_closure
```

Claim ceiling:

```text
locked_memory_empirical_EFT_closure_with_conditional_theory_mechanics
```

## 3. Branch Equations

| Object | Equation | Status |
|---|---|---|
| coherent volume time | `N_D=(1/3)ln(V_D0/V_D)` | conditional theory route |
| activation exposure | `I_M=det(Q)`, `Q^i_j=X delta^i_j` | conditional theory route |
| activation shape | `A=1-exp(-I_M)` | conditional theory route |
| memory density | `rho_M=rho_Lambda+B_mem A` | EFT closure with conditional shape |
| memory pressure | `p_M=-rho_M+(1/3)d rho_M/dN_D` | conditional theory route |
| amplitude | `B_mem=2/27` | empirical locked closure |
| growth owner | high-sound-speed / canonical EFT owner | effective EFT support |

This is the main discipline:

```text
conditional mechanics do not make the empirical amplitude derived.
```

## 4. What Can Be Said

Allowed internal wording:

```text
The locked memory branch is a serious testable EFT closure candidate.
```

More precise:

```text
The branch has conditional derivation routes for pressure and activation shape,
and a frozen empirical amplitude B_mem=2/27 that must not be refit branch by
branch.
```

Allowed:

```text
Use B_mem=2/27 as a predeclared no-refit branch in future tests.
```

Allowed:

```text
Treat late-time growth as an effective subhorizon target if the high-sound-speed
owner assumptions are stated.
```

## 5. What Cannot Be Said

Forbidden:

```text
MTS derives B_mem=2/27.
```

Forbidden:

```text
MTS derives local GR.
```

Forbidden:

```text
MTS passes CMB.
```

Forbidden:

```text
MTS has a complete unified field theory.
```

Too strong:

```text
MTS derives pressure.
```

Replacement:

```text
the pressure kernel follows conditionally from coherent-volume metric variation.
```

Too strong:

```text
MTS derives the activation shape.
```

Replacement:

```text
the activation shape follows conditionally if memory exposure is an additive hazard.
```

## 6. Status Stack

| Layer | Status | Meaning |
|---|---|---|
| empirical | promising locked closure | `B_mem=2/27` is frozen and testable |
| background EFT | stronger than before | shape and pressure have conditional mechanics |
| perturbations | effective subhorizon only | growth proxy has high-sound-speed support |
| local GR | open | local silence conditions are not PPN proof |
| parent field theory | not achieved | no full action owner yet |

Boxing-score version:

```text
The branch has footwork and scoring shots.
It is not champion of the division.
The belt is still parent action + local GR + perturbations.
```

## 7. Promotion Requirements

To promote from:

```text
empirical EFT closure
```

to:

```text
derived MTS field-theory branch
```

the branch must prove:

| Requirement | Must Prove |
|---|---|
| domain owner | `D` selected by parent action, not smoothing choice |
| load tensor owner | `Q^i_j` exists before FLRW and reduces to `X delta^i_j` |
| `u3` owner | `u3=1/4` follows from a real 3+1 cell theorem |
| amplitude owner | `B_mem=2/27` from normalized boundary charge |
| stress owner | full `T_M^munu` or `E_M^munu` is conserved and locally silent |
| perturbation owner | `mu`, slip, sound speed, sources for growth/CMB/lensing |

Until these are done:

```text
do not promote.
```

## 8. Allowed Tests

| Arena | Allowed Claim | Forbidden Claim |
|---|---|---|
| SN+BAO | competitive empirical closure branch | parent amplitude prediction |
| BAO+H(z) | late-time radial draw/preference test | CMB compatibility solved |
| growth `fσ8` | late-time subhorizon effective stress check | derived perturbation theory |
| CMB distance | compressed background stress diagnostic | passes CMB |
| local PPN | local silence target | derived local GR |

This is the test discipline going forward.

## 9. Gates

| Gate | Status | Evidence |
|---|---|---|
| source paths exist | pass | source register checked |
| branch equations consolidated | pass | conditional routes separated from empirical closure |
| amplitude overclaim blocked | pass | `B_mem=2/27` marked empirical closure |
| test claims limited | pass | allowed-test matrix written |
| parent field theory complete | fail | key owners incomplete |
| next work defined | pass | promotion requirements listed |

## 10. Decision

Decision:

```text
locked_memory_branch_consolidated_as_empirical_EFT_closure
```

Meaning:

```text
conditional mechanics retained;
amplitude and parent field theory not promoted.
```

This is a healthy position.

It is not as flashy as:

```text
we derived everything.
```

But it is much more serious.

## 11. Next Options

There are now two legitimate next moves.

### Theory Promotion Route

Attack one promotion gate:

```text
domain/load tensor owner:
derive D and Q^i_j from parent motion-time-space variables.
```

This is the right route if the goal is to keep pushing toward field theory.

### Empirical Holdout Route

Run frozen-branch tests:

```text
keep B_mem=2/27 fixed;
do not refit amplitude;
compare against LCDM/wCDM/CPL with the same jackknife/split discipline.
```

This is the right route if the goal is to build empirical credibility while the parent action remains incomplete.

Recommended next:

```text
theory route once more: domain/load tensor owner.
```

Reason:

```text
the branch has enough empirical support to deserve one more parent-action push,
but the amplitude has now been honestly fenced off.
```

