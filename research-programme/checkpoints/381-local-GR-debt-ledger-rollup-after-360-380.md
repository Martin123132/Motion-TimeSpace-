# 381 - Local-GR Debt Ledger Rollup After 360-380

Private local-GR/theory-discipline checkpoint. This is not a public WEP, PPN, Einstein-Hilbert, fifth-force, boundary no-hair, bulk-field, cosmology, or unified-field claim.

## 1. Purpose

Checkpoints 360-380 did not derive local GR.

They did something still valuable:

```text
they turned the local-GR problem from fog into a finite debt ledger.
```

This checkpoint rolls up the current state after:

```text
matter coupling,
projected metric / lifted-C,
class selection,
common-mode phi_C,
WEP,
preferred-frame,
EH exterior,
fifth-force,
source normalization,
boundary no-hair,
bulk X.
```

The answer is:

```text
the route is coherent,
but not promoted.
```

The local branch is not dead.

But:

```text
WEP/coframe,
EH exterior,
source normalization,
boundary no-hair,
fifth-force range/coupling,
and bulk-X no-hair
```

are still open.

So the honest headline is:

```text
MTS has a serious local-GR reduction programme,
not a derived local-GR theorem.
```

That is the judges' scorecard right now.

No knockout.

No collapse.

The fight is still live, but we are not allowed to raise our hand yet.

## 2. Machine Artifact

Script:

```text
scripts/local_GR_debt_ledger_rollup_after_360_380.py
```

Run:

```text
runs/20260602-005500-local-GR-debt-ledger-rollup-after-360-380
```

Outputs:

```text
results/source_register.csv
results/checkpoint_status_rollup.csv
results/local_GR_debt_ledger.csv
results/local_observable_rows.csv
results/claim_policy.csv
results/next_actions.csv
results/gate_results.csv
results/decision.csv
status.json
DONE.txt
```

Status:

```text
local_GR_debt_ledger_after_360_380_written_route_coherent_but_WEP_EH_boundary_bulk_source_gates_open_no_promotion
```

Claim ceiling:

```text
local_GR_debt_ledger_only_no_WEP_PPN_EH_fifth_force_boundary_bulk_or_local_GR_pass
```

Source paths missing:

```text
0
```

## 3. Compressed Status

The 360-380 sequence says:

| Sector | Best current result | Debt still open |
|---|---|---|
| universal matter coupling | direct WEP/clock vertices conditionally vanish | parent selector missing |
| projected metric / lifted-C | closure route labelled; lifted-C route conditionally sharpened | physical class/domain selector missing |
| common-mode `phi_C` | coefficient map and conditional zero theorem written | zero theorem premises not parent-derived |
| WEP/coframe | one-coframe kill switch exact if assumed | one coframe/common `F` is closure |
| source locks | local rows have source-lock/budget structure | coefficients not parent-derived |
| EH exterior | residual modified-gravity ledger written | EH not parent-derived |
| preferred-frame | coefficients mapped; vector no-hair route written | vector/domain no-hair not parent-derived |
| fifth-force | force-law contract written | `alpha_Y(lambda_Y)` not derived |
| source normalization | `M_eff` conservation imported | `G_eff`, measured `GM`, `Gdot` still open |
| boundary | class-only action would kill angular/vector hair | class-only/radial/flux proof missing |
| bulk `X` | mass-gap/Yukawa contract written | `m_X`, `q_X`, source charge, `alpha_X` missing |

This is the real situation.

The branch has become much better technically.

It has not become claimable as local GR.

## 4. Main Local-GR Debt Ledger

| Gate | Current status | Residual if open |
|---|---|---|
| single observed coframe / universal matter coupling | not parent-derived | WEP, clock, nonmetric light/ruler, species charge |
| projected metric or lifted-C parent selector | closure or conditional only | common-mode `phi_C`, clock/gamma/fifth-force leakage |
| topological class selection | not parent-derived | local class residuals and closure-only silence |
| common-mode `phi_C` zero/bound | conditional or budget-only | clock, gamma, WEP, fifth-force |
| source-locked local bound manifest | test-ready budget-only | no pass until coefficients exist |
| EH exterior operator | central blocker | modified-gravity operator families |
| preferred-frame/vector no-hair | conditional or budget-only | `alpha1`, `alpha2`, `alpha3`, `xi` |
| fifth-force range/coupling | unscored parameterized | `delta_G_or_fifth_force_yukawa` |
| source normalization / `GM` absorption | conditional or active residual | `delta_G`, `Gdot/G`, beta, fifth force |
| boundary no-hair | conditional coefficients retained | `eps_B_TF`, `eps_B0i`, `eps_B_rad`, `eps_B_flux` |
| bulk `X` no-hair / force law | conditional residual retained | `epsilon_bulk`, gamma/beta bulk, fifth-force |

This is the part to keep in our heads:

```text
local GR is blocked mostly by action-level selection,
not by one missing numerical fit.
```

The missing object is not:

```text
one better parameter.
```

It is:

```text
a parent action that makes the right local branch inevitable.
```

## 5. Observable Rows

The local rows are now separated as:

| Observable | Source lock | Current status |
|---|---:|---|
| `gamma_minus_1` | `2.3e-5` | budget-only, coefficients missing |
| `beta_minus_1` | `7.8e-5` | budget-only, coefficients missing |
| `eta_WEP` | `2.8e-15` | hardest ready active gate |
| `alpha_clock_redshift` | `3.1e-5` | budget-only, coefficients missing |
| `alpha1` | `1.0e-4` | budget-only after source lock |
| `alpha2` | `2.0e-9` | budget-only after source lock |
| `alpha3` | `4.0e-20` contingent | contingent budget-only |
| `xi` | `4.0e-9` | budget-only after source lock |
| `Gdot/G` | `9.6e-15 yr^-1` contingent | contingent budget-only |
| fifth-force | `alpha_Y(lambda_Y)` / `alpha_X(lambda_X)` | parameterized and unscored |

This means empirical work can continue.

But it must be phrased as:

```text
stress-testing retained residuals and closure branches.
```

Not:

```text
MTS passes PPN.
```

Not yet.

## 6. What Improved

Before this local sequence, the theory risk was:

```text
local GR by hope.
```

After checkpoints 360-380, the route is:

```text
one coframe / universal coupling
  + lifted-C or class selector
  + common-mode silence
  + Ward/source normalization
  + EH exterior operator
  + preferred-frame no-hair
  + boundary no-hair
  + bulk-X no-hair or scored force law
  -> local GR candidate.
```

That is real progress.

Each piece now has a named fate:

```text
derived zero,
conditional zero,
closure zero,
budgeted residual,
or unscored force law.
```

That is a much more serious framework than:

```text
assume local effects vanish.
```

## 7. What Still Fails

The hard failures are:

```text
one coframe is not parent-derived,
EH exterior is not parent-derived,
boundary no-hair is not parent-derived,
bulk-X no-hair is not parent-derived,
source normalization is not parent-derived,
fifth-force range/coupling is not parent-derived.
```

The worst single row is still:

```text
eta_WEP.
```

Because:

```text
eta_WEP ~ 2.8e-15
```

is brutal.

If one observed coframe/common `F` is not structurally derived, this branch becomes very expensive.

The deepest theory blocker is:

```text
EH exterior operator selection.
```

Because even if WEP and boundary rows are quiet, local GR still needs the surviving metric operator to be:

```text
Einstein-Hilbert plus Lambda,
not a hidden scalar/vector/higher-curvature/nonlocal branch.
```

The most foggy empirical row remains:

```text
fifth-force.
```

Because it cannot be scored until the theory gives:

```text
alpha_Y(lambda_Y)
```

or:

```text
alpha_X(lambda_X).
```

## 8. Claim Policy

Allowed:

```text
MTS has a coherent local-GR reduction programme.
```

Allowed:

```text
local empirical stress tests can proceed as budget-only diagnostics.
```

Allowed:

```text
conditional kill-switches exist for WEP, phi_C, boundary angular/vector hair, and bulk X.
```

Not allowed:

```text
MTS derives local GR.
```

Not allowed:

```text
MTS passes WEP/PPN/fifth-force/preferred-frame tests.
```

Not allowed:

```text
local branch is dead.
```

Why not dead?

Because no contradiction has been derived.

Why not solved?

Because the parent action has not supplied the selectors and no-hair operators.

That is the clean middle.

## 9. Gate Results

| Gate | Status | Evidence |
|---|---|---|
| source paths exist | pass | all cited source paths exist |
| checkpoint rollup written | pass | 21 checkpoints rolled up from 360 through 380 |
| local-GR debt ledger written | pass | 11 local-GR gates classified |
| observable rows separated | pass | 10 local rows mapped to source locks and active sources |
| derived local-GR claim allowed | fail | WEP/EH/boundary/bulk/source/fifth-force gates open |
| local branch dead claim allowed | fail | conditional theorem routes remain coherent |
| empirical pass claim allowed | fail | coefficients and range laws are missing |
| budget-only testing allowed | pass | retained residuals can be stress-tested |
| claim ceiling enforced | pass | no WEP/PPN/EH/fifth-force/boundary/bulk/local-GR pass |

## 10. Decision

Decision:

```text
local_GR_debt_ledger_after_360_380_written_route_coherent_but_WEP_EH_boundary_bulk_source_gates_open_no_promotion
```

Meaning:

```text
the local-GR route is coherent,
finite,
and much better disciplined than before.
```

But:

```text
it remains a route,
not a theorem.
```

The project should now stop treating the missing pieces as isolated local puzzles.

They are coupled.

The next thing to write is:

```text
a minimal parent local action contract
```

that says exactly what one future parent action must satisfy to close:

```text
coframe,
class,
EH,
source,
boundary,
bulk-X,
and local observable residuals
```

in one variation ledger.

## 11. Next Target

Next:

```text
382 - Parent Local Action Minimal Contract
```

Aim:

```text
write the exact local parent-action contract that would close the post-360 debts.
```

Pass condition:

```text
the contract lists action terms,
variation identities,
no-hair conditions,
source-normalization requirements,
and residual fallback rows.
```

Why this next:

```text
we have enough separated checkpoints now.
```

The work needs a spine:

```text
one parent action,
one variation ledger,
one local-GR sufficiency stack,
and explicit fallback residuals if any premise fails.
```
