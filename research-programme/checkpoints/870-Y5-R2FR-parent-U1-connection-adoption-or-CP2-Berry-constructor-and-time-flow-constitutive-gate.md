# 4854 Y5 R2FR parent U1 baseline, CP2 rank theorem and time-flow constitutive gate

Marker: `U1_BASELINE_CP2_CONSTITUTIVE_GATE_4854`.

**Status:** The architecture fork is closed rather than recycled. The competitive correspondence framework adopts an independent principal-`U(1)` connection as explicit field content. This is not a claim that the current real scalar creates photons. The two-pair/`CP^2` Berry route remains a mathematically viable optional parent completion, but it is not the baseline because the current corpus owns neither the required internal symplectic bundle nor equivalence between composite-field variation and Maxwell variation. For one isotropic unit time flow, the complete parity-even quadratic two-derivative photon block has one common normalization and one physical differential coefficient. That coefficient is now mapped to a real GW170817/GRB 170817A propagation bound.

**Decision:** `INDEPENDENT_U1_CORRESPONDENCE_FIELD_ADOPTED_CP2_OPTIONAL_UV_COMPLETION_ISOTROPIC_TIME_FLOW_CONSTITUTIVE_COEFFICIENT_DERIVED_AND_SOURCE_BOUNDED_PRIVATE_NONCLAIM`.

## 1. The rank theorem that separates a clue from a derivation

Let the proposed pre-electromagnetic curvature be the pullback of a parent target-space two-form,

\[
H_Q=\Phi^*\omega_Q.
\]

Pullback commutes with the wedge product, so

\[
H_Q\wedge H_Q=\Phi^*(\omega_Q\wedge\omega_Q).
\]

If the target of `Phi` has real dimension below four, every target-space four-form vanishes. Therefore

\[
\boxed{\dim N<4\quad\Longrightarrow\quad H_Q\wedge H_Q=0.}
\]

This makes several older intuitions precise:

- the real scalar `psi` has a one-dimensional target and cannot own a nonzero target two-form;
- a single Clebsch pair produces a decomposable curvature and has `H_Q wedge H_Q=0`;
- a unit timelike flow takes values in a three-dimensional hyperboloid, so its vorticity may describe simple/rank-two sectors but cannot alone own a generic rank-four Maxwell curvature;
- combining the flow with one or more scalars can reach four target dimensions, but dimension is only necessary. The current action does not provide a closed integral symplectic form, a principal-`U(1)` bundle, transition functions, a charge generator or a selector that turns those variables into the observed connection.

Choosing that missing geometry after seeing `F_obs` would be a parameterization, not a derivation.

## 2. What `CP^2` genuinely supplies

For a normalized complex triplet `z` modulo phase,

\[
z^\dagger z=1,
\qquad z\sim e^{i\chi}z,
\qquad [z]\in CP^2,
\]

the local Berry representative

\[
B_Q=-i z^\dagger dz
\]

transforms as `B_Q -> B_Q+dchi`, while

\[
H_Q=dB_Q=-i\,dz^\dagger\wedge dz
\]

is chart invariant. `CP^2` has real dimension four and a nondegenerate Fubini-Study two-form, so `H_Q wedge H_Q` need not vanish. Darboux equivalence makes this the minimal complex-projective target capable of representing a generic nondegenerate local closed two-form.

That result is constructive, but it is not yet Maxwell dynamics. Replacing an independent `A` by `B_Q(z)` restricts allowed variations. A genuine emergence theorem must additionally prove that the tangent variations generated by `z` reproduce the independent connection Euler equation, the Gauss constraint and exactly two propagating photon modes without extra sigma-model modes. The present corpus has no such parent action or variational-surjectivity theorem.

Therefore `CP^2` is retained as an optional ultraviolet/internal completion, not used as an unearned classical baseline.

## 3. Decisive field-content baseline

The correspondence framework now takes

\[
A\in\Omega^1(M,\mathfrak u(1)),
\qquad F=dA,
\]

as an independent field, alongside the coframe/connection, MTS scalar/flow variables and matter. The baseline remains

\[
S_{\rm corr}=S_{\rm EH/EC}+S_{\rm MTS}+S_A+S_{\rm matter}+S_{\rm boundary}.
\]

This is a multi-field unified theory: unity means a single covariant action, one observed metric/coframe, one Hilbert source, and exact Ward exchange between sectors. It does not require every spin representation to be a disguised scalar.

The statement closed here is field content, not primitive emergence:

\[
\boxed{E_{U1,\rm architecture}=0\ \text{by explicit adoption},
\qquad E_{U1,\rm primitive\ origin}\ \text{open}.}
\]

The distinction prevents the optional deeper origin question from blocking a valid classical Einstein-Maxwell correspondence theory.

## 4. Complete isotropic unit-flow photon operator basis

Assume locality, gauge invariance, parity evenness, at most two derivatives, quadratic order in `F`, and one normalized timelike flow `u`. Up to the parity-odd topological density, the independent isotropic scalars are

\[
F_{\mu\nu}F^{\mu\nu},
\qquad
u^\mu u^\nu F_{\mu\alpha}F_\nu{}^\alpha.
\]

Hence

\[
\boxed{
\mathcal L_A=-\frac{Z_A}{4}F_{\mu\nu}F^{\mu\nu}
+\frac{\eta_u}{2}u^\mu u^\nu F_{\mu\alpha}F_\nu{}^\alpha
+\frac{\vartheta_A}{4}F_{\mu\nu}\widetilde F^{\mu\nu}
-g_JA_\mu j^\mu .
}
\]

In the rest frame of `u`, with the local observed metric signature `(-,+,+,+)`, this is

\[
\mathcal L_A
=\frac12\left(\lambda_E E^2-\lambda_B B^2\right)
+\vartheta_A E\cdot B-g_JA\cdot j,
\]

where

\[
\boxed{\lambda_E=Z_A+\eta_u,\qquad \lambda_B=Z_A.}
\]

The constant `vartheta_A` term is a boundary/topological term. A varying pseudoscalar coefficient remains a separate open vertex.

## 5. Stability, equations and photon cone

Define

\[
\mathbf D=\lambda_E\mathbf E,
\qquad
\mathbf H=\lambda_B\mathbf B.
\]

The local equations are

\[
\nabla\cdot\mathbf D=g_J\rho,
\qquad
\nabla\times\mathbf H-\partial_t\mathbf D=g_J\mathbf j,
\]

together with the Bianchi equations. The Hamiltonian density is

\[
\rho_A=\frac12\left(\lambda_E E^2+\lambda_B B^2\right).
\]

Therefore the no-ghost/hyperbolicity gate is

\[
\boxed{\lambda_E>0,\qquad\lambda_B>0.}
\]

For a source-free plane wave,

\[
\boxed{
r_\gamma^2:=\frac{c_\gamma^2}{c_{\rm obs}^2}
=\frac{\lambda_B}{\lambda_E}
=\frac{Z_A}{Z_A+\eta_u}.
}
\]

The older two-parameter notation is a redundant reparameterization,

\[
\lambda_E=\lambda_A(1+\chi_E),
\qquad
\lambda_B=\lambda_A(1+\chi_B),
\]

with the exact observable identity

\[
\boxed{
\frac{\chi_B-\chi_E}{1+\chi_E}=r_\gamma^2-1.
}
\]

A common `chi_E=chi_B` only changes normalization. One isotropic `u` does not provide a polarization-dependent spatial tensor, so this block changes the common photon speed but does not split the two transverse polarizations. Birefringence requires an additional anisotropic/pseudotensor structure or a varying parity-odd coefficient.

## 6. Source-backed propagation bound

The LIGO/Virgo/Fermi/INTEGRAL GW170817-GRB 170817A analysis reports

\[
-3\times10^{-15}
\leq \frac{v_g-v_\gamma}{c}
\leq 7\times10^{-16}.
\]

Source: [Gravitational Waves and Gamma-rays from a Binary Neutron Star Merger](https://arxiv.org/abs/1710.05834).

On the local EH correspondence branch `v_g=c_obs`. Reversing the sign gives

\[
-7\times10^{-16}\lesssim r_\gamma-1\lesssim3\times10^{-15}.
\]

Using the exact squared-speed map rather than linearizing,

\[
\boxed{
-1.39999999999999951\times10^{-15}
\lesssim
\frac{\chi_B-\chi_E}{1+\chi_E}
\lesssim
6.000000000000009\times10^{-15}.
}
\]

In the single-flow basis define `kappa_u=eta_u/Z_A`. Since

\[
\kappa_u=r_\gamma^{-2}-1,
\]

the corresponding interval is approximately

\[
\boxed{
-5.999999999999973\times10^{-15}
\lesssim\kappa_u\lesssim
1.40000000000000147\times10^{-15}.
}
\]

This is a conditional MTS coefficient bound, not an absolute theory pass. It assumes the local EH branch sets `c_g=c_obs`, the coefficients are isotropic and effectively frequency independent over the propagation test, and no second operator cancels the speed shift. General gauge-invariant Lorentz-violating photon operators and their dispersion/birefringence signatures are classified independently by [Kostelecky and Mewes](https://arxiv.org/abs/0905.0031).

## 7. Static coupling, speed and impedance are not three free knobs

Gauss law and the same matter-current vertex give

\[
\boxed{\alpha_{\rm static}\propto\frac{g_J^2}{\lambda_E}.}
\]

The wave impedance in a fixed field convention is

\[
Z_{\rm wave}\propto\frac1{\sqrt{\lambda_E\lambda_B}}.
\]

Under `A'=sA`,

\[
\lambda_E'=\lambda_E/s^2,
\qquad
\lambda_B'=\lambda_B/s^2,
\qquad
g_J'=g_J/s.
\]

Thus the invariant matter-referenced impedance is

\[
\boxed{
g_J^2 Z_{\rm wave}
\propto\frac{g_J^2}{\sqrt{\lambda_E\lambda_B}}
=\frac{\alpha_{\rm static}}{r_\gamma}.
}
\]

Consequently one static electromagnetic calibration fixes `g_J^2/lambda_E`, while the propagation bound fixes `lambda_B/lambda_E`. Impedance is then a consistency relation, not an additional arbitrary coupling.

## 8. What is now closed and what is not

Closed inside the private classical correspondence branch:

```text
independent U1 field content selected;
scalar and u-alone generic-curvature routes rejected by rank;
CP2 retained as optional, not required, completion;
complete isotropic u/F quadratic operator basis;
positive-energy/hyperbolicity gate;
common photon speed and no isotropic-u birefringence theorem;
static-alpha / propagation / impedance invariant map;
real source-backed bound on eta_u/Z_A.
```

Still open:

```text
primitive MTS origin of the U1 bundle;
CP2 parent action and variational equivalence if emergence is pursued;
anisotropic or dispersive constitutive tensors away from the exact local branch;
dynamic f(X)F2 and varying theta_A vertices;
charge quantization and QED;
nonlinear Einstein-Maxwell ADM/Komar/Tolman source charge and charged PPN vector.
```

The next step no longer reopens the scalar-versus-connection fork. It feeds the owned Maxwell Hilbert stress and the finite constitutive interval into local nonlinear source charge and post-Newtonian observables.

## Next target

`4855-Y5-R2FR-Einstein-Maxwell-Komar-Tolman-source-charge-and-charged-exterior-PPN-gate.md`

**4860 correction:** after the tensor owner is active, the relative GW/photon result constrains `epsilon_cone=beta_u+p=(1-p)(2 delta_c+delta_c^2)`. The interval quoted here for `beta_u` is the `p=0` specialization. The explicit shared-characteristic candidate derived at 4860 has `beta_u=-p` and `epsilon_cone=0`.
