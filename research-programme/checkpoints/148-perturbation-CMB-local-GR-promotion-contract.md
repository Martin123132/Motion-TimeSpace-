# 148 - Perturbation, CMB, and Local-GR Promotion Contract

Private promotion checkpoint. This is not a public claim.

## 1. Trigger

The late-time empirical branch has now had enough honest pressure that the next useful move is not just another narrow dataset round.

The current situation is:

```text
MTS is on the cards in late-time expansion and growth-style holdouts.
```

But the branch is not promoted to a serious field-theory claim until it earns the harder reductions:

```text
derived perturbations,
Boltzmann-level CMB interface,
local GR / PPN silence,
domain selector,
and the B_mem=2/27 amplitude.
```

This checkpoint writes that contract explicitly so we do not accidentally treat a strong empirical scorecard as a completed unified theory.

## 2. Machine Artifact

Script:

```text
research-programme\scripts\promotion_contract_auditor.py
```

Run:

```text
research-programme\runs\20260531-234500-promotion-contract-audit
```

Generated:

```text
source_register.csv
empirical_readiness_scorecard.csv
promotion_requirement_matrix.csv
claim_ladder.csv
next_work_queue.csv
boxing_scorecard.csv
gate_results.csv
decision.csv
status.json
```

Status:

```text
late_time_empirical_branch_ready_for_theory_promotion_work_not_promoted
```

Claim ceiling:

```text
promotion_contract_private_audit_no_unified_theory_or_CMB_or_local_GR_claim
```

## 3. Empirical Standing

The fair readout is better than "random numerology" and much less than "done".

| Arena | Card | Readout |
|---|---:|---|
| SN+BAO full covariance | `Delta BIC = -5.259466877748764` vs fitted LCDM | locked branch preferred |
| BAO-only release holdout | DR2 `Delta BIC = -2.108368627321081`; DR1 `Delta BIC = -0.8483738675035859` | preferred/draw |
| BAO+H(z), no CMB | 10/10 production branches draw | survives no-CMB expansion pressure |
| Fresh CC H(z) | primary `Delta BIC = +0.33256956910347313` vs LCDM | competitive draw |
| SDSS/eBOSS Gaussian growth covariance | primary all rows `Delta chi2 = -2.30357028036`; jackknife failures `0` | preferred/draw under GR-proxy growth |
| SDSS/eBOSS ELG grid | ELG `Delta chi2 = +0.03750180074447895`; diagnostic Gaussian+ELG sum `-2.2660684796112336` | ELG draw; diagnostic combined card preferred |
| CMB bridge | compressed-distance draw, transfer/joint bridge unresolved | not promoted |

This is the Mayweather version rather than the Tyson version: not every round is a knockout, but the branch is slipping, countering, and staying on the cards against fitted baselines.

## 4. Promotion Contract

The machine audit marks these as the hard blockers:

| Gate | Current result | Meaning |
|---|---|---|
| parent perturbation action | fail missing derivation | no completed `delta_U_mem` or equivalent perturbation owner |
| conservation / Bianchi identity | partial contract | `Q^nu` must be derived or eliminated, not inserted |
| growth functions | fail missing derivation | `F_fric(a,k)`, `mu(a,k)`, slip/Sigma, and `S_mem(a,k)` are not yet derived |
| CMB interface | fail missing derivation | compressed priors are stress tests only until a Boltzmann-level branch exists |
| local GR / PPN silence | fail missing derivation | `q_loc^nu -> 0`, `G_eff/G -> 1`, and `Phi=Psi` remain theorem targets |
| domain selector `D` | fail missing derivation | zero-knob selector not yet derived |
| amplitude `B_mem=2/27` | blocked for promotion | frozen empirical closure, not yet a parent prediction |

The important discipline is that the empirical branch is alive, but the field-theory promotion is blocked until those rows move.

## 5. Allowed Claims

Allowed now:

```text
The frozen 2/27 late-time branch is empirically alive and worth serious derivation work.
```

Allowed with warning:

```text
Under a GR-proxy growth treatment, source-locked growth covariance and ELG scoring do not disfavor the branch.
```

Forbidden now:

```text
MTS passes CMB.
MTS derives local GR.
MTS derives B_mem=2/27.
MTS has a completed perturbation theory.
MTS is already a completed unified field theory.
```

## 6. Boxing Readout

The short honest version:

```text
Late-time rounds: MTS is drawing to winning.
Growth holdouts: MTS is drawing to winning, but with a GR-proxy growth equation.
CMB round: not won.
Local-GR round: not won.
Parent-action/amplitude round: promising but blocked.
```

So the theory is not grim in the empirical sense. It is grim only in the good engineering sense: the remaining problems are exact, structural, and cannot be hand-waved.

## 7. Decision

The next target should be:

```text
149-smooth-memory-or-controlled-growth-theorem.md
```

Question:

```text
Can the memory sector be smooth enough to justify the GR-proxy growth success,
or must it generate controlled modified growth?
```

Deliverable:

```text
derive delta_U_mem = 0 / high-smoothness theorem,
or derive explicit F_fric, mu, slip/Sigma, and S_mem terms with GR/local limits.
```

If that fails, the late-time branch stays an empirical closure. If it works, the branch becomes much more than a curve fit: it starts becoming a genuine field-theory candidate.
