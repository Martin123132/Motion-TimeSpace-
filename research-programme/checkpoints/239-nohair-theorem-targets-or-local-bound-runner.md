# 239 - Nohair Theorem Targets or Local-Bound Runner

Private local-bound preflight. This is not a public Cassini, clock, WEP, PPN,
or local-GR claim.

## 1. Trigger

Checkpoint 238 left two honest choices:

```text
attack no-hair targets N1-N6,
or run a closure-flagged local-bound preflight.
```

This checkpoint runs the preflight.

Reason:

```text
the no-hair targets should be prioritized by pressure, not vibes.
```

## 2. Machine Artifact

Script:

```text
scripts/nohair_theorem_targets_or_local_bound_runner.py
```

Run:

```text
runs/20260601-000056-nohair-theorem-targets-or-local-bound-runner
```

Command:

```text
python scripts/nohair_theorem_targets_or_local_bound_runner.py --timestamp 20260601-000056
```

Status:

```text
closure_flagged_local_bound_preflight_identifies_WEP_universal_coupling_as_hardest_no_pass_claim
```

Claim ceiling:

```text
closure_flagged_local_bound_pressure_no_official_pass_or_PPN_promotion
```

## 3. What Was Tested

Inputs:

```text
checkpoint 226 epsilon_J coefficient budgets,
checkpoint 227 official local-bound manifest,
checkpoint 238 no-hair targets N1-N6.
```

The runner converts the current closure form:

```text
residual_i = c_i epsilon_J
```

into coefficient-pressure rows:

```text
max |c_i| before bound scale = bound_i / epsilon_J.
```

This is not an official likelihood.

It is a pressure test on the closure coefficients.

## 4. Main Result

The hardest channel is not beta.

It is:

```text
epsilon_matter / WEP / composition coupling.
```

The MICROSCOPE-scale bound makes a direct memory-sector composition coupling
nearly impossible:

```text
using the tight earth_GPS_shell closure budget,
|c_matter| < 1.263e-10,
not merely order unity.
```

So the top theorem target is:

```text
N3_universal_coupling.
```

Meaning:

```text
matter and clocks must couple only to the metric/coframe,
with no direct memory-sector composition vertex.
```

This is exactly the kind of result we needed from the runner.

## 5. Pressure Ranking

The new priority order is:

| rank | target | reason |
|---:|---|---|
| 1 | `N3_universal_coupling` | WEP/composition coupling is brutally constrained |
| 2 | `N2_no_TF` | gamma/slip are order-unity tight |
| 3 | `N1_Meff` | source normalization must remain monopole-only |
| 4 | `N6/N4` | beta/q_loc no-hair remains theoretically decisive |
| 5 | `N3_clock_part` | clock coupling is also order-unity tight |
| 6 | `N5_projector_stress` | conservation failure can leak anywhere |

This does not make beta unimportant.

It says:

```text
the first empirical knife at the throat is direct matter coupling.
```

## 6. Scenario Readout

The runner evaluates:

```text
safe_branch_contract,
unit_unknown_coefficient,
direct_matter_leak_1e-6,
stress_leakage_coefficient_10.
```

Readout:

```text
safe branch can be conditionally quiet,
unit coefficients are not automatically safe for every channel,
even a 1e-6 direct matter leak is too large for the WEP channel,
direct matter coupling is crushed unless parent-forbidden.
```

So the correct interpretation is:

```text
the local route can stay alive only if N3 is a theorem,
not a calibration choice.
```

## 7. What This Does Not Claim

This checkpoint does not claim:

```text
MTS passes MICROSCOPE,
MTS passes Cassini,
MTS passes clock redshift tests,
MTS passes PPN,
MTS derives local GR.
```

It says:

```text
if the current closure coefficients were allowed to float,
WEP/composition coupling would be the first disaster.
```

That is useful pressure.

## 8. Gate Results

| gate | result |
|---|---|
| all cited local sources exist | pass |
| official-bound manifest converted to numeric pressure rows | pass |
| closure scenarios evaluated | pass |
| hardest no-hair target identified | pass |
| official local-bound pass claimed | fail |
| local GR or PPN promoted | fail |

The fail rows are intentional.

## 9. Decision

Decision:

```text
closure_flagged_local_bound_preflight_identifies_WEP_universal_coupling_as_hardest_no_pass_claim
```

Meaning:

```text
the next no-hair target is empirically prioritized rather than guessed.
The strongest pressure is direct matter/composition coupling, so
N3_universal_coupling becomes the next best derivation target.
```

Main gain:

```text
we now know which local branch theorem to attack first.
```

Main failure:

```text
all coefficients remain closure-level until N-targets are parent-derived.
```

## 10. Next Target

Create:

```text
240-universal-coupling-parent-contract-or-local-bound-data-runner.md
```

Purpose:

```text
derive the parent universal-coupling contract for matter/clocks,
or prepare a stricter official local-bound runner that remains closure-flagged.
```

Pass condition:

```text
matter and clocks couple only to g_mu_nu/coframe,
not directly to X, J_rel, V_def, P_mem, or memory-sector variables.
```

Fail condition:

```text
WEP safety is assumed because the model needs it.
```
