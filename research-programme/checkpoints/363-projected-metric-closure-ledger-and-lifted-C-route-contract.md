# 363 - Projected Metric Closure Ledger And Lifted C Route Contract

Private derivation checkpoint. This is not a public WEP, clock, PPN, local-GR, cosmology, or unified-field claim.

## 1. Purpose

The priority is now explicit:

```text
connect MTS to GR,
and make as much of the route derived as possible.
```

Checkpoint 362 blocks one tempting shortcut.

The scalar `Cperp` route did not derive relative exactness.

Therefore:

```text
ghat_mn = exp(P_D C) g_mn
```

is not parent-derived in the current scalar C-sector.

It is an explicit effective closure unless a harder lifted `C` theorem is supplied.

That is not fatal.

It is a clean map of the next derivation target.

## 2. Machine Artifact

Script:

```text
scripts/projected_metric_closure_ledger_and_lifted_C_route_contract.py
```

Run:

```text
runs/20260601-201000-projected-metric-closure-ledger-and-lifted-C-route-contract
```

Outputs:

```text
results/source_register.csv
results/gr_reduction_spine.csv
results/closure_ledger.csv
results/lifted_C_contract.csv
results/no_smuggling_rules.csv
results/theorem_queue.csv
results/gate_results.csv
results/decision.csv
status.json
DONE.txt
```

Status:

```text
projected_metric_closure_ledger_written_lifted_C_derivation_contract_set_GR_route_kept_open
```

Claim ceiling:

```text
contract_only_no_lifted_C_theorem_no_local_GR_WEP_clock_or_PPN_promotion
```

Source paths missing:

```text
0
```

## 3. Where The GR Route Actually Stands

The serious GR-reduction target is not:

```text
can we write something metric-looking?
```

It is:

```text
can the parent theory force the local observed sector to become Einstein-Hilbert gravity
with universal matter coupling and controlled residuals?
```

Current spine:

| Layer | Target | Current status | Main blocker |
|---|---|---|---|
| exterior operator | local exterior equations reduce to metric-only 4D second-order EH form | not derived | Ward closure is necessary but not sufficient |
| matter coupling | all species see one observed coframe/metric | conditional contract | selector depends on `Cperp` exactness or closure |
| projected metric selector | matter invariance selects `exp(P_D C)g` | closure in scalar C-sector | scalar `Cperp` exactness rejected |
| local tests | PPN/WEP/clock/fifth-force residuals vanish or are bounded | runner ready, no pass | coefficients and selector theorem not derived |
| Newtonian limit | Poisson plus geodesic acceleration for observed metric | target, not claim | needs operator and matter-metric gates |

This is the honest state:

```text
the local-GR branch is alive,
but not promoted.
```

## 4. Closure Ledger

| Object | Current label | Allowed role | Forbidden role | Upgrade condition |
|---|---|---|---|---|
| `ghat_mn = exp(P_D C)g_mn` | explicit effective closure | testable branch and theorem target | parent-derived matter metric or local-GR proof | derive lifted `C` representative invariance and local `Cperp` nullness |
| scalar `Cperp` | physical risk or unproved gauge | residual variable or demoted closure branch | silent gauge degree in scalar sector | show it is representative data, not fundamental scalar data |
| `P_D` projector | conditional parent target | varied domain/projector with stress accounting | external fixed selector inserted after variation | derive from parent domain/action variables |
| universal matter coupling | conditional contract | single observed coframe branch | WEP pass without selector theorem | prove all matter depends only on parent-owned observed coframe |
| local GR branch | open, not promoted | derivation target with bounds | claim recovered because closure branch looks metric | EH operator, universal coupling, residual-vector gates all pass |

This prevents a quiet overclaim.

The closure branch can still be useful and testable.

But it cannot be sold as the GR derivation.

## 5. Lifted C Route Contract

The next derivation route is:

```text
do not treat C as an ordinary local scalar.
lift C into a form / connection / holonomy / boundary-class sector.
```

Required theorem contract:

| Requirement | Exact contract | Why it matters for GR |
|---|---|---|
| replace scalar with class representative | `C` is a representative of a connection/form/holonomy/boundary/relative class | local representative noise can quotient away |
| derive local kernel nullness | compact-domain `Cperp` lies in gauge/null/exact kernel with no local boundary primitive | matter cannot see local nonmetric mismatch |
| preserve global memory | `P_D C` or class observable survives on FLRW domains | local GR silence does not erase cosmology |
| vary projector/domain data | projector, domain, and boundary variables are varied before Ward identities are read | conservation is real, not bookkeeping |
| recover observed metric theorem | representative-shift invariance forces matter to depend on `exp(P_D C)g` | closure becomes derived metric |
| connect to EH exterior | after quotienting local representative data, exterior metric operator is EH plus controlled residuals | this is the actual GR reduction target |

This is the bridge we need.

If this theorem fails, the projected metric remains a disciplined closure branch.

If it passes, the local-GR route becomes much more serious.

## 6. No-Smuggling Rules

| Rule | Required practice |
|---|---|
| closure labels visible | every use of `exp(P_D C)g` says closure unless lifted `C` theorem is supplied |
| GR reduction before GR claim | local GR means EH operator plus universal matter metric plus residual vector controlled |
| derive or bound every residual | WEP, clock, `gamma`, `beta`, and fifth-force terms are theorem-zero or runner-bounded |
| compare against baselines | empirical stress tests compare MTS against appropriate GR/LCDM/DM/MOND baselines where meaningful |
| local and cosmic same parent | one parent object must explain local silence and cosmic memory activation |

That last rule is the big one.

The route only becomes a unified field-theory route if:

```text
the same structure gives local GR silence and nonzero cosmological memory.
```

## 7. Decision

Decision:

```text
projected_metric_closure_ledger_written_lifted_C_derivation_contract_set_GR_route_kept_open
```

Meaning:

```text
the scalar projected-metric route stays demoted to closure,
but the lifted C route is now the next formal derivation target.
```

No promotion:

```text
lifted C theorem not proved,
projected metric not parent-derived,
WEP/clock/PPN not passed,
local GR not derived.
```

## 8. Next Target

Next:

```text
364 - Lifted C Sector Form Holonomy Theorem Attempt
```

Aim:

```text
attempt the derivation directly:
local Cperp must become exact/null representative data,
while global P_D C survives as cosmological memory.
```

This is the correct next fight.

Not a patch.

Not a numerical rescue.

A derivation attempt.
