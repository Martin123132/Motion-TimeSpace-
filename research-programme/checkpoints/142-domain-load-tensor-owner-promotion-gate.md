# 142 - Domain / Load Tensor Owner Promotion Gate

Private theory-promotion checkpoint. This is not a public claim.

## 1. Trigger

Checkpoint 141 consolidated the locked memory branch as:

```text
empirical EFT closure with conditional theory mechanics.
```

It named the next theory promotion gate:

```text
derive D and Q^i_j from parent motion-time-space variables.
```

This checkpoint asks:

```text
Is Q^i_j still the main independent blocker, or is the real blocker the
coherent domain D?
```

Short answer:

```text
Q is mostly reduced to coherent-volume load if D and u3 are owned.
D is now the main blocker.
```

## 2. Machine Artifact

Script:

```text
research-programme\scripts\domain_load_tensor_owner_promotion_gate.py
```

Run:

```text
research-programme\runs\20260531-211500-domain-load-tensor-owner-promotion-gate
```

Generated:

```text
source_register.csv
domain_selector_candidates.csv
load_tensor_owner_chain.csv
branch_limit_tests.csv
phase_field_action_contract.csv
input_promotion_requirements.csv
gate_results.csv
decision.csv
status.json
```

Status:

```text
domain_load_owner_sharpened_Q_reduced_D_not_derived
```

Claim ceiling:

```text
domain_load_owner_contract_sharpened_not_parent_derived
```

## 3. Load Tensor Chain

The retained chain is:

```text
Theta^i_j = h^{i alpha} h_{j beta} nabla_alpha u^beta
```

then:

```text
P_coh[Theta]^i_j = (1/3)<theta>_D delta^i_j
```

and:

```text
N_D = (1/3) ln(V_D0/V_D).
```

If `D` and `u3` are owned, then:

```text
Q_coh^i_j = (N_D/u3) delta^i_j.
```

Therefore:

```text
I_M = det(Q_coh) = (N_D/u3)^3.
```

This is the important narrowing:

```text
Q no longer needs to be an arbitrary tensor invented for FLRW.
```

It can be the isotropic coherent-volume load.

But:

```text
that only works if D and u3 are truly parent-owned.
```

## 4. Domain Selector Candidates

| Candidate | Verdict |
|---|---|
| external smoothing domain | rejected rescue knob |
| pointwise no-domain rule | rejected local-hair risk |
| maximal coherent volume-flow domain | best contract, not parent action |
| variational phase-field domain | best parent-action route if coefficients fixed |
| relative boundary current domain | best topological support, not full selector |

The best current rule remains:

```text
D = maximal coherent volume-flow domain.
```

Meaning:

```text
largest connected domain with stable sign of <theta>_D
and small normalized expansion variance.
```

That is not random smoothing.

But it is still not a parent Euler equation.

## 5. Branch Limit Tests

| Branch | Status | Meaning |
|---|---|---|
| FLRW | pass conditional | gives `Q=(N/u3)delta` |
| Minkowski local | pass conditional | stable volume gives `Q=0` |
| stationary solar system | pass conditional | local memory off if bound volume is stable |
| tracefree shear / GW | pass conditional | trace channel off at leading order |
| virialized galaxy | open conditional | bound/unbound split may import GR/Newtonian assumptions |
| collapse / merger | open risk | coherent local volume change can activate memory |

So the route is useful but not safe enough for promotion.

Stationary silence is plausible.

Dynamic local safety is not solved.

## 6. Variational Domain Route

The best parent-action route is a phase-field or auxiliary selector:

```text
chi_D
```

with terms like:

```text
chi_D(1-chi_D)
```

to enforce a sharp auxiliary domain,

```text
chi_D (theta-<theta>_D)^2 / (<theta>_D^2 + epsilon)
```

to select coherent volume flow,

and either:

```text
|nabla chi_D|^2
```

or:

```text
d_rel J_rel = 0
```

to control the boundary.

But this is only acceptable if:

```text
the coefficients are fixed by the parent theory,
not tuned to make local tests go away.
```

Otherwise it is just the old smoothing knob wearing a better coat.

## 7. Gates

| Gate | Status | Evidence |
|---|---|---|
| `Q` defined before FLRW | pass conditional | `Theta^i_j` and coherent trace exist before symmetry |
| `Q` independent blocker reduced | pass partial | `Q=(N_D/u3)delta` if `N_D,u3` owned |
| domain parent selector | fail | `D` is still contract, not Euler equation |
| `u3` parent owner | fail | `1/4` remains conditional cell route |
| local stationary silence | pass conditional | stable volume gives `N_D=0`, `Q=0` |
| dynamic local safety | open risk | collapse/merger/boundary motion unresolved |
| promotion to parent field theory | fail | domain, `u3`, boundary current, stress action open |

## 8. Decision

Decision:

```text
domain_load_owner_sharpened_Q_reduced_D_not_derived
```

Meaning:

```text
Q can be owned by coherent-volume load if D and u3 are derived;
D remains the main blocker.
```

This is a useful result because it stops the branch from chasing a vague `Q`
problem.

The exact target is now:

```text
derive the domain selector.
```

## 9. Current Standing

The branch is now:

```text
better than arbitrary closure,
not yet parent field theory.
```

Boxing-score version:

```text
We found that the jab is probably coming from the legs, not the glove.
Q is the glove.
D is the footwork.
No footwork theorem, no title shot.
```

## 10. Next Target

The next precise target is:

```text
143-domain-selector-variational-action-attempt.md
```

Purpose:

```text
try to derive or reject a zero-knob variational selector for D.
```

Pass condition:

```text
the selector gives FLRW coherent domain, local stationary silence,
and avoids wall stress without fitted smoothing parameters.
```

Fail condition:

```text
D remains an empirical/coarse-graining closure,
and the memory branch cannot be promoted beyond EFT closure.
```

