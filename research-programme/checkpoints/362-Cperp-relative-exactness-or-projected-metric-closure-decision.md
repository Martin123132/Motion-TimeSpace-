# 362 - Cperp Relative Exactness Or Projected Metric Closure Decision

Private closure/derivation checkpoint. This is not a public WEP, clock, PPN, local-GR, cosmology, or unified-field claim.

## 1. Purpose

Checkpoint 361 made the projected matter metric route mathematically clean:

```text
if Cperp is residual gauge / exact representative data,
then matter invariance selects exp(P_D C)g.
```

This checkpoint asks the hard follow-up:

```text
is current scalar Cperp actually relative-exact / gauge?
```

Answer:

```text
no, not from the current scalar C-sector.
```

So the projected matter metric:

```text
ghat_munu = exp(P_D C) g_munu
```

is now demoted to:

```text
explicit effective closure for the current scalar C-sector.
```

This is not a dead-end declaration.

It means the current scalar route failed, while a future lifted `C` route remains open.

## 2. Machine Artifact

Script:

```text
scripts/Cperp_relative_exactness_or_projected_metric_closure_decision.py
```

Run:

```text
runs/20260601-194500-Cperp-relative-exactness-or-projected-metric-closure-decision
```

Key outputs:

```text
results/source_register.csv
results/prior_361_decision_update.csv
results/exactness_transfer_tests.csv
results/closure_policy.csv
results/lifted_C_route_contract.csv
results/residual_runner_update.csv
results/decision_matrix.csv
results/next_queue.csv
results/gate_results.csv
results/decision.csv
status.json
DONE.txt
```

Status:

```text
Cperp_scalar_exactness_rejected_projected_metric_demoted_to_explicit_closure_lifted_C_route_open
```

Claim ceiling:

```text
closure_decision_only_no_projected_metric_derivation_no_WEP_clock_PPN_or_local_GR_pass
```

Source paths missing:

```text
0
```

## 3. Exactness Transfer Tests

| Test | Result | Reason |
|---|---|---|
| `Jrel` flux cohomology transfer | fail | `J_rel` is flux/current-like; `Cperp` is currently scalar/0-form |
| relative `H^0` triviality | fail | kills only boundary-zero closed scalar classes, not arbitrary local profiles |
| `dCperp` exact one-form | fail | exact gradients can still carry energy, force, or boundary charge |
| first-class shift constraint | conditional fail | self-algebra works, but Hamiltonian preservation needs `delta H/delta Cperp≈0` |
| quotient no-`Cperp` skeleton | conditional pass but assumes target | works only if quotient principle is already assumed |
| presymplectic null direction | conditional fail | requires `C`-sector exactness and vanishing local boundary primitive |

So the current answer is decisive:

```text
scalar Cperp exactness is not derived.
```

The previous `J_rel` cohomology success does not transfer automatically.

## 4. Closure Policy

| Object | New status | Allowed use | Forbidden use |
|---|---|---|---|
| projected matter metric `exp(P_D C)g` | explicit effective closure | disciplined local-silence/common-mode branch | parent-derived matter coupling, WEP pass, local-GR proof |
| `Cperp` shift | not parent-derived gauge | future theorem target | claim `Cperp` is gauge in scalar sector |
| universal matter coupling | conditional contract with closure metric | direct vertices absent if closure imposed | WEP/clock pass without selector theorem |
| local GR branch | still unpromoted | conditional theorem stack and guardrails | use projected metric closure as GR proof |

This is the right kind of demotion:

```text
not thrown away,
not overclaimed,
explicitly labelled.
```

## 5. Why This Is Not A Disaster

This does hurt the strongest local derivation route.

But it also cleans the project.

Before:

```text
exp(P_D C)g was a possible theorem target.
```

Now:

```text
exp(P_D C)g is closure in the scalar C-sector,
unless C is lifted to a form / connection / holonomy / boundary-class object.
```

That means future work cannot accidentally sell the projected metric as parent-derived.

It can still be used as:

```text
an effective closure branch to test,
and a theorem target for a harder lifted C-sector.
```

## 6. Lifted C Route Contract

The future derivation route is now explicit.

| Contract | Required statement | Pass condition |
|---|---|---|
| lift `C` from scalar to class variable | `C` is a representative of a connection/holonomy/boundary/relative class | local `Cperp` is representative data while `P_D C` is observable |
| derive relative-exact local residuals | local `Cperp` modes are `d_rel`-exact or constraint-trivial | kernel shifts are gauge/null |
| preserve FLRW memory class | cosmological `P_D C` survives while local `Cperp` quotients away | same `P_D` owns local and global limits |
| own boundary primitive | local primitive/charge vanishes or is pure gauge | no hidden boundary hair |
| retain Ward accounting | selector/projector/domain stresses are varied | no fake conservation |
| recover projected metric as theorem | matter invariance selects `exp(P_D C)g` | closure becomes derivation |

This is where the route can be rescued.

But it is a new theorem route, not something already proved.

## 7. Residual Runner Update

The local runner must treat the demotion as follows.

| Residual | Policy after demotion |
|---|---|
| `eta_WEP` | direct vertices are conditionally absent only inside the closure branch; no pass claim |
| `alpha_clock_redshift` | common-mode `P_D C` drift/gradient remains open |
| `gamma_minus_1` | nonmetric `Cperp` mismatch removed only by closure; other gamma sources remain |
| `delta_G_or_fifth_force` | physical scalar `Cperp` would be fifth-force risk; remains quarantined |

So:

```text
WEP/clock/nonmetric residuals remain explicit unless the lifted C theorem is supplied.
```

## 8. Decision Matrix

| Claim | Status | Evidence |
|---|---|---|
| current scalar `Cperp` is relative-exact/gauge | rejected | cohomology transfer tests fail; Hamiltonian/presymplectic conditions not derived |
| projected metric is parent-derived now | rejected | depends on scalar `Cperp` exactness that failed |
| projected metric is explicit closure in current scalar C-sector | pass | `exp(P_D C)g` can be used only with closure labels |
| projected route is dead forever | not supported | lifted form/holonomy/class route remains possible |
| local GR/PPN follows | rejected | closure demotion blocks promotion |

This is the cleanest possible outcome from a failed derivation:

```text
fail the overclaim,
keep the useful branch,
define the harder theorem needed to upgrade it.
```

## 9. Gate Results

| Gate | Status | Evidence |
|---|---|---|
| source paths exist | pass | all cited source files exist |
| `Cperp` exactness tests run | pass | six exactness tests recorded |
| scalar `Cperp` exactness derived | fail | scalar `Cperp` does not inherit `J_rel` exactness |
| projected metric demoted to closure | pass | `exp(P_D C)g` labelled explicit effective closure |
| future lifted `C` route kept | pass | form/connection/holonomy/class contract written |
| WEP/clock runner updated | pass | residuals remain explicit unless lifted theorem supplied |
| projected metric parent promotion | fail | selector depends on failed scalar exactness |
| local GR promoted | fail | closure demotion and unresolved gates block claim |
| claim ceiling enforced | pass | no projected metric derivation, WEP/clock/PPN/local-GR claim |

## 10. Decision

Decision:

```text
Cperp_scalar_exactness_rejected_projected_metric_demoted_to_explicit_closure_lifted_C_route_open
```

Meaning:

```text
The current scalar C-sector cannot derive Cperp relative exactness.
Therefore exp(P_D C)g is an explicit effective closure in this branch.
```

But:

```text
a harder lifted C-sector route remains open.
```

No promotion:

```text
projected metric not parent-derived,
WEP/clock not passed,
local GR not derived.
```

## 11. Next Target

Next:

```text
363 - Projected Metric Closure Ledger And Lifted C Route Contract
```

Purpose:

```text
make the closure label impossible to miss,
then specify exactly what a lifted C-sector theorem must deliver.
```

Pass condition:

```text
all later local-GR/coupling statements mark exp(P_D C)g as closure
unless a lifted C theorem supplies relative exactness.
```

This is the honest way forward:

```text
keep testing the closure branch,
keep deriving the lifted route,
do not confuse the two.
```
