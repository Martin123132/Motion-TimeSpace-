# 346 - GR And Derivation North-Star Spine

Private project-orientation checkpoint. This is not a public local-GR, PPN, CMB, cosmology, or unified-field claim.

## Purpose

This checkpoint pins the actual programme priority.

The project is not mainly:

```text
fit cosmology data better than LCDM.
```

The project is:

```text
build a parent field-theoretic framework that reduces to GR in the correct limit
and derives as much of the MTS structure as possible instead of fitting it.
```

The empirical scorecards matter because they tell us whether a branch is worth deriving.

But the central victory condition is still:

```text
MTS -> GR locally,
MTS -> controlled FLRW memory/cosmology globally,
MTS -> perturbations/CMB/growth consistently,
with closure constants progressively replaced by theorems.
```

## Existing Spine Files

The project already has several partial spine documents, but not one single master spine.

| File | Role | Current use |
|---|---|---|
| `141-consolidated-locked-memory-branch-contract.md` | consolidated locked memory branch | best pre-343 branch contract |
| `177-parent-action-perturbation-local-GR-contract.md` | parent action / perturbation / local-GR contract | cleanest requirements file for promotion |
| `250-local-GR-gate-scorecard-and-test-readiness.md` | local GR gate status | says local branch is coherent but blocked |
| `261-Hstar-H0-scale-lock-and-local-silence-attempt.md` | `H_star=H0` derivation attempt | clean identity found, theorem failed |
| `343-dim27-rank2-origin-closure-decision-gate.md` | amplitude derivation decision | freezes `B_mem=2/27` as explicit closure |
| `344-amplitude-closure-claim-ledger-update.md` | claim ledger | prevents amplitude overclaim |
| `345-post343-locked-branch-robustness-manifest.md` | latest empirical manifest | locked branch remains live, no promotion |

This file becomes the current high-level spine and gap map.

## Honest Overall Status

The current position is:

```text
MTS has a live, disciplined late-time closure branch.
MTS does not yet have a completed parent field theory.
MTS does not yet derive local GR/PPN.
MTS does not yet derive CMB/perturbations.
MTS does not yet derive B_mem=2/27.
```

That sounds harsh, but it is actually a good place to be compared with a vague speculative model:

```text
the empirical branch is not dead,
the missing theorem list is explicit,
and the GR-reduction gate is now sharply identifiable.
```

## Current Working Spine

The strongest current skeleton is:

```text
parent motion/time/space structure
    -> metric/coframe and matter coupling
    -> memory/load sector
    -> local silence / GR exterior limit
    -> FLRW memory projection
    -> locked late-time branch
    -> perturbation/CMB/growth interface
```

The current cosmology-facing amplitude factorization is:

```text
B_mem = q_trace * epsilon_H * (H_star/H0)^2.
```

Post-343 status:

| Object | Status | Meaning |
|---|---|---|
| `q_trace=2/27` | conditional algebraic identity | exact if rank/dim assumptions are imposed |
| `epsilon_H=1` | closure / conditional theorem target | not yet an unconditional Hamiltonian normalization |
| `H_star=H0` | closure / boundary theorem target | clean identity exists, but parent selection missing |
| `B_mem=2/27` | explicit locked closure | empirically live, not parent-derived |
| `u3=1/4` | closure / cell-theorem target | needs parent cell derivation |
| local GR | open | local silence/EH exterior not yet proven |
| perturbations/CMB | open | growth is diagnostic/proxy-level only |

## GR Reduction Is The Main Gate

For this to become a serious fundamental theory, the GR limit has to work like:

```text
MTS parent action
    -> effective field equations
    -> local vacuum / screened matter limit
    -> Einstein equations plus controlled residuals
    -> Newtonian and PPN limits inside experimental bounds
```

The local-GR gate is not optional.

It must show at least:

| Requirement | Pass condition |
|---|---|
| unique physical metric/coframe | matter, clocks, rulers, lensing, and local PPN see the same geometry |
| Einstein-Hilbert exterior | local exterior action reduces to GR, not a different metric theory |
| conserved stress | Bianchi identity and matter conservation are respected |
| local silence | memory/load sector does not generate unacceptable fifth force, clock drift, or WEP violation |
| PPN residual vector | `gamma-1`, `beta-1`, preferred-frame terms, clock terms are derived or bounded |
| Newtonian limit | Poisson/Newton recovery appears as the weak-field limit |

Current status from the local scorecard:

```text
local branch coherent but blocked.
local-bound proxy tests are allowed as diagnostics.
local GR/PPN is not yet derived.
```

That means the next derivation route should be GR-first, not cosmology-first.

## Closure-To-Theorem Conversion List

The real work is turning closures into theorems.

| Current closure/debt | Needed theorem |
|---|---|
| `B_mem=2/27` | parent action derives amplitude, not just rank/dim algebra |
| `q_trace=2/27` | parent action derives active rank 2 and cell dimension 27 |
| `epsilon_H=1` | Hamiltonian/stress normalization fixed by variation or Ward identity |
| `H_star=H0` | boundary/observer equation selects present Hubble scale without fitting |
| `u3=1/4` | 3+1/cell projection theorem |
| local silence | memory/load stress vanishes or is screened in local systems |
| perturbation owner | derived `mu(a,k)`, slip, sound speed, and source terms |
| CMB bridge | Boltzmann-level interface, not compressed-distance survival only |

This is the difference between:

```text
interesting empirical closure model
```

and:

```text
serious field-theoretic framework.
```

## Reordered Priority

The previous checkpoint suggested `H_star/H0` because it is a clean remaining amplitude factor.

That is still important, but the broader project priority should be:

```text
1. local GR / PPN reduction,
2. parent action ownership,
3. closure-to-theorem conversions,
4. perturbation/CMB bridge,
5. empirical robustness.
```

Reason:

```text
if MTS cannot reduce to GR locally, it becomes another modified-gravity closure model,
not a fundamental theory candidate.
```

So the next derivation target should be:

```text
347 - Local GR Parent-Reduction Theorem Attempt
```

with this exact pass/fail rule:

```text
derive the local Einstein-Hilbert exterior and PPN residual vector from the parent action,
or isolate the exact missing axiom/closure that blocks the derivation.
```

## Exact Contract For The Next GR Attempt

A future local-GR theorem attempt must not handwave "GR limit".

It must write:

```text
S_parent[g/e, MTS fields, matter]
```

and then show:

```text
delta S_parent / delta g^{mu nu}
    -> G_{mu nu} = 8 pi G T_{mu nu} + E^{MTS}_{mu nu}.
```

Then it must prove, in the local screened/exterior regime:

```text
E^{MTS}_{mu nu} -> 0
```

or at least:

```text
E^{MTS}_{mu nu} = small residual with explicit PPN vector bounds.
```

Required outputs:

| Output | Required form |
|---|---|
| field equations | explicit metric/coframe variation |
| conservation identity | `nabla_mu E^{mu nu}_{MTS}=0` or controlled exchange |
| local silence condition | derived projector/no-hair/screening equation |
| Newton limit | Poisson equation recovered |
| PPN vector | symbolic residuals before numbers |
| closure list | any assumption not derived is named and quarantined |

## Claim Policy Going Forward

Allowed:

```text
MTS has a live empirical closure branch and a serious derivation programme.
```

Allowed:

```text
The locked branch justifies further parent-action and local-GR derivation work.
```

Not allowed:

```text
MTS derives GR.
MTS passes PPN.
MTS passes CMB.
MTS derives B_mem=2/27.
MTS is a completed unified field theory.
```

Until the GR theorem passes, the phrase "unified field theory" should mean:

```text
programme target,
not achieved status.
```

## Bottom Line

The project is not in a grim place.

It is in a sharper place:

```text
the late-time branch has enough empirical oxygen to keep,
but the next serious move is not another scorecard.
```

The next serious move is:

```text
connect MTS to GR from the parent action.
```

If that succeeds, the programme changes category.

If it fails, the exact failure tells us whether MTS is:

```text
a closure-level modified gravity/cosmology model,
or a field theory still missing one named structural axiom.
```

That is the right fight.
