# 4994 - Strict-4D mixed bubble and evanescent basis pole

Checked: `2026-07-14`.

Marker: `MTS_4994_STRICT_4D_MIXED_BUBBLE_AND_EVANESCENT_POLE`.

## 1. Result

The mixed `h phi` discontinuity now fixes the strict-four-dimensional
`I2(u)` coefficient of the one-loop opposite-helicity `h h phi phi`
amplitude. This is an exact rank-four one-loop IBP calculation, not a
crossing fit.

In the convention

```text
M1=kappa^4 F/<1|3|2]^4,
s+t+u=0,
```

the four mixed-cut tensor families reduce to

```text
J_AC =  u^3(11t^2-9tu+6u^2)/(6t^3),
J_AD =  0,
J_BC =  0,
J_BD = -u^3(11t^2+15tu+6u^2)/(6t^3).
```

Therefore

```text
J_AC+J_AD+J_BC+J_BD = -4u^4/t^2,

C_u^(strict 4D)
 = (t^4/16) sum J
 = -t^2 u^4/4.
```

Its crossed image is

```text
C_t^(strict 4D)=-u^2 t^4/4.
```

This closes the strict-4D mixed-channel bubble. It does **not** yet close
the dimensionally regulated bubble sector or the full one-loop amplitude.

## 2. Cut coordinates

Checkpoint 4992 used the exact mixed-cut chart `z,w`. It maps to Forde's
two-particle-cut variables `y,tau` by

```text
w=(y-1)/tau,
z=-tau/y,
1+zw=1/y.
```

The entire helicity numerator then collapses to

```text
r=(1+z)/(1+zw)=y-tau.
```

The cut integrand is consequently a sum of four rank-four one-loop
families:

```text
G_u=(t^4/16)(V.l)^4
 [1/(D0 D1 A C)+1/(D0 D1 A D)
 +1/(D0 D1 B C)+1/(D0 D1 B D)],

V^2=0,
V.l=<l3>[4l].
```

This null numerator is what makes a closed IBP derivation possible.

## 3. Dimension-dependent IBP system

For shifts `r_i`, propagators `D_i=(l-r_i)^2`, distances
`S_ij=(r_i-r_j)^2`, and

```text
J_r(a)=integral (V.l)^r / product_i D_i^(a_i),
A=sum_i a_i,
```

the four total-derivative identities generated by `l-r_j` are

```text
0=(D+r-A) J_r(a)
  -r(V.r_j) J_(r-1)(a)
  -sum_i a_i J_r(a+e_i-e_j)
  +sum_i a_i S_ij J_r(a+e_i).
```

The null-vector identity is

```text
0=sum_i a_i[-J_(r+1)(a+e_i)+(V.r_i)J_r(a+e_i)].
```

No auxiliary propagator, fitted coefficient, or closure term is inserted.
The only apparent extra object, the rank-one unit box, is removed by a
direct Lorentz decomposition. Its scalar-box projection is

```text
(V.r)^T Gram(r)^(-1) (r_i^2/2),
```

and its scalar-bubble projection is exactly zero.

## 4. Independent leading-singularity check

At the exact rational point `t=2,u=3`, the same IBP reduction reproduces
all four 4992 scalar-box coefficients before the common `t^4/16`
prefactor:

```text
AC: 7857/32,
AD: 81/2,
BC: 81/2,
BD: 50625/32.
```

These equal the independently derived quadruple-cut values. The bubble
reconstruction used six exact rational kinematic points and passed two
held-out points. No floating-point fit enters the result.

## 5. Generic-D diagnostic

The calculation also exposes a necessary evanescent warning. On the exact
slice `t=1,u=2`, generic-dimensional reduction gives

```text
C_u(D)=
 -(27D^3+532D^2-6036D+8720)
 /[40(D-4)(D-2)(D-1)].
```

Four exact rational dimensions reconstruct this expression and three
additional dimensions validate it. Around `D=4`,

```text
C_u(D)=108/[5(D-4)]-959/60+O(D-4).
```

With `D=4-2epsilon`, the basis pole is

```text
-54/(5 epsilon).
```

By contrast, the strict-four-dimensional result on the same slice is

```text
C_u^(strict 4D)=-4.
```

The finite value `-959/60` is not equal to `-4`, and neither value may be
declared the physical rational completion by itself. The generic-D scalar
basis has become singular: its `1/(D-4)` term must cancel against the
evanescent shifts of the box and triangle coefficients before the finite
amplitude is taken.

This is a positive diagnosis. It identifies the exact missing operation
instead of hiding it in an arbitrary rational constant.

## 6. Remaining calculation

The next derivation is now sharply defined:

1. reduce the mixed `AC,AD,BC,BD` families to **all** scalar masters at
   generic `D`;
2. expand their box, triangle, and bubble coefficients together about
   `D=4-2epsilon`;
3. prove cancellation of every `1/(D-4)` basis pole;
4. retain the resulting finite rational mixed-channel remainder;
5. reduce the identical-scalar `s`-cut bubble;
6. combine it with the sourced `hh` component to obtain `C_s`;
7. only then assemble the complete one-loop hard kernel.

No complete-one-loop, outer-cut, local-GR, or full-MTS claim is promoted.

## 7. Reproducibility

Generator:

`scripts/Y5_R2FR_4994_strict_4d_mixed_bubble_and_evanescent_pole.py`

Independent validator:

`scripts/Y5_R2FR_4994_strict_4d_mixed_bubble_and_evanescent_pole_validation.py`

The generator closes `11/21` gates. The independent validator reconstructs
both rational functions afresh and passes `198/198` checks.
