# 312 - Empirical Pivot After Local Fence

Private empirical-planning checkpoint. This is not a public cosmology, CMB, local-GR, galaxy, or parent-field-theory claim.

## Purpose

Checkpoints 298-311 squeezed the local branch down to a narrow remaining theorem target:

```text
derive C_D and [K_boundary,A_D]=0 from the parent action.
```

That is useful, but it is now too narrow to keep burning cycles locally without new empirical pressure.

This checkpoint pivots back to:

```text
stricter holdout testing,
baseline parity,
and claim ceilings.
```

## Current Scorecard

| Arena | Readout | Claim Ceiling |
|---|---|---|
| local branch | fenced as conditional theorem target | no local-GR/PPN promotion |
| generic SN+BAO short smoke | fixed no-clock branch competitive/mixed | no stable evidence |
| no-SH0ES branch | pattern survives removing local-H0 pressure | diagnostic only |
| DESI DR1/DR2 split | survives qualitatively, LCDM BIC gap worsens in DR1 | release diagnostic only |
| CPL prior sensitivity | DR2 CPL cleanable; DR1 CPL still edge-hits | baseline fragility explicit |
| source-locked H(z) | competitive draw | late-time holdout only |
| growth covariance | preferred/draw under GR-proxy growth gate | no perturbation promotion |
| ELG grid likelihood | competitive draw | no official joint-likelihood claim |
| CMB bridge | unresolved/mixed | no CMB pass claim |

Boxing-card version:

```text
MTS is not knocking LCDM out.
But it is also not getting knocked out across the late-time cards.
The right next move is not louder theory language;
it is a cleaner judges' scorecard.
```

## Hard Claim Ceilings

Allowed:

```text
MTS is a serious late-time empirical closure candidate under current short-smoke/source-locked diagnostics.
```

Forbidden:

```text
MTS has stable cosmology evidence.
MTS passes CMB.
MTS derives local GR.
B_mem=2/27 is parent-predicted.
```

Reason:

```text
DR1 CPL remains edge-unstable,
the SN+BAO runs are not full official likelihoods,
growth remains proxy/effective,
CMB bridge remains unresolved,
and local GR remains a theorem target.
```

## Acceptance Gates

| Gate | Result | Meaning |
|---|---|---|
| source paths exist | pass | pivot ledger traceable |
| local branch parked not promoted | pass | empirical work will not assume local GR |
| CPL DR2 clean but DR1 unstable | pass | baseline fragility stays explicit |
| stable cosmology evidence allowed | fail | no evidence language yet |
| non-SN holdouts available | pass | H(z), growth covariance, and ELG grid exist |
| CMB bridge promoted | fail | no CMB pass claim |
| growth perturbation promoted | fail | no perturbation theory claim |
| official likelihood queue ready | pass | next empirical target is concrete |

## Next Holdout Queue

Priority 1:

```text
official/full-likelihood SN+BAO+growth wrapper.
```

Minimum rule:

```text
LCDM, wCDM, CPL, MTS fixed, and negative controls
must face the same rows, nuisance policy, covariances, splits, and jackknifes.
```

Priority 2:

```text
full-sample/full-cov no-SH0ES SN+BAO release split
with wide CPL boxes.
```

Reason:

```text
current SN+BAO evidence is useful but still short-smoke.
```

Priority 3:

```text
refresh source-locked H(z), growth covariance, and ELG grid only as needed.
```

Reason:

```text
those branches already exist and did not knock the theory out.
```

Priority 4:

```text
CMB/perturbation contract,
but not as a CMB score claim.
```

Reason:

```text
CMB is the unresolved title fight.
```

## Dry-Run Policy

No long empirical run should start blind.

The next run should first produce:

```text
schema report,
row counts,
covariance shape checks,
model matrix,
nuisance policy,
prior boxes,
edge policy,
negative controls.
```

Only then should scoring run.

For future VS Code/long-run workflow:

```text
runs/<timestamp>/status.json
runs/<timestamp>/results/*.csv
runs/<timestamp>/COMPLETE.txt
```

and no Codex token-wasting watch loop.

## Decision

Decision:

```text
empirical_pivot_after_local_fence_ready_official_likelihood_queue_no_stable_evidence_claim
```

Claim ceiling:

```text
empirical_holdout_planning_and_scorecard_only_local_branch_conditional_no_public_support_claim
```

Meaning:

```text
the local derivation route is fenced;
the empirical branch is alive but not claimable;
the next real upgrade is an official/full-likelihood-style holdout wrapper.
```

## Machine Artifacts

Script:

```text
scripts/empirical_pivot_after_local_fence.py
```

Run:

```text
runs/20260601-000135-empirical-pivot-after-local-fence
```

Output files:

```text
runs/20260601-000135-empirical-pivot-after-local-fence/results/source_register.csv
runs/20260601-000135-empirical-pivot-after-local-fence/results/empirical_scorecard.csv
runs/20260601-000135-empirical-pivot-after-local-fence/results/acceptance_gates.csv
runs/20260601-000135-empirical-pivot-after-local-fence/results/next_holdout_queue.csv
runs/20260601-000135-empirical-pivot-after-local-fence/results/command_queue.csv
runs/20260601-000135-empirical-pivot-after-local-fence/results/claim_ceilings.csv
runs/20260601-000135-empirical-pivot-after-local-fence/results/decision.csv
```
