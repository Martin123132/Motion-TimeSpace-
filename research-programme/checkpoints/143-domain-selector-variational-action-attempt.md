# 143 - Domain Selector Variational Action Attempt

Private theory-promotion checkpoint. This is not a public claim.

## 1. Trigger

Checkpoint 142 sharpened the domain/load-tensor problem:

```text
Q can be owned by coherent-volume load if D and u3 are derived.
D is the main blocker.
```

This checkpoint asks:

```text
Can D be selected by a zero-knob variational action?
```

Short answer:

```text
not yet.
```

The best route remains:

```text
auxiliary C_coh selector + relative boundary current.
```

But that is still a contract, not a parent derivation.

## 2. Machine Artifact

Script:

```text
research-programme\scripts\domain_selector_variational_action_attempt.py
```

Run:

```text
research-programme\runs\20260531-213000-domain-selector-variational-action-attempt
```

Generated:

```text
source_register.csv
action_candidate_ledger.csv
variation_chain.csv
zero_knob_tests.csv
branch_readout.csv
boundary_stress_ledger.csv
input_domain_selector_candidates.csv
gate_results.csv
decision.csv
status.json
```

Status:

```text
zero_knob_domain_selector_not_derived_auxiliary_contract_retained
```

Claim ceiling:

```text
domain_selector_formal_action_not_parent_derived
```

## 3. Candidate Actions

| Candidate | Verdict |
|---|---|
| dynamic phase-field selector | rejected for promotion due to scale and stress |
| algebraic `C_coh` multiplier | clean selector, not domain owner |
| coherence extremal domain | best domain contract, not local action |
| auxiliary domain multiplier | theorem target, not derived |
| relative current selector | conservation support, not selector |
| external threshold selector | rejected rescue knob |

The dynamic phase-field route fails promotion because it introduces:

```text
ell_chi
T_chi
```

which are exactly the kind of new scale/stress risks the local-GR route cannot afford.

The auxiliary route is cleaner:

```text
S_aux = integral sqrt(-g) lambda_chi(chi_D - C_coh[D]).
```

It avoids an independent propagating selector field.

But:

```text
it selects chi_D after D is already supplied.
```

It does not select the physical domain itself.

## 4. Variation Chain

The useful invariant remains:

```text
C_coh[D] =
<theta>_D^2 /
(<theta^2>_D + <sigma^2>_D + <omega^2>_D + eps_D).
```

The algebraic selector gives:

```text
delta_lambda S = 0
=> chi_D = C_coh[D].
```

But domain variation gives:

```text
delta_D C_coh[D] = bulk terms + boundary terms.
```

Those boundary terms are the problem.

The relative current route supplies a formal object:

```text
d_rel(j_3,b_2)=0.
```

But closure alone does not select:

```text
the local trivial representative;
the FLRW nontrivial representative;
the moving-boundary exchange current.
```

So the zero-knob selector is not derived.

## 5. Branch Readout

| Branch | Status | Risk |
|---|---|---|
| FLRW | pass conditional | homogeneous domain must be selected |
| Minkowski / stationary local | pass conditional | boundary stress not derived |
| tracefree shear | pass conditional | second-order/boundary terms open |
| virialized bound system | open conditional | may import Newton/GR binding ideas |
| collapse / merger | open risk | dynamic local activation possible |

This is enough for a serious conditional branch.

It is not enough for:

```text
derived local GR.
```

## 6. Zero-Knob Tests

| Test | Status |
|---|---|
| no empirical threshold | pass for `C_coh` contract |
| no transition length | dynamic phase-field fails; auxiliary passes |
| no selector stress | auxiliary passes conditionally; dynamic fails |
| domain not chosen by outcome | partial |
| boundary not hand tuned | fail open |
| local GR not imported | partial |

The important loss is:

```text
boundary not hand tuned = fail open.
```

That means the local-to-FLRW boundary is still the dangerous place.

## 7. Boundary Stress Ledger

| Source | Status | Needed Fix |
|---|---|---|
| dynamic phase-field wall | rejected for promotion | remove kinetic wall or derive suppression |
| algebraic multiplier boundary | open | derive Noether identity with `D` variation |
| relative current representative | open | derive local trivial / FLRW nontrivial representative |
| moving domain average | open | boundary exchange without wall stress |

This is the same villain wearing fewer masks:

```text
the boundary current / representative is not owned.
```

## 8. Gates

| Gate | Status | Evidence |
|---|---|---|
| source paths exist | pass | source register checked |
| dynamic phase-field promoted | fail | `ell_chi` and `T_chi` risks |
| auxiliary selector safe | pass conditional | no independent kinetic stress |
| domain selected by action | fail | no zero-knob Euler equation selects `D` |
| boundary exchange owned | fail open | `J_rel` representative not derived |
| FLRW/local split | pass conditional | `C_coh` separates ideal branches |
| dynamic local safety | open risk | collapse/mergers/boundaries unresolved |
| domain promotion | fail | `D` remains closure-level |

## 9. Decision

Decision:

```text
zero_knob_domain_selector_not_derived_auxiliary_contract_retained
```

Meaning:

```text
auxiliary selector avoids scalar stress,
but no parent action selects D and the boundary representative.
```

So:

```text
the memory branch remains empirical EFT closure with conditional mechanics.
```

## 10. Current Standing

The domain route did not collapse.

But it did not promote.

Boxing-score version:

```text
We ducked the bad punch: no dynamic chi_D wall field.
But we have not landed the selector theorem.
The boundary is still clinching us on the ropes.
```

## 11. Next Options

There are now two honest options.

### Theory Option

Try one more precise target:

```text
derive the J_rel representative / boundary exchange current.
```

Pass condition:

```text
local stationary class is trivial,
FLRW class is nontrivial,
and the transition carries no PPN-sized wall stress.
```

### Empirical Option

Pause the theory route and run:

```text
frozen B_mem=2/27 empirical holdout tests
```

under the checkpoint-141 claim limits.

Recommended next:

```text
empirical holdout route.
```

Reason:

```text
the theory route has now honestly fenced the domain blocker;
more derivation attempts risk circling the same boundary-current obstruction.
```

