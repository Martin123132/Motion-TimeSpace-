# 374 - Fifth-Force Preferred-Frame Source-Lock Manifest

Private local-bound source-lock checkpoint. This is not a public fifth-force, preferred-frame, PPN, WEP, clock, Einstein-Hilbert, local-GR, cosmology, or unified-field claim.

## 1. Purpose

Checkpoint 369 kept three local rows quarantined:

```text
preferred-frame alpha1/alpha2,
preferred-location / anisotropy xi,
delta_G / fifth force.
```

Checkpoint 373 then forced WEP honesty:

```text
one observed coframe remains closure until parent-selected.
```

This checkpoint asks:

```text
which quarantined local rows can now receive real source locks,
and which still must stay unscored?
```

Answer:

```text
preferred-frame alpha1/alpha2 and xi can now be source-locked as internal budget rows.
```

Also:

```text
alpha3 and Gdot/G should be retained as contingent guardrails.
```

But:

```text
fifth-force / delta_G cannot be reduced to one scalar score.
```

It is a range-dependent force-law problem:

```text
alpha_Y(lambda_Y)
```

and MTS has not yet derived:

```text
lambda_Y,
alpha_Y,
screening,
composition dependence,
or source normalization.
```

So the fifth-force row becomes:

```text
source-context locked,
parameterized,
not scalar-scored.
```

## 2. Machine Artifact

Script:

```text
scripts/fifth_force_preferred_frame_source_lock_manifest.py
```

Run:

```text
runs/20260601-234000-fifth-force-preferred-frame-source-lock-manifest
```

Outputs:

```text
results/source_register.csv
results/external_source_lock.csv
results/residual_source_lock_manifest.csv
results/fifth_force_parameterization.csv
results/MTS_source_map_requirements.csv
results/runner_update.csv
results/quarantine_policy.csv
results/gate_results.csv
results/decision.csv
results/next_queue.csv
status.json
DONE.txt
```

Status:

```text
preferred_frame_xi_source_locked_fifth_force_parameterized_not_scalar_scored_no_local_GR_pass
```

Claim ceiling:

```text
source_lock_manifest_only_no_fifth_force_preferred_frame_PPN_EH_WEP_or_local_GR_pass
```

Source paths missing:

```text
0
```

## 3. External Source Locks

The external source-lock manifest records:

| Source | Use |
|---|---|
| Will Living Reviews 2014, PPN Table 4 | `alpha1`, `alpha2`, `alpha3`, `xi` internal target scales |
| PDG 2025 gravity-tests review | current cross-check for EEP, local Lorentz invariance, WEP, `Gdot/G`, `gamma`, `beta`, and metric coupling |
| Lee et al. PRL 2020 | short-range inverse-square-law anchor down to tens of micrometers |
| Tan et al. PRL 2020 | submillimeter Yukawa anchor and `lambda_Y` range context |

These are not used to claim MTS passes.

They are used to prevent fake proxy scoring.

## 4. Preferred-Frame Rows Now Source-Locked

The previous quarantine was:

```text
no numeric source lock here.
```

After this checkpoint:

| Residual | Internal target | Source status | MTS status |
|---|---:|---|---|
| `alpha1` | `1.0e-4` local conservative row, `4.0e-5` stricter pulsar context retained | source-locked | coefficient missing |
| `alpha2` | `2.0e-9` | source-locked | coefficient missing |
| `alpha3` | `4.0e-20` | source-locked contingent row | coefficient/channel relevance missing |
| `xi` | `4.0e-9` | source-locked | coefficient missing |
| `Gdot/G` | `9.6e-15 yr^-1` one-sigma scale | source-locked contingent row | time-variation map missing |

Meaning:

```text
preferred-frame / xi are no longer quarantined for missing external bounds.
```

But:

```text
they are still not scored as pass/fail because MTS has no derived coefficients.
```

Budget row is not victory.

It is a loaded target.

## 5. MTS Source Map

The source channels now become precise:

| MTS channel | Observable rows | Theorem-zero route | Current status |
|---|---|---|---|
| `boundary_vector_B0i` | `alpha1`, `alpha2` | no physical marker fields; vector representative pure gauge/topological | coefficient missing |
| unowned selector stress/flux | `alpha3`, `Gdot/G`, secular drift | total Ward ledger owns selector stress and boundary flux cancels | Ward ownership open |
| trace-free boundary shear `B_TF` | `xi`, `gamma`, lensing slip | class-only boundary action has no angular representative source | coefficient missing |
| radial scalar hair / bulk `X` gradient | fifth-force, `beta`, perihelion | no independent radial scalar charge or universal monopole absorbed into measured `GM` | range/coupling missing |
| common-mode `phi_C` gradient | fifth-force, `gamma`, clock | local trivial class gives `grad(phi_C)=0` | conditional or budget-only |
| species-specific class response | WEP / composition fifth force | parent-selected one coframe/common `F(C_D)` | closure axiom required |

This is the important spine:

```text
no vector no-hair,
no preferred-frame pass.
```

```text
no range/coupling map,
no fifth-force score.
```

## 6. Fifth-Force Contract

A fifth-force score needs a force law, not a vibe.

The correct standard contract is:

```text
V(r) = -G m1 m2 / r * [1 + alpha_Y exp(-r/lambda_Y)].
```

So MTS must derive:

```text
alpha_Y(lambda_Y),
```

or an equivalent source-normalized acceleration residual:

```text
a_extra/a_GR.
```

The current `phi_C` gradient proxy:

```text
a_extra/a_GR ~ 0.5 |grad phi_C| / |grad U_GR|
```

is useful but not enough.

It still lacks:

```text
range,
source composition,
screening,
boundary/source normalization,
and whether the force is universal, WEP-violating, or absorbed into measured GM.
```

Therefore:

```text
delta_G_or_fifth_force_yukawa = parameterized_source_locked_but_not_scalar_scored.
```

That is not cowardice.

That is refusing to compare a shark to a bicycle.

## 7. Runner Update

| Runner row | After 369 | After 374 | Pass claim |
|---|---|---|---|
| `preferred_frame_alpha1` | quarantined | source-locked budget row | blocked by missing MTS coefficient |
| `preferred_frame_alpha2` | quarantined | source-locked budget row | blocked by missing MTS coefficient |
| `preferred_frame_alpha3` | not separated | contingent source-locked row | blocked by coefficient and channel relevance |
| `xi_preferred_location_anisotropy` | quarantined | source-locked budget row | blocked by missing MTS coefficient |
| `Gdot_over_G` | not separated | contingent source-locked row | blocked by missing time-variation map |
| `delta_G_or_fifth_force_yukawa` | quarantined | parameterized source-context locked | blocked by missing range/coupling map |

So the local residual deck is cleaner:

```text
ready numeric budgets:
gamma, beta, eta_WEP, clock, alpha1, alpha2, alpha3, xi, contingent Gdot/G.
```

```text
still not scalar scored:
range-dependent fifth force / delta_G.
```

## 8. Quarantine Policy After 374

| Sector | Policy |
|---|---|
| preferred-frame / `xi` | no longer missing external bounds; still budget-only until MTS coefficients exist |
| `alpha3` / `Gdot/G` | use only if MTS branch predicts nonconservation/preferred-frame or secular-G channel |
| fifth-force / Yukawa | parameterized quarantine until `lambda_Y` and `alpha_Y` are derived |
| composition-dependent fifth force | first controlled by `eta_WEP` unless a separate mediator/range law is derived |

This matters because a lazy local test could accidentally double-count:

```text
eta_WEP
```

and:

```text
composition-dependent fifth force.
```

The manifest says:

```text
do not double-count,
do not fake-score,
do not promote.
```

## 9. Gate Results

| Gate | Status | Evidence |
|---|---|---|
| source paths exist | pass | all cited source paths exist |
| external sources recorded | pass | four external source URLs recorded |
| preferred-frame numeric source-locked | pass | `alpha1`, `alpha2`, `alpha3`, `xi` source rows loaded |
| `Gdot/G` contingent guardrail written | pass | PDG 2025 LLR scale retained only if applicable |
| fifth-force parameterized | pass | Yukawa `alpha(lambda)` contract written |
| ready rows have missing coefficients visible | pass | no score because MTS coefficients are missing |
| fifth-force numeric scored | fail | range/coupling not derived |
| preferred-frame or PPN pass claimed | fail | guardrails only |
| local GR promoted | fail | WEP, `phi_C`, EH, vector no-hair, and fifth-force map remain incomplete |
| claim ceiling enforced | pass | no local-GR/PPN/fifth-force claim |

## 10. Decision

Decision:

```text
preferred_frame_xi_source_locked_fifth_force_parameterized_not_scalar_scored_no_local_GR_pass
```

Meaning:

```text
preferred-frame and xi are now real budget rows,
not vague quarantines.
```

But:

```text
MTS still owes vector no-hair or coefficient maps.
```

And:

```text
fifth-force remains unscored until MTS derives a range/coupling law.
```

No promotion:

```text
preferred-frame not passed,
fifth-force not passed,
PPN not passed,
EH not derived,
local GR not derived.
```

## 11. Next Target

Next:

```text
375 - EH Exterior Operator Or Residual Modified-Gravity Ledger
```

Aim:

```text
separate Einstein-Hilbert exterior derivation from residual modified-gravity operator testing.
```

Pass condition:

```text
EH operator is derived,
or residual operator coefficients are retained and source-bounded.
```

Why this next:

```text
the local runner now has cleaner WEP, clock, gamma, beta, preferred-frame, xi, and fifth-force policies.
```

So the next root-level issue is:

```text
does the exterior metric operator actually reduce to EH,
or are we carrying a modified-gravity residual operator?
```
