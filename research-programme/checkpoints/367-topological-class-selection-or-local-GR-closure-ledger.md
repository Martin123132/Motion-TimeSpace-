# 367 - Topological Class Selection Or Local GR Closure Ledger

Private derivation checkpoint. This is not a public WEP, clock, PPN, local-GR, cosmology, or unified-field claim.

## 1. Purpose

Checkpoint 366 gave the lifted `C` matter-selector theorem shape:

```text
if representative shifts are parent gauge/admissible symmetries,
matter must descend to C_D / P_D C.
```

But it left the biggest class-selection burden:

```text
why local Q_rel = 0,
and why FLRW Q_rel != 0?
```

This checkpoint tests the last clean route:

```text
topological / discrete relative class selection,
without smooth Pi(C),
without a fitted smoothing scale,
without hand-picking a quiet domain.
```

Result:

```text
topological machinery sharpens the target,
but does not parent-derive the physical class split.
```

So the local-GR branch is now explicitly demoted to:

```text
labelled closure plus residual testing.
```

That is not giving up.

That is stopping the theory from cheating.

## 2. Machine Artifact

Script:

```text
scripts/topological_class_selection_or_local_GR_closure_ledger.py
```

Run:

```text
runs/20260601-213000-topological-class-selection-or-local-GR-closure-ledger
```

Outputs:

```text
results/source_register.csv
results/class_selection_targets.csv
results/topological_route_tests.csv
results/closure_ledger.csv
results/no_go_reasoning.csv
results/residual_test_pivot.csv
results/gate_results.csv
results/decision.csv
results/next_queue.csv
status.json
DONE.txt
```

Status:

```text
topological_class_selection_not_parent_derived_local_GR_route_demoted_to_labelled_closure_and_residual_testing
```

Claim ceiling:

```text
closure_ledger_and_test_pivot_only_no_class_selection_WEP_PPN_EH_or_local_GR_promotion
```

Source paths missing:

```text
0
```

## 3. Class Selection Target

The target was:

```text
stationary local domains -> Q_rel = 0 or exact lifted-C class
FLRW coherent domains -> Q_rel != 0
```

with one parent rule doing both.

Why it matters:

```text
local silence and cosmological memory must come from the same structure.
```

Otherwise the framework becomes:

```text
local patch here,
cosmology patch there.
```

That is not the goal.

The desired route was:

```text
topology selects the relative class discretely,
instead of a smooth arbitrary selector Pi(C_coh).
```

## 4. Route Tests

| Route | Result | Why |
|---|---|---|
| smooth boundary polarization `Pi(C_coh)` | fail | endpoint/regularity constraints underselect infinitely many functions |
| relative-current admissibility | conditional pass, not selection | preserves class but does not choose the physical class |
| relative-index `k=9` | conditional pass, topology-sensitive | requires B3-like coherent cell |
| domain topology selection | fail | local differential equations do not generally select topology |
| boundary-current quantization | fail | periods do not choose level, endpoints, or arrow |
| IR boundary-bath selector | conditional pass, not parent-derived | local/FLRW boundary-state split is a contract |

The important bit:

```text
topology can label sectors,
but it does not automatically choose which sector the physical domain occupies.
```

That is the current stopping point.

## 5. What We Did Derive Or Sharpen

This is not a blank failure.

The branch has genuine structure:

| Piece | Status |
|---|---|
| fixed-`D` coherent projection `Q_coh` | derived for supplied physical `D` |
| lifted `C` boundary primitive | derived inside fixed relative class |
| representative-invariant matter action | conditional theorem |
| exponential class metric | natural from additive class plus multiplicative scale |
| relative index `k=9` | conditional for B3-like coherent cell |
| local/FLRW boundary-state selector | precise contract |

That is enough to make a serious closure branch.

It is not enough to claim:

```text
MTS derives local GR.
```

## 6. Closure Ledger

| Piece | Current label | Allowed use | Forbidden use |
|---|---|---|---|
| `P_coh[Q]` for fixed `D` | derived for supplied domain | build coherent class candidate | claim domain selection |
| boundary primitive silence | fixed-class admissibility | remove representative leakage inside selected class | claim local class is selected |
| representative-invariant matter action | conditional theorem | forbid direct representative vertices | claim WEP/clock pass |
| `exp(lambda C_D)g` | labelled closure/theorem target | test common-mode class metric | claim fully parent-derived metric |
| local `Q_rel=0`, FLRW `Q_rel!=0` | closure branch assignment | empirical/local residual testing | claim derived local GR |
| EH exterior operator | not derived | separate theorem or residual ledger | fake EH proof from matter selector |

This is the clean pivot:

```text
from trying to promote local GR,
to testing a labelled class-metric closure branch.
```

## 7. No-Go Reasoning

The no-go is narrow, not dramatic.

Rejected claims:

| Claim | Why rejected |
|---|---|
| topology alone selects the physical class | topology labels sectors but does not choose realization |
| relative closure selects representative | `d_rel J=0` preserves class but does not choose it |
| `k=9` is automatic | it depends on B3-like topology |
| matter selector implies local GR | EH operator and residual vector remain open |
| local route is dead | no; it is now a disciplined closure/test branch |

That last point matters.

This is not:

```text
the idea is dead.
```

It is:

```text
the derivation route has reached a real missing theorem.
```

Until that theorem exists, the honest move is testing.

## 8. Residual-Test Pivot

The next branch should be tested as:

```text
labelled class-metric closure,
not derived local GR.
```

Test queue:

| Test area | Observable | Rule |
|---|---|---|
| common-mode clock/redshift | local drift/gradient of `C_D/P_D C` | derive zero or source-bound it |
| PPN `gamma/beta/slip` | `gamma-1`, `beta-1`, `Phi-Psi` | compare to GR baseline and source bounds |
| WEP/species universality | `eta_WEP`, hidden `F_A(C_D)` | theorem-forbid or keep active |
| fifth-force/domain channels | scalar/boundary/bulk forces | quarantine unless zero/bounded |
| local/FLRW class split | `b_D`, `c_D`, `sigma_D` edge cases | check local silence does not erase cosmology |
| cosmology closure branch | SN/BAO/CMB/growth residuals | compare to `LCDM/wCDM/CPL` baselines |

This is the proper Mayweather route:

```text
do not need a knockout on every scorecard,
but every round must be judged against a baseline.
```

## 9. Decision

Decision:

```text
topological_class_selection_not_parent_derived_local_GR_route_demoted_to_labelled_closure_and_residual_testing
```

Meaning:

```text
topological and relative-current machinery sharpen class selection,
but do not derive local Q_rel=0 and FLRW Q_rel!=0 from the parent action.
```

Therefore:

```text
the local-GR route is not promoted.
```

The branch remains:

```text
a disciplined class-metric closure and theorem target,
now ready for residual-bound testing.
```

No promotion:

```text
physical class selection not derived,
WEP/clock/PPN not passed,
Einstein-Hilbert exterior not derived,
local GR not derived.
```

## 10. Next Target

Next:

```text
368 - Common-Mode Class-Metric Clock / PPN Residual Gate
```

Aim:

```text
start testing the labelled class-metric branch:
common-mode clock drift,
PPN residual vector,
fifth-force/domain channels,
and baseline comparison rules.
```

This is the right next move.

The derivation path is not abandoned.

But the local branch now has to survive as a scored, labelled closure until the missing class-selection theorem is found.
