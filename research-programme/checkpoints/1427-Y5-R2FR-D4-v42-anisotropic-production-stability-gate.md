# 5411: v42 anisotropic production-stability gate

## Decision

**PASS FOR CONTINUED V42 PRODUCTION ONLY.**

The anisotropic projective repair is not merely a historical-box patch. Two bounded same-revision resumes retain the existing certificates and advance the active connector without creating a new analytic failure class.

## Production evidence

- accepted boxes advance `67 -> 105`;
- the active stack contains `13` boxes at maximum depth `16`;
- exact `8 x 16` projective certificates increase `2 -> 14`;
- the formerly deepest pending path `LLDLDLDRDLDRDRDLU` is committed with denominator margin `0.0004176254172608071` and `|J| >= 20.715268296834868`;
- the geometric-denominator split counter remains frozen at `27`, and the failure-category set remains exactly the five previously declared outer-enclosure classes.

Accepted plus pending parameter area remains exactly the parent rectangle within outward floating tolerance. No accepted certificate uses a point sample, fitted tolerance, or closure coefficient.

## Claim boundary

This checkpoint establishes production stability of the v42 finite-cover proof on the active path only. The path is incomplete, so regular-away W3, event-local W3, combined W3, regulator-limit, UV, local-GR, and full-MTS claims remain open.
