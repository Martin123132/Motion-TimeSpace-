# MTS Status - 2026-07-27

## Executive Position

MTS remains a work-in-progress research programme, not a completed unified
theory. The strongest local result is unchanged: on one source-selected,
locally silent motion state, the nonlinear two-derivative restriction of the
common parent action is an exact classical consistent truncation to
GR + Lambda + Standard Model + Maxwell.

The new checkpoint range does not strengthen that claim. It advances the
separate attempt to calculate the first canonical MTS-specific crossed-`hhh`
coefficient, corrects the topology at every order-9 outer node, and restores
the locked outer-convergence gates. Those gates fail, so the coefficient
remains unresolved.

## Local GR / Newton / Maxwell Branch

On

```text
chi=0,
nabla chi=0,
rho_local=rho_0,
```

public checkpoint `1227` establishes the exact two-derivative selected
truncation. It yields:

- Einstein gravity with one Hilbert source;
- `G_N=1/(8 pi M_R^2)`, Poisson's equation, and inverse-square Newton motion;
- neutral and null geodesics and lensing;
- Maxwell equations, Lorentz force, Hilbert stress, and Poynting flux;
- the ten-parameter GR PPN vector.

The numerical value of `G_N` remains one calibration datum, as in GR. The
result is not an all-operator, all-state, compact-body, horizon, or
radiation/back-reaction theorem.

## Crossed-`hhh` Coefficient Work

Public checkpoints `1231-1250` test the fresh `A00` estimator and replace
several failed variance-control assumptions with physical-chart and
pole-subtraction structure.

The main outcomes are:

- the fresh control pilot exposed grouped/direct residue ownership that the
  retrospective control did not resolve;
- exact grouped-zero and permutation-chart results were obtained where their
  hypotheses applied;
- a completed `520/520` replacement run rejected the frozen controlled scale
  without retuning;
- a one-chart constant cross-sector blend was rejected;
- native-chart replication, a local double-residue identity, analytic outer
  pole subtraction, and a complete active-family pole atlas were constructed;
- bounded dynamic-root integrations passed their declared internal gates, but
  did not produce a claimable full coefficient.

These are methodological advances and no-go results, not a value of `p8`.

## Topology Correction

Public checkpoints `1251-1266` move from fixed-source pole continuation to
causal, branch-aware outer integration.

The first nested two-angle cubature exposed a failed topology gate. Uniform
adaptive rebuilding was computationally unsuitable, so the route was replaced
by a reciprocal-projective chamber-boundary tracker. That tracker changed all
legacy Q03 and Q05 interval maps in the tested sets.

The first corrected inner slices were:

```text
Q03 corrected =  252.15977839895186 + 0.0006007742174577774 i
Q03 fixed     =  274.9820027716404  + 0.0006347940663025060 i
relative change = 0.0829953383953

Q05 corrected = -160.8231942911625  - 0.0006217358495971361 i
Q05 fixed     = -141.4775803303836  - 0.0005548604602885521 i
relative change = 0.136739785311
```

Applying only those two corrected nodes shifted the order-9 outer value from
approximately

```text
18.92637003551503 - 0.5644314513109665 i
```

to

```text
15.132221063537779 - 0.5644405295938296 i,
```

a relative shift of about `20.04%`. Checkpoint `1267` then rebuilt the complete
order-5 backbone with paired reciprocal-projective transport. Checkpoint
`1268` rebuilt Q01 and Q07 and assembled all nine corrected nodes.

The resulting corrected cubatures are:

```text
order 3 = 12.397121012079863 - 1.911653332637763 i
order 5 = 35.259983428209416 - 1.147062885779279 i
order 9 = 15.035421321258777 - 0.5644406519310965 i
```

The order-9 inner integrations are stable, with a maximum 128-to-512 relative
change of about `7.19e-7`. The outer sequence is not converged:

```text
order 3 -> 5 relative change = 0.6484276629388201  (gate 0.2)
order 5 -> 9 relative change = 1.3447385187509027  (gate 0.2)
degree 5-8 Chebyshev tail    = 0.7675812734797481  (gate 0.2)
```

The corrected calculation therefore replaces the earlier hybrid value but
does not promote a final outer integral or `p8` coefficient.

## Current Decision

```text
HOLD_FULL_ORDER9_CUBATURE__LOCALIZE_CORRECTED_OUTER_PROFILE
```

The next exact task is to separate the even and odd outer profile under the
symmetric integration rule, verify the exact cancellation of odd Chebyshev
modes at the integral level, and then run the nested order-17 rule on its eight
new nodes. The original convergence gates stay failed unless that higher-order
calculation closes them.

## Empirical and Large-Scale Boundary

The completed frozen 12-seed galaxy-formation gate remains a metric split:
its `q` component is MTS-directed, RMSE is unresolved, and the joint result is
not model preference. The occupied/composite retarded motion response,
preparation law, Hilbert stress, and cross-arena scale map remain open. Those
questions are not resolved by the crossed-`hhh` calculation.

## Claim Boundary

No completed unified theory, ultraviolet completion, first MTS-specific
`p8` coefficient, or empirical replacement of GR, dark matter, CDM, or
Lambda-CDM is claimed. The exact selected local theorem survives; the current
higher-operator calculation is materially better controlled but still open.
The full private-sequence audit trail through checkpoint `5252` is now
published separately under `research-ledger/` without changing this claim
ceiling.
