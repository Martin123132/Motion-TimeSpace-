# Publication Notes - 2026-07-24

## Purpose

This update publishes the complete contiguous research record produced after
the checkpoint-`1192` preregistration. It includes successful derivations,
corrected sign errors, no-go results, empirical robustness tests, and the
current coefficient-estimator boundary. It is not a completed-theory or
model-preference release.

## Scope

The established public offset is `3984`. Private checkpoint `5176` is
refreshed as public checkpoint `1192`; private checkpoints `5177-5214` become
public checkpoints `1193-1230`.

| Item | Count | Size |
|---|---:|---:|
| Checkpoint Markdown documents | 39 | 0.394 MiB |
| Checkpoint Python scripts | 40 | 2.639 MiB |
| Compact validation CSV/JSON files | 39 | 0.400 MiB |
| Hash-verified checkpoint artifacts | 118 | 3.433 MiB |
| Final compact checkpoint-1192 snapshot files | 69 | 73.242 KiB |

The update also refreshes the README, claim ceiling, project map, local gate
map, research-programme index, protocol verifier, and dated status.

## Included

- the completed frozen 12-seed formation result and compact per-seed outputs;
- the exact 2PI Schur/Ward reductions and recorded no-go routes;
- the scalar-clock no-go and minimal coframe requirement;
- one common CTP translation-gauge/coframe parent action;
- direct parent-scalar cosmology and physical-sound-horizon robustness gates;
- invariant mass/state and cross-arena scale-separation theorems;
- source-complete coframe variation and the full two-derivative PPN result;
- the source-selected exact GR+SM+Maxwell consistent truncation;
- the complete fresh crossed-`hhh` nonclaim pilot;
- the guarded source-separated Cauchy zero theorem;
- the identical-graviton permutation control variate and its retrospective
  variance gate.

## Excluded

- virtual environments and `__pycache__`;
- large raw run directories, numerical caches, arrays, and logs;
- the local `functional_rg` cache except for the curated checkpoint-1192
  compact snapshot;
- third-party source bundles and datasets;
- any statement of overall MTS, CDM, dark-matter, GR, or Lambda-CDM
  preference;
- any claim that the first MTS-specific `p8` coefficient is complete.

## Integrity

The existing publication helper selected the contiguous checkpoint range with
an extension and size allowlist, checked destination collisions, and verified
all `118` copied files against their private sources with SHA-256. A separate
allowlisted helper copied and hash-verified `69` compact final-ensemble files.

The portable checkpoint-1192 verifier confirms:

- all five frozen preregistration hashes remain unchanged;
- the schedule still contains 12 unique confirmatory seeds;
- the excluded pilot remains outside inference;
- all 12 compact seed snapshots are present and nonclaim;
- the final metric-split statistics match the frozen output;
- all 12 checkpoint validation rows pass.

The protected `formalization-workbench` digest remains
`b0f45c104b5d1ab4762e3f96f23c6e2b2a7afec8c70612879bda90c90308a758`.

## Public Claim

The update supports a stronger exact selected local-limit statement and a
more explicit parent-action boundary. It does not support a completed unified
theory, an all-operator local-GR theorem, ultraviolet completeness, or
empirical replacement of standard baselines.
