# MTS Status - 2026-07-22

## Executive Position

MTS remains a work-in-progress research programme, not a completed unified theory. Its strongest current result is the conditional weak-local chain already published on July 21: within the declared integrated metric-density `H`, exact-Diff/BRST parent and selected reflection-even branch, one universal massless spin-2 residue yields Einstein gravity, Newton's weak static limit, leading `PPN gamma=beta=1`, and the Maxwell/Lorentz/stress/Poynting chain.

The July 22 advance is methodological rather than a new theory claim. The matched MTS/CDM formation test now has a frozen, symmetric 12-seed protocol published before confirmatory seeds 2-12 are inspected.

## Frozen Ensemble

- Private checkpoint `5176` maps to public checkpoint `1192`.
- The earlier high-mode seed `517500409` is declared a pilot and excluded from confirmatory inference.
- The 12 confirmatory seeds, their order, the physics inputs, numerical grids, scoring rules, stopping rule, and final decision rule are frozen.
- Protocol SHA-256: `64529978cc452b302a5f09f52fff4be7af2ae8ef5cd64f29a8352005925fb7e7`.
- Runner SHA-256: `c15616bf4eedd9fae55c11c67a5f9064c88f6af0321ec89697f167d228ac8303`.
- The compact machine-readable protocol, schedule, freeze record, source hashes, and seed-1 outputs are published under `research-programme/protocols/1192/`.
- The six-seed point is descriptive only; formal model preference is assessed once, after all 12 seeds.

## Current Result

One of 12 confirmatory seeds is complete.

- `D_q = d_q(MTS) - d_q(CDM) = 0`: both branches lie inside the inherited `q` band.
- `D_R = RMSE(MTS) - RMSE(CDM) = -0.0053882279920328124`: RMSE is slightly lower for MTS in this realization.
- The outcome is a metric split, not a joint win.
- Current verdict: `INCOMPLETE_PREDECLARED_ENSEMBLE_NO_PREFERENCE_ALLOWED`.

This result contrasts with the excluded pilot and therefore reinforces, rather than resolves, the concern about high-mode realization sensitivity.

## What Is Still Missing

1. Complete the remaining 11 frozen paired seeds without changing the runner, schedule, estimands, or inference rule.
2. Derive the occupied-state retarded motion self-energy `B K_ret^-1 B_dagger` from the parent CTP Hessian.
3. Derive the large-scale amplitude, transition scale, and exponent from source history without per-galaxy fitting.
4. Extend the weak-local theorem to compact strong fields, radiation, and back-reaction.
5. Decide whether integrated `H`, Diff/BRST, and visible field content are fundamental parent data or emerge from a simpler motion/time/space construction.
6. Keep cosmology explicitly at closure level until its fitted response is derived from the same parent dynamics.

## Claim Boundary

No empirical preference over CDM, GR plus dark matter, or Lambda-CDM follows from one confirmatory realization. Even a final ensemble preference would apply only to the locked UGC09133 formation gate, not to the full MTS programme.

## Next Calculation

Run confirmatory seed 2 (`2557716234`) only through the frozen verifier. Do not modify the protocol or use the outcome to alter later seeds.
