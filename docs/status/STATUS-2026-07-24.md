# MTS Status - 2026-07-24

## Executive Position

MTS remains a work-in-progress research programme, not a completed unified
theory. The strongest current result is now sharper than the earlier weak
local statement: on one source-selected, locally silent motion state, the
nonlinear two-derivative restriction of the common parent action is an exact
classical consistent truncation to GR + Lambda + Standard Model + Maxwell.

The result is exact within that declared branch and derivative order. It is
not an all-operator, all-state, strong-field, or ultraviolet-complete theorem.

## Parent Structure

Public checkpoint `1219` assembles one CTP translation-gauge/coframe parent
functional containing the retained gravitational, electromagnetic, visible,
motion, boundary-state, and controlled-EFT sectors. Checkpoint `1204` proves
why a coframe is necessary: four scalar clocks with a fixed internal metric
give a flat pullback metric when nondegenerate and a degenerate metric
otherwise.

The coframe and visible field representations are therefore honest parent
premises. They have not yet been derived from motion, time, and space alone.

## Exact Selected Local Branch

On

```text
chi=0,
nabla chi=0,
rho_local=rho_0,
```

checkpoint `1227` proves:

- the motion equation and stress vanish;
- linear metric-motion, photon-motion, and visible-motion blocks vanish;
- the complete two-derivative action reduces exactly to
  GR + Lambda + Standard Model + Maxwell;
- the same Hilbert source yields Einstein gravity, Newton's equation,
  inverse-square motion, geodesics, and lensing;
- the same electromagnetic action yields Maxwell equations, Lorentz force,
  Hilbert stress, and Poynting flux;
- all ten standard constant PPN values equal the GR vector.

The relation `G_N=1/(8 pi M_R^2)` is derived and reused universally. Its
absolute numerical value remains one calibration datum, and `Lambda_cal`
remains a renormalization datum.

## Higher-Operator Boundary

The exact theorem does not discard `C3`, curvature-photon, nonlocal, or
`p8+` terms. They are separately matched or bounded. The current first
crossed-`hhh` coefficient pilot is deliberately nonclaim:

- all `280/280` locked jobs converged;
- the real candidate remained unresolved;
- an exact guarded Cauchy theorem repaired only strict cross-additive residue
  collisions;
- a symmetry-fixed identical-graviton control reduced the retrospective
  topological-local real variance by a factor `7.4976`;
- a fresh independent control pilot is still required before scaling.

## Galaxy Formation Gate

The checkpoint-`1192` protocol was frozen publicly before confirmatory seeds
2-12 were inspected. All 12 seeds are now complete:

```text
mean D_q    = -0.0392272547
95% D_q     = [-0.0625657352,-0.0167294234]
q p-value   = 0.01171875

mean D_RMSE = 0.0006039774
95% D_RMSE  = [-0.0012737961,0.0025214142]
RMSE p-value= 0.560546875
```

The `q` component is MTS-directed, while RMSE does not distinguish the
models. Joint outcomes are 3 MTS wins, 0 CDM wins, and 9 ties/splits, with an
insignificant joint sign result. The locked verdict is:

```text
STATISTICAL_DRAW_OR_METRIC_SPLIT_WITHIN_THIS_LOCKED_FORMATION_GATE
```

This is not overall MTS or CDM preference.

## Cosmology Boundary

The parent scalar has been run against matched Pantheon+, DESI DR2 BAO,
growth, and compressed-CMB likelihoods with a physical sound horizon. These
are useful robustness calculations. They still fit relevant mass/state
coordinates, use compressed rather than official full CMB likelihoods, and
do not establish the full field theory.

The fitted homogeneous cosmology mass and the conditional galactic
collective scale cannot be the same elementary one-pole mass. The surviving
galactic interpretation is collective/environmental and still needs a
parent-owned retarded response and preparation law.

## Remaining Gates

1. Test the checkpoint-`1230` control on fresh independent `A00` events.
2. Derive or bound the first canonical MTS-specific `p8` on-shell coefficient.
3. Derive the occupied/composite retarded motion response, its Hilbert stress,
   scale map, and preparation law.
4. Extend the selected local theorem to higher operators, compact bodies,
   horizons, and radiation/back-reaction.
5. Derive the parent coframe/visible ontology from simpler MTS primitives or
   retain it explicitly as fundamental.
6. Derive the absolute gravitational/vacuum scales or keep their
   renormalization/calibration status explicit.

## Claim Boundary

No empirical replacement of GR, dark matter, CDM, or Lambda-CDM is claimed.
The public result is a materially stronger and more coherent field-theory
programme, with an exact selected local limit and clearly isolated remaining
derivation gates.
