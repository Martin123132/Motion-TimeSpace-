# Publication Notes - 2026-08-29

## Purpose

This update brings the public research audit from private checkpoint `5336` through private checkpoint `5417` while preserving the established claim ceiling and public checkpoint offset.

## Scope

The public offset remains `3984`. Private checkpoints `5337-5417` map to public checkpoints `1353-1433`.

| Item | Count | Size |
|---|---:|---:|
| Checkpoint Markdown documents | 77 | 0.270 MiB |
| Python scripts | 88 | 2.730 MiB |
| Compact residual CSV/JSON files | 47 | 0.100 MiB |
| Total copied research artifacts | 212 | 3.097 MiB |

Four private checkpoint numbers in the interval have no Markdown document. Their public numbers remain absent rather than being fabricated or silently renumbered.

## Included

- the D4 regulator, endpoint, contour, Cauchy, pole-primitive, and regular-away derivation trail;
- the exact closed-parent local-state-attractor no-go and its effect on the local-GR claim boundary;
- scripts associated with the private `5337-5417` range;
- compact CSV/JSON residual and validation artifacts from the established `mts_residuals` allowlist;
- updated public status, project map, claim ceiling, bounded catalogue shards, dated range manifest, and Git-index publication inventory.

## Excluded

- virtual environments, bytecode, caches, logs, and temporary files;
- third-party datasets, large arrays, and raw numerical run folders;
- the local `functional_rg` tree, including resumable production state and large generated products;
- any direct force-push or history rewrite of the protected default branch.

## Integrity

The existing synchronization tool performed a dry run, applied an extension/size allowlist, detected no destination collisions, and SHA-256 verified all `212` copied artifacts. `PUBLICATION-MANIFEST-2026-08-29.csv` records each source-relative path, public path, byte count, and SHA-256 digest. Catalogue shards are regenerated with at most `250` direct links each, and the staged Git-index inventory records every committed path independently of GitHub folder and pull-request display caps.

## Public Claim

The exact selected two-derivative GR + Standard Model + Maxwell branch remains the strongest public result. The new D4 work is published as an incomplete higher-operator proof: `29/240` contour path jobs are certified, but regular-away W3, combined W3, the regulator limit, the UV coefficient, and full MTS remain open.
