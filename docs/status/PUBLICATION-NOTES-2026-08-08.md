# Publication Notes - 2026-08-08

## Purpose

This update extends the contiguous public record from checkpoint `1266` to checkpoint `1352` while preserving the nonclaim posture and audit-first workflow. It carries forward the strongest local GR/Newton/Maxwell result and publishes the latest higher-operator topology, regulator, and fold-equivalence work.

## Scope

The established public offset is `3984`. Private checkpoints `5176-5336` map to public checkpoints `1192-1352`.

| Item | Count |
|---|---:|
| Checkpoint Markdown documents | 78 |
| Python scripts | 84 |
| Compact validation CSV files | 76 |

## Included

- checkpoints `1267-1352`, including boundary reconstruction, outer-repair, pole atlas refinement, and the 2026-08-06 fold-equivalence branch;
- 84 matching `Y5_R2FR_*.py` scripts in the same numeric window;
- 76 compact residual/validation CSVs with new status labels and comparison data;
- one `maths-exploration-bridge-source-lock` note (`research-programme/source-intake/maths_exploration/5335/`);
- updated catalogue shards and publication inventory for the enlarged tree;
- synchronized repository index docs (`README.md`, `PROJECT_MAP.md`, `research-programme/README.md`, and `CLAIM_CEILING.md`) to reflect `1352` and the current unresolved-carry-forward posture.

## Excluded

- virtual environments, `__pycache__`, and runtime caches;
- raw run folders, numerical caches, and large arrays;
- third-party/source bundles and the local `functional_rg` cache.

## Integrity

- all updated `Y5_R2FR_*.py` scripts in the numeric window compile (`84/84`);
- catalogue sharding regenerated; catalogue links remain bounded to ≤250 links each;
- publication inventory regenerated to include the expanded checkpoint/script/residual set;
- no new claims beyond the existing exact local branch are promoted in the publication notes.

## Public Claim

The update preserves the strongest claim of an exact selected local GR + SM + Maxwell branch and explicitly keeps the unresolved crossed-`hhh`/outer coefficient programme nonclaim.
